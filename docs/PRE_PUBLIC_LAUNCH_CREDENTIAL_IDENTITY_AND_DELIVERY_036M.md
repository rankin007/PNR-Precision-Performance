# Sprint 036M Pre-public Launch Recovery

Status: closed `prelaunch-recovery-blocked-clean` after same-Architect repair-diff PASS. ProtectedWindow remains forbidden; provider/private/trainer execution never started.

This contract recovers the Sprint 036K blocked-clean boundary without changing JWT signing, sessions, schema, RLS, roles, permissions, commerce, public enquiry, DNS or email policy. It governs one paired Supabase publishable/secret migration, independent dispositions for seven credential classes, two privately exact-matched identities, one real trainer and one exact-source Production candidate with five fixed aliases.

Opaque Supabase publishable and secret keys are sent as `apikey`. Only an `Authorization` value exactly duplicating an opaque application key is removed. An authenticated user's different JWT remains in `Authorization`; PKCE code exchange and request-scoped session construction remain unchanged.

## Private-window sequence

Protected values, addresses, codes, identity rows, cookies and provider payloads stay in a non-transcribed local ConsoleHost or signed-in provider interface. They are never pasted into chat, evidence, commands visible to other people or durable files.

1. Reserve one uninterrupted 90-minute window with the accountable owner/operator, one real trainer and Builder verification available. The trainer waits until step 8.
2. Do not run `ProtectedWindow` under this closed sprint. A later reviewed corrective authority must first provide finite native consumer/old-deployment and credential-class readback; `SelfTest` and `CapabilityGate` do not authorize a people window.
3. The finite native adapters independently confirm exact Supabase/Vercel authority and inventory every declared legacy consumer: repository, current deployment, old deployments, jobs, webhooks, integrations, Edge Functions, database webhooks and `pg_net`. Operator-entered counts, completeness booleans or success attestations are not authority. Any unavailable inventory/readback blocks clean before mutation.
4. Privately select exactly one publishable and one named secret key. For each key, seal immutable provenance as `created-this-sprint` or `pre-existing-selected`. Never delete a selected pre-existing key during compensation.
5. Install the public/server bindings together, create at most one alias-free exact-source candidate, and run public, authenticated-user and server-admin no-business-effect probes. Require the fixed five-alias readback before legacy deactivation.
6. Latch the deactivation attempt before it is called. Independently prove both legacy keys disabled and rejected while replacement keys and current Auth remain compatible; do not change JWT signing or rotate sessions.
7. Finite native adapters disposition the other declared credential classes independently. An absent Vercel row, source substring or operator-entered outcome is not provider-native revocation proof. An unavailable adapter/readback lands `blocked-retained`; any class that can affect the trainer path blocks trainer start if uncertain.
8. Privately exact-match and owner-disposition both identities. Delete only after the 25-migration/50-dependency manifest is zero/owned, global session invalidation is proved, Auth is deleted last and absence is reread.
9. Privately supply one separately masked, owner-approved exact trainer Auth ID. The controller independently reads that exact Auth identity, requires exactly zero matching `public.users` rows and captures `last_sign_in_at` before any graph write. Then create the exact-owned eight-row replacement graph. While the same protected process remains alive, the trainer alone enters their address in the phone UI, presses Send once and submits the received code once. The wrapper/controller never receives the address, code, cookie or phone session and never calls `signInWithOtp`, `verifyOtp`, creates a link or bridges a session.
10. The trainer performs portal, dashboard, synthetic-horse review, no-submit review, generic wrong-horse denial, sign-out, signed-out denial and fresh-anonymous denial on a supported phone. The wrapper accepts only finite yes/no observations; controller readback is exact-ordinal/session-state only. Record sanitized time classes only; make no under-60 or representative-completion claim.
11. Remove the eight-row replacement graph and prove each row absent. Retire the eight-row prior graph only after replacement success; otherwise retain it unchanged and withhold the target. Reconcile session, application, Auth, Storage, deployment, alias, binding and provider residue.
12. Rerun focused 360 plus retained 943, quality/privacy/scope/lock/migration gates. Fresh critical inspection must pass before closeout.

Expected working allocation is 20 minutes for provider/key baseline, 25 minutes for bindings/candidate/probes/aliases, 10 minutes for legacy and credential readback, 15 minutes for identity authority, and 20 minutes for the trainer journey and cleanup. Stop rather than overrun a ceiling or guess an uncertain identity/provider state.

## Compensation and landing

Before deactivation, restore the public/server values across the complete current Vercel target set, restore the fixed five aliases to the exact prior deployment, remove the attempted exact-owned candidate, and delete only sprint-created keys, followed by independent target/key/candidate readback. The URL row stays unchanged. After any deactivation attempt, never restore or delete based on an exception; independent provider readback determines the safe landing. Replacement graph creates, replacement cleanup deletes and prior graph retirement deletes are separate ceilings of eight each, maximum 24, with independent absence for every delete.

Allowed outcomes are the complete-clean target, trainer-complete/credential-partial clean, blocked clean, compensated clean or blocked material. Local readiness alone is not launch readiness.

## Protected transport and exact source

- The outer ConsoleHost must be visible, interactive and non-transcribed. Redirected input/output, a concurrent or consumed window, wrong root/HEAD/branch, conflicts and unsafe restart are refused before the child or marker is created.
- One wrapper-owned child remains alive for all nine phases. Masked wrapper memory writes bounded private JSON lines to child stdin. The child emits one bounded allowlisted sanitized JSON result per request through child stdout. Child stderr is redirected into a fixed 4097-byte in-memory drain; any byte produces only a fixed sanitized failure and its content is never emitted.
- Protected values never enter arguments, environment variables, clipboard, history, temporary/repository files, outer redirected I/O, child output or evidence. Owned buffers are disposed where supported; no universal-erasure claim is made.
- The accepted source is Git object `bfeb0b23c339b819f5dbcd4fe28d61c7a4dd9570`, exported directly to one randomized `036M-exact-source-*` system-temp directory. Its full path/mode/blob set is checked. Among `.env*`, only exact mode-100644 blob `d790ad8998a4919d8ae5f308904047512b156f69` at `.env.example` is allowed after assignment-aware placeholder/protected-pattern validation.
- Only private `.vercel/project.json` is added, then the explicit directory is deployed with `deploy --prod --skip-domain --yes`. Dirty canonical files, checkout/worktree, redeploy and automatic stable routing are refused. The exact temp path is removed and independently absent before proceeding.
- Interruption before `legacy-deactivate-readback` runs paired-key/binding/candidate compensation and reread. At or after that latch, the process permits predecessor readback plus exact-owned trainer-fixture cleanup only; it never attempts predecessor restoration or retry.
