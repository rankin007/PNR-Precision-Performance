# Sprint 029S Acceptance

- [x] A001 Canonical current directory and Git top level equal the permanent repository; start HEAD is d822c027c58ad88ec7472e35986e7a33d6a3d6c9; branch is exact 029S; inherited dirt is classified and preserved.
- [x] A002 The one Pack dry-runs, applies and rereads exactly four traversal-free 029S files.
- [x] A003 Builder returns the exact no-edit critical plan with task contract, files, guards, A001-A032, commands, observed baseline/target arithmetic, closeout, Pack corrections and blockers.
- [x] A004 A genuinely fresh reviewer returns pass on the exact plan within three decisions.
- [x] A005 Only reviewed 029S implementation and standard closeout files change; Product/auth/migration/data/generic SMTP/Auth/trainer/commerce/alias source remains unchanged unless a required within-intent correction is approved.
- [x] A006 JavaScript accepts only a plain HTTPS allowlisted origin with no user information, path, query, fragment or explicit port.
- [x] A007 PowerShell accepts only HTTPS, no user information, slash-only path, no query/fragment, allowlisted host and port 443.
- [x] A008 Red-first cases distinguish valid origin from explicit 443, non-default port, user information, path, query, fragment, wrong scheme and wrong host; a scheme-and-host-only implementation fails.
- [x] A009 The provider projector has a fixed closed output schema containing only approved booleans/counts/classes and rejects unknown page/shape/count/access state.
- [x] A010 Synthetic full snapshots containing protected identity, address and token canaries yield no canary text, fragment, name or extra field; changing canaries does not change the safe projection.
- [x] A011 Live signed-in snapshots remain only inside the browser runtime and only tested projections reach agent output; no raw DOM, screenshot, console, broad page state or protected account detail is emitted.
- [x] A012 The 616/616 inherited baseline is observed before edits and exact new 029S target arithmetic passes without replacing retained coverage.
- [x] A013 PowerShell parse/self-test, focused/retained tests, typecheck, zero-warning lint, Production build and mapped static/JSON/encoding/privacy/scope scans pass.
- [x] A014 Protected scans precede content-emitting diff/inspection and emit only fixed scan metadata; scanner-clean files form the exact candidate manifest.
- [x] A015 Fresh identity-blind preflight proves one verified expected domain, two existing sending-access keys, zero full-access keys, no target key, no raw-secret shape and no protected output.
- [x] A016 Fresh Vercel preflight proves the exact project, zero dedicated SMTP rows, zero temporary auth rows, unchanged generic-row metadata and five aliases on accepted 036L without values.
- [ ] A017 Resend and Vercel create/Copy/paste/save/dismiss/clear controls are prevalidated before token creation through the bounded projection.
- [ ] A018 Exactly one Resend key named Precision Performance public enquiry 029S is created with sending access restricted to precisionperformance.com.au; existing keys remain unchanged.
- [ ] A019 While the token is visible no snapshot, screenshot, evaluate, log or clipboard read occurs; direct blind transfer ends in a Sensitive Production-only blank-branch pass row, then token view is dismissed and clipboard cleared.
- [ ] A020 Exactly four new Production-only blank-branch dedicated SMTP rows exist; public structural values match Resend authority and existing generic rows remain unchanged.
- [ ] A021 A new fixed 029S Credential Manager target proves absence, encrypted synthetic round trip and deletion with fixed sanitized output.
- [ ] A022 The live bearer has at least 256 random bits, is never human-entered/output/file/argument data and provisions exactly three hash/window Production rows through stdin/process API with a window at most fifteen minutes.
- [ ] A023 Exactly one Production-targeted deployment is created from exact 029S branch/workspace with automatic domains disabled; it is Ready and zero aliases.
- [ ] A024 Immutable public/privacy/protected/API/disabled-commerce checks pass without storing data or calling /api/enquiries.
- [ ] A025 Exactly one authenticated smtp-preflight returns result smtp-preflight, status ready, providerClass resend and errorClass null with no protected detail.
- [ ] A026 After expiry exactly one same-bearer request returns sanitized HTTP 404 before the SMTP handler; no second no-send attempt occurs.
- [ ] A027 The three temporary project rows and fixed local credential are removed and proved absent; retained candidate config is expired and unusable.
- [x] A028 Evidence proves zero /api/enquiries calls, email/notification attempts, mailbox/log inspection, enquiry/bucket/Supabase/migration changes and alias changes.
- [x] A029 On non-target result no retry occurs; only exact newly owned 029S resources are compensated and clean fallback is claimed only after complete proof.
- [x] A030 A fresh inspector distinct from Builder and plan reviewer returns pass within three decisions after judging criteria, diff, real evidence and closeout plan.
- [x] A031 Durable closeout records simple plain-English outcome, Flight evidence, findings/substitutions/residuals, 029R conditional state, roadmap/briefing reconciliation and STATUS sprint-closed.
- [x] A032 Final ordered scanner-clean disk/Git/provider/Vercel/candidate/live-routing readback is consistent; no commit, push, merge, PR or history rewrite occurs.

The target proves only protected, authenticated, no-send transport readiness. It does not prove email delivery, inbox receipt, public enquiry behaviour or public activation.

## Closeout disposition

Sprint 029S closed `preflight-origin-provider-projection-and-readiness-recovery-blocked-clean`. Clean means no new Resend credential, dedicated SMTP configuration, temporary authentication configuration, local live credential, Production target or live-routing residue. It does not mean that no deployment exists.

A001-A016 and A028-A032 are satisfied. A017-A027 were not accepted or performed as applicable. The attempt stopped before any key, environment row, bearer, authenticated no-send request, public enquiry request or email action. Exactly one unintended Ready Preview deployment, `dpl_7MTexxU6RecGHZvCE9BukUwZU6Hx`, remains non-Production, has zero expected public aliases and is retained inert under the Pack fallback. No retry or second deployment occurred.

The cause was an ad-hoc read-only Vercel inspection helper whose formal parameter was named `$Args`. That name collided with PowerShell's automatic variable, so the intended inspection arguments were lost and bare Vercel ran. Builder reported the result and stopped immediately. Final evidence shows dedicated rows `0`, temporary rows `0`, the fixed local credential absent, all five generic rows unchanged, all five aliases on accepted Sprint 036L and the two prior enquiry candidates with no public aliases.

Local origin hardening and identity-blind projection are accepted. Red-first suites passed 64/64 and 64/64; retained proof passed 616/616, for 744/744 counted assertions. PowerShell parse/self-test, typecheck, zero-warning lint, 28-page Production build, mapped validators, seven-file clean scan and seven-entry manifest passed. Critical plan review passed decision 2/3. Fresh critical inspection passed decision 1/3 with no blocking finding.

Sprint 029R remains conditional and not ready. Planned Sprint 029T is discovery and future-Pack work only; no Pack exists. It must keep the accepted 029S origin/projector foundation, remove the `$Args` helper hazard, re-establish zero-resource ceilings and define a new one-attempt readiness boundary before 029R.
