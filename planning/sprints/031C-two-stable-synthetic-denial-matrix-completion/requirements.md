# Sprint 031C — Two-Stable Synthetic Denial Matrix Completion

## Outcome

Expand only the Sprint 031 synthetic fixture ceiling from one stable/three horses to two stables/four horses, prove genuine rendered cross-stable denial with the protected Preview transport already proven in Sprint 031B, then complete the unchanged 25-case authenticated matrix and restore exact-owned Auth/application/Storage state and owned processes to zero.

Sprint 031C is a corrective completion sprint under core Sprint 031. It does not reopen Sprint 031 or 031B, change product behavior, redefine access, or authorize new schema, RLS, permission, provider, domain, commerce or Production scope.

## Workflow profile

Strict. The sprint creates bounded synthetic Auth/application/Storage state in a remote non-Production environment and executes rendered cross-role/RLS denial. Exact target, fixture ownership, protected-session, output-redaction and cleanup controls remain mandatory.

## Required baseline

Builder must apply this Pack from the canonical project and execute it in:

`C:\tmp\pnr-027b-completed-product-lineage-reconciliation`

Required prior outcome:

`protected-preview-contract-expansion-required-clean`

Freeze and record the exact Sprint 031B worktree, branch, HEAD, complete modified/untracked manifest, migrations, lockfile and source fingerprints. Preserve all completed Sprint 025–031B work except the explicit 031C fixture-ceiling/harness/evidence/planning additions and narrow in-contract corrections.

Reverify before mutation:

- exact approved Singapore Supabase project and `ap-southeast-1` region;
- exact remote/local ledger `0001`–`0021`;
- exact generated non-Production Vercel Preview with no Production custom-domain alias;
- initial new-run Auth/application/Storage `0/0/0` and no run/recovery anchors;
- zero owned browser/driver/helper processes;
- unchanged genuine-cookie Preview transport, writer bootstrap and rendered revocation contracts from 031B;
- frozen Sprint 031 25-case matrix and unavailable clinical/audio/commerce boundaries.

Refuse any target, ledger, Preview binding, source identity or initial-state mismatch.

## Exact fixture-ceiling amendment

This Pack supersedes only the one-stable/three-horse ceiling in the Sprint 031 control harness for Sprint 031C execution.

Approved maximums:

- synthetic stables: `2`;
- synthetic horses: `4`;
- synthetic Auth identities: `3`;
- commerce/order/payment fixtures: `0`;
- evidence/test/assignment rows: the minimum exact-owned rows required by the unchanged matrix.

Required fixture topology:

- Stable A contains three synthetic horses representing complete, incomplete and empty/unavailable workspace states.
- Stable B contains one synthetic denial-target horse.
- The writer is authorized only for Stable A/its approved horses under existing assignment/membership contracts.
- The Stable B horse has no writer authorization and exists only to prove cross-stable list/count/order/route/action/evidence denial.
- Administrator and read-only identities use only existing roles/permissions and the minimum assignments required by their matrix cases.

Do not create a fourth Auth identity, third stable, fifth horse or any commerce fixture. Do not use a non-owned existing stable/horse as the denial target. Do not weaken cross-stable denial into wrong-horse denial inside the same stable.

Update `scripts/test-field-trial-controls-031.mjs` or introduce a versioned 031C control without falsifying historical Sprint 031 evidence. The executable control must accept exactly the amended ceiling and still reject values above it. Historical records must continue to state the original limit used in Sprints 031/031B.

## Proven transport reuse

Reuse the Sprint 031B protected rendered transport unchanged unless a deterministic compatibility correction is necessary. Before creating fixtures, rerun:

- credential-free Chrome launch/page control;
- three-context isolation;
- synthetic cookie delivery/server receipt;
- simulated revocation;
- protected-output allowlist/redaction;
- timeout and owned process cleanup;
- exact Preview/Supabase/ledger/zero preflight.

The existing process-memory Vercel automation bypass may be reused read-only under the 031B contract. It must not be persisted, printed, mutated at provider level or used against Production.

No auth bypass, debug login, middleware exception, token output, browser-profile reuse or protected value on disk/arguments/logs/screenshots/clipboard/conversation is permitted.

## Denial gate before full matrix

After transport and local gates pass:

1. Create the minimum exact-owned Stable A, Stable B, writer and horse fixtures needed for denial.
2. Install the genuine writer session into an isolated Preview browser context.
3. Prove the writer can access its authorized Stable A horse.
4. Prove Stable B and its horse do not appear in stable-dashboard rows, horse lists, counts, ordering, summaries or action targets.
5. Prove direct Stable B horse route access and any supported write/evidence action deny safely.
6. Prove the denial target cannot influence the writer's derived incomplete/change/next-action state.
7. Record fixed-class/count-only results.

If any cross-stable data or action is exposed, stop as a material RLS/authorization failure, compensate all exact-owned state and close restored-clean. Do not change RLS, permissions or assignment semantics inside 031C.

## Frozen matrix completion

The Sprint 031 matrix remains exactly 25 cases. Sprint 031B passed record writer, revocation and cleanup zero before its material stop. Sprint 031C must:

- rerun record writer and revocation as shared transport/authorization controls;
- prove cross-stable denial through the new two-stable topology;
- execute all cases not run in 031B: cases 1–2, 4, 6 and 8–24;
- run cleanup zero as the final case;
- produce one final 25-row matrix ledger that distinguishes fresh 031C evidence from preserved historical 031B evidence, while every case has current 031C executable coverage.

No case may be removed, merged, downgraded, marked not applicable or satisfied only by static/historical evidence when rendered execution is required.

Required coverage remains:

- anonymous, administrator, writer, read-only, inactive and revoked access;
- cross-stable denial;
- phone/desktop capture, review, correction, retry, duplicate prevention and persistence;
- results, history, compatible/incompatible change and trends;
- dashboard/workspace complete, incomplete and empty states;
- governed evidence lifecycle and denial;
- unavailable commerce, clinical authority and application audio;
- keyboard/focus, responsive reflow, failure/retry and cleanup zero.

## Correction scope

Builder may change only:

- fixture ceilings/topology, ownership and cleanup harness logic;
- 031C focused tests/reporters/verifiers and validation registration;
- deterministic protected transport compatibility corrections, if required;
- narrow in-contract application defects discovered after the denial/full matrix starts;
- 031C evidence and standard planning closeout files.

Any application correction must preserve schema, migrations, RLS, roles, permissions, assignments semantics, domain authority, evidence lifecycle, commerce-disabled behavior and all unavailable boundaries. Rerun the affected case plus cross-stable denial, revocation and cleanup controls.

## Approved external actions

After local and protected-transport gates pass, Builder may:

- reuse the exact generated non-Production Preview or deploy a corrected candidate to a new generated Preview;
- create the bounded two-stable/four-horse/three-identity synthetic topology on the approved Singapore project;
- run protected rendered sessions and exact matrix actions;
- remove exact-owned Storage/application/Auth state;
- perform read-only target, ledger, alias, state and process verification.

No Production, Stripe, custom-domain, public-reopening, staging, commit, push, merge or PR action is permitted.

## Explicitly out of scope

- Schema, migration, RPC, RLS, role, permission, membership model or assignment semantics.
- More than two stables, four horses or three Auth identities.
- Real people, horses, stables, customer/payment data, operational files or real participants.
- Clinical thresholds/recommendations, clinical priority, diagnosis/treatment/performance claims or `healthScore` rename.
- Application audio/transcription, new provider, OCR/device integration or CSV.
- Commerce/pricing activation, Stripe, orders or payments.
- Production deployment/configuration/data, domain/alias movement, SEO/indexing or public reopening.
- Dependency/framework upgrades, broad redesign or new matrix behavior.

## Safety and execution standard

Follow the Evidence-Proportional Execution Standard in `AGENTS.md`: stop only for material target, authority, security, privacy, migration, destructive, integrity, Production, scope or cleanup risk; substitute equivalent or stronger safe evidence for unavailable optional tooling; keep deterministic harness/validator/reporter/formatting/encoding corrections in 031C; and use manual intervention only after safe alternatives are exhausted.

For genuine manual intervention, record what is blocked, evidence checked, exact user action, step-by-step instructions and what Builder will verify afterward. Operator action is limited to protected sign-in/MFA/mailbox/provider interaction; never request pasted secrets or technical command execution.
