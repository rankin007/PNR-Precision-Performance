# Builder Handoff — Sprint 031C

You are Builder for Sprint 031C — Two-Stable Synthetic Denial Matrix Completion.

Apply this Pack from the canonical project, verify the four generated files, then execute in `C:\tmp\pnr-027b-completed-product-lineage-reconciliation`. Freeze and preserve the full unstaged Sprint 025–031B lineage. Reverify the exact generated non-Production Preview, approved Singapore Supabase project/region, ledger `0001`–`0021`, new-run `0/0/0` and zero owned processes. Refuse all unexpected, Production or Stripe targets.

Amend only the synthetic fixture ceiling to exactly two stables, four horses, three Auth identities and zero commerce. Stable A holds the complete/incomplete/empty horses; Stable B holds one denial-target horse. Do not use non-owned data or downgrade cross-stable proof to a same-stable case. Keep historical Sprint 031/031B ceiling records unchanged.

Rerun the proven 031B credential-free Chrome transport, isolation, cookie delivery, revocation simulation, redaction, timeout and process-cleanup controls before fixtures. Reuse the genuine-cookie transport and process-memory Vercel bypass without exposing or persisting protected values. Add no auth bypass or product test route.

Execute genuine two-stable denial first: writer-authorized Stable A access must pass, while Stable B must be absent from lists, counts, ordering, summaries and actions; direct route/write/evidence access must deny. On any exposure, stop, clean exact-owned state and do not modify RLS or permissions.

Then rerun writer and revocation, execute every case not run in 031B, and finish with one complete current 25-case ledger. Do not remove, merge, downgrade, mark not applicable or replace rendered cases with static/history. Preserve unavailable clinical thresholds/recommendations, clinical priority, application audio/transcription and commerce.

Correct only narrow in-contract harness or application defects and rerun affected cases plus denial, revocation and cleanup controls. Stop for target mismatch, ceiling/ownership breach, protected output, real data, context leakage, RLS/privacy/integrity failure, schema/RLS/permission/dependency/new-authority scope, Production/Stripe impact or uncertain cleanup.

Clean Storage/evidence first, application rows and both stables dependency-safely, and Auth identities last. Run primary and independent proof of Auth/application/Storage `0/0/0`, absent stable/run/recovery anchors and zero owned browser/driver/helper processes. Confirm migrations, lockfile, Production, Stripe and non-owned state unchanged.

Follow the Evidence-Proportional Execution Standard and use manual intervention only after safe alternatives are exhausted. Finish with exactly one accepted outcome, sanitised denial/matrix/cleanup evidence, an updated Production-release recommendation and refreshed durable planning state. Leave work unstaged and uncommitted; do not push, merge, open a PR, move aliases or deploy to Production.
