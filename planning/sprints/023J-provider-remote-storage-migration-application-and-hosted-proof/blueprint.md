# Sprint 023J - Provider, Remote Storage, Migration Application And Hosted Proof Blueprint

## Phase 1 - Clean Commit And Isolated Baseline

1. Require the separately created clean combined 023E–023I commit.
2. Verify ancestry, paths, reconciled outcomes, ledger and clean status.
3. Create isolated 023J branch/worktree from the exact commit.
4. Apply and verify all four 023J sprint files.
5. Stop cleanly if no valid commit exists.

## Phase 2 - Authority, Provider And Read-Only Preflight

1. Record exact non-production Supabase/Vercel targets, operator and mutation allowlist.
2. Verify exact approved `ap-southeast-1` Singapore region, international-processing/provider/privacy suitability and Storage-object recovery approval.
3. Recheck official primary documentation.
4. Perform count-only target/ledger/schema/conflict/legacy/zero-state preflight.
5. Stop before mutation on any ambiguity, real data, discrepancy, unknown object or recovery gap.

## Phase 3 - Local Contract Completion

1. Add migration `0019` with exact RPC, permission, bucket and Storage-policy contract.
2. Complete signed-direct transfer/finalisation UI and server flow while preserving fail-closed safety.
3. Add focused SQL/RLS/action/UI/harness tests and synthetic fixtures.
4. Align candidate-ledger validators through `0019` with adversarial proof.
5. Run complete local validation and reparse-safe build.
6. Stop `remote-candidate-ready-for-commit` for separate commit instruction.

## Phase 4 - Clean Candidate Commit Gate

1. After separate instruction, stage only approved 023J files.
2. Review staged manifest/diff and rerun integrity checks.
3. Commit locally and verify exact clean SHA.
4. Invalidate remote progression if any source changes afterward.

## Phase 5 - Remote Migration And Storage

1. Repeat exact identity/region/ledger/inventory/conflict preflight.
2. Capture sanitised recovery/pre-apply evidence.
3. Apply only `0018` and `0019` through supported migration tooling.
4. Stop on any failure without repair/manual continuation.
5. Verify ledger, schema, RLS, grants, bucket and policies.
6. Prove anonymous/ordinary direct denials and fail-closed legacy treatment.

## Phase 6 - Protected Configuration And Hosted Preview

1. Configure only matching protected non-production values.
2. Create/configure `CRON_SECRET` without exposing it.
3. Deploy exact clean commit only to named preview/non-production target.
4. Verify no production alias/deployment mutation.
5. Manually prove Cron route; do not activate production schedule.

## Phase 7 - Synthetic Hosted Matrix And Recovery

1. Create bounded synthetic actors/scopes/test/files.
2. Run accepted/denied role, forgery, limit, direct Storage and fail-closed lifecycle matrix.
3. Prove idempotency, replacement failure, reconciliation overlap and audit redaction.
4. Prove one approved synthetic Storage-object recovery cycle without availability.
5. Clean exact owned fixtures Auth-last and prove final `0/0/0`.

## Phase 8 - Closeout

1. Verify rollback/containment and production non-mutation.
2. Produce all eight reviews and update planning truth.
3. Finish with exactly one allowed outcome.
4. Do not push/merge/promote unless separately instructed.
