# Sprint 023J - Builder Handoff Prompt

You are Builder for Sprint 023J - Provider, Remote Storage, Migration Application And Hosted Proof.

Apply and execute from these four sprint files only after a separate clean combined Sprint 023E–023I commit exists. If no exact clean SHA has been separately created/reported, stop `remote-baseline-commit-blocked-clean`; never use the uncommitted 023E worktree for remote work.

After the baseline exists, create an isolated 023J branch/worktree. Before any external mutation, establish the exact authorised non-production Supabase/Vercel target, approved `ap-southeast-1` Singapore region, synthetic-only data classification, international-processing/provider/privacy suitability, Storage-object recovery method, operator, rollback and mutation allowlist. Perform count-only ledger/schema/legacy/conflict/zero-state preflight. Stop on ambiguity, real data, discrepancy or missing recovery.

The current candidate is not remotely ready: migration `0018` lacks the RPCs and executable Storage policy called by source, and the UI does not transfer bytes. Complete this additively in migration `0019` and the narrow approved evidence source. Preserve fail-closed scanner/sanitiser behavior and disabled CSV. Add complete local tests, align candidate-ledger validators through `0019`, and run full validation/build.

Then stop `remote-candidate-ready-for-commit` for a separate commit instruction. Do not apply or deploy uncommitted source. After the clean 023J commit exists, rerun every target/preflight gate and apply only committed `0018–0019` to the exact non-production project. Never reset/repair/force or manually continue a failed migration.

Configure protected values only in the named non-production hosted target, deploy only the exact commit to preview/non-production, and do not move production aliases or activate production Cron. Run synthetic role/scope/Storage/lifecycle/Cron/recovery proof. Evidence must remain unavailable without real approved safety adapters. Clean exact owned fixtures Auth-last and prove final `0/0/0`.

Do not expose secrets, use real data/live malware, enable CSV, add providers/dependencies, alter production, push/merge/promote or claim production readiness. Finish with one allowed outcome and complete the five-part Manual Intervention Rule for every blocker.
