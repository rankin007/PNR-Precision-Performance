# Sprint 023H - Builder Handoff Prompt

You are Builder for Sprint 023H - Repository Lint Baseline Correction And Combined Closeout.

Apply and verify all four Sprint 023H files in the existing intentionally uncommitted worktree `C:\tmp\pnr-023e-local-upload-storage` on branch `codex/023E-local-upload-and-storage-implementation-and-proof`. Do not create another branch/worktree.

First prove complete attribution, clean index, no earlier diff in the three lint targets, unchanged migrations/dependencies, and exactly five intentional CommonJS import errors plus one unused type error. Stop before editing if the root cause or scope differs.

Make only the narrow correction: add one justified line-local `@typescript-eslint/no-require-imports` suppression immediately before each existing built-in `require()` and remove only the unused `BiochemistryRecommendationCategory` type import. Preserve CommonJS, filenames, package type, exports, `require.main`, behavior and every recommendation assertion. Do not use broad lint configuration or convert modules.

Add network-free behavior proof for syntax, Pack check/dry-run/safe failures, update-method require/pure exports, recommendation validation and suppression scope. Never execute the live updater network/write path.

Run full repository ESLint without stale-cache ambiguity and every combined 023E–023G gate. Reconcile earlier outcomes only if all evidence supports it, preserving all executable database, remote/provider and production deferrals.

Do not modify product/migrations, install dependencies, inspect protected configuration, apply migrations, create Storage, contact providers, invoke the live updater, deploy, stage, commit, push, merge or begin Sprint 023I. Finish with one allowed outcome and leave all combined work uncommitted unless separately instructed.
