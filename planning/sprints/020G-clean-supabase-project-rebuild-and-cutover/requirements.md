# Sprint 020G - Clean Supabase Project Rebuild And Cutover Requirements

## Role And Profile

Builder executes this follow-up under the `strict` workflow profile and implements only from this applied sprint folder.

## Goal

Create a separate clean Supabase project from the current repository source of truth, establish a genuine migration ledger, recreate only current required hosted configuration and secrets, validate security and application behavior, cut the application over with a tested rollback path, and retain the old project unchanged as temporary rollback evidence.

## Accepted User Decisions

The user accepted all Sprint 020F recommendations:

- create a new Supabase project rather than rebuilding the linked legacy project in place
- preserve no legacy public-table rows
- preserve no existing Supabase Auth users, identities, MFA state, or sessions
- preserve no existing Storage buckets, objects, or metadata
- retire all seven remote-only public surfaces and migrate none of their data
- recreate only hosted configuration and secrets required by the current application
- establish fresh migration history by applying repository migrations to the new empty project
- retire legacy security findings with legacy objects, review/restrict current helper execution, and resolve leaked-password protection through either enablement or an explicitly accepted plan-appropriate exception
- use a controlled maintenance/cutover with immediate smoke tests and keep the old project available for rollback until later decommissioning

The seven retired remote-only surfaces are:

- `public."Test User"`
- `public."Test User_id_seq"`
- `public.client_applications`
- `public.etrakka_sessions`
- `public.etrakka_biochem_comparison`
- `public.horse_biochemistry_results`
- `public.horse_gallery_items`

Do not export, migrate, recreate, or copy data from those surfaces.

## Relationship To Earlier 020 Sprints

- Sprint 020F is complete and is the authority for the remote/repository comparison, preservation decisions, security findings, and exact cleanup proof.
- Sprints 020C and 020E are closed as superseded and cleaned up.
- Sprint 020D is the authority for protected CLI authentication and connectivity to the old project.
- Sprint 020G creates and validates a separate project. It must not reset, delete, drop, repair, replay against, or otherwise clean the old project.
- Old-project decommissioning requires a later separately approved follow-up after cutover acceptance and rollback-retention expiry.

## Sources Of Truth

- `AGENTS.md`
- `planning/STATE.md`
- `planning/ARCHITECT_BRIEFING.md`
- all Sprint 020D/020F artifacts
- `docs/SUPABASE_CLI_CONNECTIVITY_020D.md`
- `docs/SUPABASE_REMOTE_REPLACEMENT_AUDIT_020F.md`
- `planning/reviews/020F-supabase-object-classification.md`
- `planning/reviews/020F-supabase-preservation-decisions.md`
- `docs/DESIGN_AND_MESSAGING_AUTHORITY.md`
- `supabase/config.toml`
- `supabase/migrations/0001*` through `0009*`
- `supabase/bootstrap/remote-init.sql` as generated compatibility output, not independent authority
- repository Auth, RLS, Storage, deployment, environment, workflow, and validation documentation
- application/server Supabase clients and every database consumer under `app/`, `components/`, and `lib/`
- package/lockfile pinned Supabase CLI `2.109.1`
- current official Supabase documentation for project creation, regions/plans, linking, migrations, Auth configuration, security advisors, backups, and production cutover

## New Project Identity Boundary

Before creation, Builder must record safe non-secret decisions for:

- owning Supabase organization
- project display name
- unique project slug/reference generated by Supabase
- region, recommended `ap-southeast-1` to match the current application locality
- billing plan and expected cost
- environment classification: replacement production candidate, not production until cutover acceptance
- named project administrator and rollback owner
- old project reference `tagnbgkroihagjmvehlx` retained as rollback source

Project creation is blocked until the operator confirms organization, region, plan/cost, and administrator. Do not guess an organization, accept a charge silently, or expose the generated database password.

## In Scope

### Repository Readiness

- verify migrations `0001`-`0009`, their hashes/order/dependencies, bootstrap agreement, destructive-statement absence, and empty-project compatibility
- inspect every migration-defined owner, role, grant, function, RLS policy, seed, extension, and managed-schema assumption
- create narrow corrective migration(s) after `0009` only where required to make the clean source-of-truth rebuild secure and reproducible
- correct current helper-function execution grants by revoking unintended `PUBLIC`/`anon` exposure while retaining only execution required by current authenticated RLS/application behavior
- ensure no migration recreates the seven retired legacy surfaces
- ensure no migration depends on old rows, Auth identities, Storage objects, or missing remote-only objects
- regenerate bootstrap output if a reviewed migration is added

### New Project Creation And Linking

- create exactly one new Supabase project in the operator-confirmed organization/region/plan through a supported protected flow
- keep the old project unchanged and linked identity recorded separately
- capture only safe new project reference/name/region/status and never credentials
- link the repository to the new project only after exact identity confirmation
- preserve a validated rollback procedure to relink to the old project without printing link or credential files

### Clean Build

- apply repository migrations once, in order, to the empty new project through the supported migration command/path
- establish genuine remote migration-history entries matching the applied repository versions
- apply only repository-defined seed/reference data, including exact biochemistry lookup data
- create no legacy public data, Auth users, Storage buckets, or objects
- do not import any rows from the old project

### Hosted Configuration And Secrets

- recreate only current application-required site URL, callback/redirect URLs, Auth provider/email settings, exposed schemas, API/database settings, and other hosted configuration
- keep leaked-password protection disabled only under the accepted Free-plan/passwordless exception jointly owned by Randell Rankin and Philip Rankin; do not upgrade or incur a charge
- recreate required secrets through protected operator-controlled channels; rotate rather than copy old secret values where supported
- update Vercel/environment configuration only at the controlled cutover phase, never during initial build
- never store secret values in Git, sprint artifacts, conversation, screenshots, arguments, or retained output

### Validation And Cutover

- validate schema, migration history, extensions, constraints, indexes, RLS, policies, grants, helper execution, seed counts/hashes, and security-advisor results
- prove the seven legacy surfaces are absent
- create fresh synthetic Auth/application fixtures only where separately defined in this sprint, with named owner and cleanup
- perform authenticated application/RLS smoke for permitted and denied cases supported by current approved role behavior; do not invent the unresolved Sprint 021 expanded role matrix
- validate mobile biochemistry capture/result persistence using synthetic data and exact scoring fixtures
- validate public under-construction gate, protected redirects, Auth callbacks, admin/portal/data-entry routes, and application build/runtime against the new project
- define a maintenance window, final pre-cutover checks, exact environment-variable target change, redeploy/restart path if required, immediate smoke tests, rollback triggers, and rollback command/path
- cut over only after all pre-cutover acceptance passes and the operator confirms the maintenance window and rollback readiness
- retain the old project unchanged after successful cutover

## Out Of Scope

- deleting, pausing, resetting, modifying, repairing, or decommissioning old project `tagnbgkroihagjmvehlx`
- importing any old public, Auth, Storage, Vault, session, identity, token, customer, horse, trainer, gallery, eTrakka, client-application, or historical-result data
- preserving old user passwords/sessions or old Storage content
- adding product features, uploads/provider architecture, voice transcription, trend charts, dashboards, CMS, commerce changes, thresholds, recommendation content, or expanded role definitions
- public website/shop reopening
- Stripe product/price/payment/refund/subscription/tax changes
- DNS changes unless strictly necessary for an explicitly approved application cutover step; current recommendation uses environment target changes only
- commit, push, or PR unless separately requested

## Security Requirements

- No `SECURITY DEFINER` routine or view may be accepted without reviewed owner, fixed safe search path, minimum execute grants, and justified need.
- Revoke routine execution from `PUBLIC` and `anon` unless a current documented public flow requires it. Grant only exact signatures to the minimum current role(s).
- Preserve current RLS intent but verify every application-owned table has the expected RLS/policy posture.
- Avoid always-true write policies unless explicitly justified by the current repository and protected server-side controls.
- Run Supabase security advisors before migration application review where supported, after build, and after hosted configuration; no unresolved error may remain at cutover.
- Warnings require explicit disposition: fixed, accepted with reason/owner, or blocked.
- Verify Auth settings and record the accepted Free-plan/passwordless leaked-password exception before creating synthetic users. The exception applies only while the application remains passwordless; any future password-authentication feature must reopen and resolve the control before implementation.

## Migration-History Strategy

The new project starts empty. Apply migrations `0001` onward through the supported Supabase migration workflow so the remote ledger truthfully records each applied version.

Do not:

- copy or fabricate the old project's absent migration ledger
- mark versions applied without executing them on the empty project
- use migration repair to bypass failed SQL
- apply `remote-init.sql` and then separately mark incremental migrations applied
- replay migrations against the old project

If a migration fails, stop. Preserve sanitized error evidence, inspect the new candidate state, and prepare a reviewed forward fix or recreate the still-disposable candidate project only through a separately confirmed recovery step. Never redirect recovery to the old project.

## Data And Fixture Boundary

- New project begins without legacy production rows, Auth users, or Storage objects.
- Repository-supplied deterministic seed/reference rows are allowed.
- Synthetic validation fixtures must be unmistakably synthetic, minimal, owned, timestamped, and cleaned up before production acceptance unless explicitly retained as documented test fixtures.
- Do not use real people, stables, horses, customers, pathology information, email addresses, or uploaded files.
- Auth test accounts must use controlled non-real addresses and an approved operator-managed secret path.

## Cutover Boundary

Cutover changes the application's Supabase endpoint/public key and required server-side credentials to the new project through protected deployment configuration. These are production-impacting external changes expressly included only after all pre-cutover checks pass.

Before cutover record:

- exact old and new project references by safe identifier
- backup/rollback availability of the old project
- maintenance owner/window
- exact environment scopes to update
- protected secret rotation/re-entry owner
- deployment/restart requirement
- smoke-test owner and checklist
- rollback trigger/time limit

Never print configuration values. After cutover, verify the application points to the new reference by safe hostname/reference only.

## Manual Intervention Rule

For organization/plan/cost, project creation, protected password/secret handling, configuration, migration failure, Auth/provider configuration, synthetic fixtures, security warning disposition, Vercel configuration, deployment/restart, maintenance window, smoke failure, or rollback, Builder must record:

- what is blocked or not working
- evidence already checked
- the exact user/operator action or decision needed
- numbered completion steps
- what Builder will verify afterward

Stop on target ambiguity, unexpected billing, secret exposure, legacy-data import, migration mismatch/failure, unresolved security error, missing rollback, failed synthetic RLS test, or any action affecting the old project.

## Required Deliverables

- `docs/SUPABASE_CLEAN_REBUILD_020G.md`
- `planning/reviews/020G-new-project-manifest.md`
- `planning/reviews/020G-security-disposition.md`
- `planning/reviews/020G-synthetic-auth-rls-test-plan.md`
- `scripts/supabase-synthetic-auth-rls-020G.mjs`
- `planning/reviews/020G-cutover-and-rollback.md`
- `supabase/verification/020G-clean-project-verification.sql`
- local validators for migration safety, new-project identity, migration history, structure, secrets, and cutover evidence
- migration/bootstrap corrections required for secure clean reproducibility
- refreshed planning state/status/briefing/decisions/risks/questions/schedule

## Approved File Set

Builder may edit:

- `planning/sprints/020G-clean-supabase-project-rebuild-and-cutover/**`
- `docs/SUPABASE_CLEAN_REBUILD_020G.md`
- `planning/reviews/020G-new-project-manifest.md`
- `planning/reviews/020G-security-disposition.md`
- `planning/reviews/020G-synthetic-auth-rls-test-plan.md`
- `scripts/supabase-synthetic-auth-rls-020G.mjs`
- `planning/reviews/020G-cutover-and-rollback.md`
- `supabase/migrations/**` only for new corrective migrations after `0009`; do not rewrite historical migrations without stopping for scope confirmation
- `supabase/bootstrap/remote-init.sql` only through the existing generator after reviewed migration changes
- `supabase/verification/020G-clean-project-verification.sql`
- `scripts/validate-supabase-clean-rebuild-020G.*`
- existing database/bootstrap validators where compatibility updates are required
- `docs/AUTH_RLS_PORTAL_ACCESS.md`
- `docs/PORTAL_DATA_ENTRY_WORKFLOW.md`
- `docs/DEPLOYMENT.md`
- `docs/VALIDATION.md`
- `.gitignore` only for narrow generated/temp exclusions
- `planning/STATE.md`
- `planning/STATUS.json`
- `planning/ARCHITECT_BRIEFING.md`
- `planning/DECISIONS.md`
- `planning/RISKS.md`
- `planning/QUESTIONS.md`
- `planning/SPRINT_SCHEDULE.md`

Production/external configuration changes included only at the controlled cutover gate:

- creation/configuration of exactly one new Supabase project
- protected new-project secrets/settings
- Vercel/environment Supabase target values required to point the current application to the new project
- application redeploy/restart if required to load those values

Inspection only:

- all application/source/docs/references outside the approved file set
- old Supabase project `tagnbgkroihagjmvehlx`
- protected environment/credential presence and safe project-reference evidence; never values

Generated but never staged or committed:

- `supabase/.temp/**`
- CLI/credential state
- unsanitized SQL, migration, advisor, Auth, environment, or deployment output

## Design And Architecture Carry-Forward

No visual or public messaging change is planned. `docs/DESIGN_AND_MESSAGING_AUTHORITY.md` remains binding. This sprint crosses new-project creation, migration, hosted configuration, Auth/security, synthetic production-candidate testing, environment cutover, and deployment/restart gates only as defined. It does not authorize new product behavior, public reopening, commerce changes, or old-project deletion.
