# Sprint 023N - Governed Upload Initiation Failure Diagnosis Requirements

## Boundary

Builder executes this diagnostic follow-up under the `strict` profile in the existing controlled Sprint 023L worktree and branch. Preserve all uncommitted 023L evidence and applied remote migrations `0018` and `0019`.

Sprint 023N may perform read-only catalog inspection, create one exact-owned synthetic Auth/application lineage, invoke governed initiation once, inspect only sanitised error class/stage and exact owned audit/count classifications, then clean up with Auth last. It does not authorise a product or migration correction.

Do not edit migration `0019`, create migration `0020`, repair/reset/reapply migration history, manually create a Storage object, bypass governed initiation, use unrestricted SQL, configure Vercel, deploy, push or merge.

## Mandatory Continuation Baseline

Before Pack application and diagnostic access, verify:

- existing worktree `C:\tmp\pnr-023l-remote-application-and-hosted-proof` on branch `codex/023L-remote-application-and-hosted-proof`;
- Git baseline `a15d89b2f95382d77a3f3ed450e1f4f16f254b51`, parent `fcf818fe3a8001b12941adc9dd121c6dbe8c002f`;
- existing uncommitted changes are only the approved 023L planning/sprint/evidence set;
- index is clean;
- exact linked target is project `uvskssaecdhxcgytkasc`, Singapore `ap-southeast-1`;
- remote/local ledger is exactly `0001` through `0019`, once each, with nothing pending;
- post-application structural/security aggregate passed every Boolean;
- protected bindings target only the approved project, are ignored, and are never printed;
- latest governed attempt returned only `INITIATION_FAILED` before Storage transfer;
- final Auth/application/Storage/recovery-artifact counts are `0/0/0/0`; and
- no Preview configuration or deployment exists from 023L.

Apply this Pack in that existing worktree. Do not create a competing worktree or lose the uncommitted 023L evidence. If any fact differs, stop `initiation-diagnostic-baseline-blocked-clean`.

## Diagnostic Questions

Classify exactly which boundary fails:

1. committed TypeScript RPC name, parameter names, types and supplied values versus applied PostgreSQL identity arguments;
2. execute grants for the authenticated caller and function security/search-path behavior;
3. `auth.uid()` and application-user mapping;
4. active membership, role, test/horse/stable authority and composite scope;
5. acknowledgement, MIME/category, size, count, aggregate quota and idempotency requirements;
6. replacement-null behavior for an ordinary new upload;
7. advisory lock or constraint failure;
8. audit insertion or return-shape failure;
9. PostgREST function-resolution/cache classification; or
10. application error translation masking a safe database error.

Return only sanitised SQLSTATE/error class, function stage marker, Boolean/count classification and correlation identifier generated for the exact owned synthetic attempt. Do not return names, emails, keys, filenames, object paths, tokens, payloads or database connection details.

## Permitted Diagnostic Mechanisms

- Static comparison of committed source and immutable migration `0019`.
- Read-only catalog queries inside `BEGIN TRANSACTION READ ONLY ... ROLLBACK` for function identity arguments, grants, owner/security mode, search path, constraints and policy metadata.
- One exact-owned synthetic Auth/application setup through the established protected harness.
- One governed `initiate_test_evidence_upload` invocation with an exact valid JPEG contract, checked acknowledgement, null `replacesId`, unique idempotency key and no Storage transfer.
- Narrow exact-owned audit/count reads that reveal only event type, reason class, stage Boolean and counts.
- A diagnostic wrapper or harness outside production source and repository only if it makes no database change beyond exact fixtures/governed initiation and is deleted afterward.

Do not change deployed functions, grants, policies, schema, PostgREST settings or application source. Do not repeat the governed call after a classified result.

## Cleanup

Delete only exact owned diagnostic rows using established scoped cleanup, then delete Auth last. Verify Auth/application/Storage/recovery artifacts return to `0/0/0/0`, no upload attempt/object/audit orphan remains, and the private bucket/schema stay intact.

## Evidence And Approved Files

Create:

- `planning/reviews/023N-initiation-contract-reconciliation.md`;
- `planning/reviews/023N-bounded-hosted-diagnostic.md`;
- `planning/reviews/023N-cleanup-and-correction-classification.md`; and
- `planning/reviews/023N-closeout.md`.

Builder may additionally edit only:

- `planning/sprints/023N-governed-upload-initiation-failure-diagnosis/**`;
- existing `planning/reviews/023L-closeout.md` only to append the 023N diagnostic boundary;
- `planning/STATUS.json`;
- `planning/STATE.md`;
- `planning/ARCHITECT_BRIEFING.md`;
- `planning/EVIDENCE_INDEX.md`;
- `planning/RISKS.md` and `planning/QUESTIONS.md` only for the classified defect.

No source, migration, package, lockfile, dependency or provider configuration file may change.

## Manual Intervention Rule

For every blocker record: what failed, evidence checked, exact operator action, step-by-step instructions, and what Builder will verify afterward. Never request or expose a secret.
