# Sprint 036Q Builder Handoff - Protected Five-Provider Authority Completion

## Task contract

**objective:** Complete one executable, privacy-safe, read-only five-provider authority record for the seven retained credential classes, or land the exact blocked-clean fallback without changing accepted 036L.

**owns:** The exact 12 implementation/evidence files and, only after same-Architect final diff PASS, the exact 12 scaled closeout files listed in `requirements.md`.

**must_not:** Do not change Product/schema/migrations/RLS/roles/permissions/dependencies/lockfile; reveal or persist protected values; install/login to provider tooling; perform provider writes, credential lifecycle, deploy/alias/env changes, identity/Auth/session/trainer/message/verification/graph/Storage actions, business effects, staging, commit, push, PR or merge/conflict work.

**acceptance:** AC-01..40 are truthfully dispositioned in `SPRINT-036Q-REPORT.md`. Target requires independently bound Vercel, Supabase, Resend, Stripe and Railway authority, exhausted required pages, exactly seven valid rows and zero action/residue. Missing safe Resend team identity or any other incomplete provider lands only `protected-five-provider-authority-blocked-clean` with exact alternatives and later owner action.

**verification:** New tests pass exactly `70 + 40 = 110`; retained passes `1783`; combined passes `1893`; current official contract checks, wrapper SelfTest/CapabilityGate, JSON/static/encoding, JS syntax, PowerShell AST, privacy/taint, typecheck, zero-warning lint, Production build, `git diff --check`, exact scope/lock/migration/Product/staged/conflict checks and cumulative provider action/residue reconciliation pass before and after the protected session.

## Sequence

1. Verify canonical Git truth and read all four direct Fly sprint files.
2. Return an exact no-edit Builder plan with files, red-first tests, provider request matrix, transport, evidence transaction, arithmetic, manual steps and target/fallback landing.
3. Wait for the same Architect's `pass|fix|ask`; PASS authorizes build automatically.
4. Implement red-first inside the exact file boundary. Preserve closed 036P evidence and change the retained reader only when a current-contract discriminator proves it necessary.
5. Prove local `110 + 1783 = 1893`, quality/privacy/scope gates and CapabilityGate before provider access.
6. Run at most one 90-minute protected read-only session with Phillip and Randell. Release provider authority only in fixed provider order and never request a protected value in chat.
7. Reconcile five providers, seven rows, cumulative reads at most 28 and exact zero writes/mutations/business effects/residue/trainer actions.
8. Stop and return the full diff/evidence to the same Architect.
9. Apply exact fixes until final PASS, then synchronize the exact 12 closeout files and perform disk readback.

Follow the Evidence-Proportional and Manual Intervention rules in `requirements.md`. The trainer is not required for Sprint 036Q. Existing draft PR #3 and its merge conflict are outside scope.
