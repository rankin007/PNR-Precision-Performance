# Sprint 036Q Blueprint - Protected Five-Provider Authority Completion

## Flight evidence

1. **Class and reason:** critical - this flight handles credential-bearing authority across five Production providers, but is deliberately restricted to read-only discovery with zero mutation.
2. **Invariant:** target is derived only from independently bound exact provider identities, exhaustive bounded pagination, exactly seven validated capability rows and zero writes/mutations/business effects/residue. Caller labels, token possession, fixtures, screenshots, incomplete pages and operator attestation cannot mint success.
3. **Affected origin-to-sink paths:** masked Vercel/Supabase/Resend/Stripe/Railway authority -> visible parent memory -> provider-specific private child request -> current read-only provider response -> strict value-blind projection -> parent validation -> three 036Q evidence artifacts -> later credential/identity/delivery planning. Negative paths end in `protected-five-provider-authority-blocked-clean` before any mutation.
4. **Sources, transforms and sinks:** sources are the retained complete accepted-source graph, masked exact authority IDs/credentials and fixed provider read-only responses; transforms are request-descriptor assertion, exact ownership binding, cursor exhaustion, duplicate/byte/ceiling checks, safe-field projection, taint rejection and seven-row reconciliation; sinks are only the approved 036Q evidence files and scaled closeout. No raw response, credential or control file is a sink.
5. **Discriminating examples:** Resend key/domain rows without independent team identity must fall back; Vercel `next`/cursor requires another page until exhausted or request 25 would be needed; Supabase generic `/database/query` is refused; a Railway project token using the account-token header is refused; a forged complete flag cannot create target; provider request 25 is not issued.
6. **Git truth:** canonical HEAD starts at `c90c320...`; accepted Production source remains `bfeb0b23...`; Sprint 036P's complete source graph and four retained reads remain durable. Closed 036P evidence is not rewritten, broad dirty-tree work is preserved and no Product file is authorized.
7. **Uncertainty and fallback paths:** missing already-authorized access, no safe exact Resend team binding, provider schema drift, wrong authority, incomplete pagination, cumulative-byte overflow, protected reflection, timeout, stderr or uncertain cleanup lands the fallback with exact later owner action. No fallback claims trainer readiness, credential readiness, launch readiness or Product Done.

## Phases

0. Canonical guard: verify exact CWD/Git root, branch, starting HEAD, staged/conflict zero and broad dirty-tree preservation.
1. Retained authority: reproduce Sprint 036P focused/retained proof, complete accepted-source graph and cumulative four-read/zero-action baseline.
2. Current contract check: revalidate official Vercel, Supabase, Resend, Stripe and Railway read-only endpoints, auth semantics, pagination and retrieved date. Any incompatible drift is repaired red-first only inside the exact authorized reader/test files or lands fallback.
3. 036Q parent: implement a current-HEAD wrapper that owns masked provider-by-provider release, strict child envelope validation, 036Q-only evidence reconciliation and zero-residue cleanup while reusing the already inspected 036P reader core.
4. Red-first proof: completion `70`, transport `40`; focused `110`, retained `1783`, combined `1893`; no caps, padding or uncounted assertion loops.
5. CapabilityGate: prove visible-session prerequisites without provider request. Missing safe access records exact alternatives and later action.
6. One protected read-only session: Phillip and Randell privately bind exact authorities; requests run sequentially; Resend requires independent pre-existing exact-team identity or falls back before its API calls.
7. Reconcile five provider rows, seven class rows, cumulative reads at most 28 and zero writes/mutations/business effects/residue. Keep trainer closed on any unknown.
8. Rerun local, quality, privacy, scope, lock/migration/Product, process/temp, staged/conflict and durable-evidence gates.
9. Same Architect reads the complete diff and evidence, returns `pass|fix|ask`, and repairs loop without a human gate.
10. After PASS only, synchronize the exact 12 scaled closeout files and read back from disk.

## Exact Request and Action Ceilings

- New provider requests: maximum `24`; retained plus new cumulative reads: maximum `28`.
- Pages: maximum `100` per provider family, subordinate to the global new-request ceiling.
- Raw response before projection: cumulative maximum `2 MiB` per provider.
- Provider writes, mutations and business effects: `0`.
- Reveal/decrypt calls and protected values persisted: `0`.
- Deployment/alias/env/domain/key/webhook/event/email/Railway mutations: `0`.
- Identity/Auth/session/trainer/message/verification/graph/Storage actions: `0`.
- Product/schema/migration/RLS/permission/dependency/lockfile edits: `0`.
- Stage/commit/push/PR/merge/conflict-resolution actions: `0`.

## Exact Provider Order

The protected child requests authority only when needed, in this fixed order: Vercel, Supabase, Resend, Stripe, Railway. The parent releases only that provider's credential/expected IDs and clears its owned transfer buffers after the response. A failure ends the session; later provider credentials are never released.

## Evidence Transaction

The child remains read/projection-only. The parent validates one strict final sanitized result. It atomically commits `external-ledger.json` as the canonical result, then deterministically regenerates and rereads `evidence.md` and `SPRINT-036Q-REPORT.md`. On each entry, mismatch between the canonical ledger and Markdown is repaired before any provider request. No journal, backup or durable control file is permitted; caught interruption/timeout cleanup proves owned temp/process residue zero. No landing is claimed until all three approved artifacts reread consistently.

## Landing

Target language is allowed only when all five exact provider authorities, all required pagination and exactly seven rows pass with zero actions/residue. Otherwise close `protected-five-provider-authority-blocked-clean` with the provider, alternatives checked, exact refusal and later owner action. Accepted 036L and Product Not Done remain unchanged.
