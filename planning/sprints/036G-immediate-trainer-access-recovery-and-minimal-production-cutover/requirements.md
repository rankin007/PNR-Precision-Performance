# Sprint 036G - Immediate Trainer Access Recovery And Minimal Production Cutover

## Outcome

Restore stable live Production trainer access through the smallest bounded release transaction that can prove the real user journey.

Sprint 036G reuses the existing exact Ready Sprint 036 Production-target candidate, makes no new deployment, creates no Supabase Management token, performs no Management API request and changes no Supabase or Vercel project setting. After exact retained-pilot, Product-integrity, candidate and five-alias preflight pass, Builder assigns only the five known stable aliases to the existing candidate one at a time, proves public and protected safety, and requires two fresh private Production trainer sign-ins. Any discrepancy or material journey failure triggers the exact all-five rollback.

Target outcome: `production-trainer-access-stable-live-accepted-clean`.

Sprint 036G is a genuinely different release approach from the consumed Sprint 036F lifecycle. It does not retry, reinterpret or repair the unproven 036F same-token invalidation result. Sprint 029N remains outside this sprint and stays gated unless the live trainer-access outcome passes or the owner later changes the road separately.

## Workflow profile

Use `strict`. This sprint touches Production routing, an existing protected service-role value used only by the retained-pilot verifier, a retained human trainer identity, mailbox-controlled OTP entry, live sessions and rollback.

Strict controls attach to canonical and remote authority, exact existing-candidate identity, no-Management enforcement, exact-ID retained-pilot verification, unchanged Product proof, the five-alias transaction, private human authentication, identity-free denial, final reconciliation and rollback. Strict does not turn a redundant configuration read or unavailable supporting tool into an artificial gate.

## Canonical starting authority

Start only from:

`C:\Users\rrank\OneDrive\PNR Precision Performance Canonical`

Before any action, resolve the current directory and `git rev-parse --show-toplevel`; both must equal that path after normalizing Windows separators. Expected closed Sprint 036F authority is SHA `b88d68d98fe374df7f32f3be4d0f0a73dd02c5e7` on `codex/036F-corrected-wrapper-live-lifecycle-retry`, exact on `origin/codex/036F-corrected-wrapper-live-lifecycle-retry`, with exactly one canonical worktree registration.

The only expected Architect handoff changes are this Pack and `planning/STATUS.json`. Any additional uncommitted path, changed starting SHA, unresolved direct-remote authority or extra worktree is a material baseline mismatch until reconciled.

Create only `codex/036G-immediate-trainer-access-recovery-and-minimal-production-cutover` in the permanent canonical repository. Do not use a retired legacy path, `C:\tmp` worktree, deployment directory, alternate history or Sprint 035Q.

## Source authority

Use, in descending order:

1. `AGENTS.md`, including the canonical-workspace guard, Evidence-Proportional Execution Standard and Manual Intervention Rule.
2. This Sprint 036G Pack after Builder applies it and verifies exactly four generated files.
3. Closed Sprint 036F briefing, state, decisions, risks, acceptance and review for the consumed Management lifecycle, exact cleanup and mandatory no-retry boundary.
4. Closed Sprint 036, 036B, 036C, 036D, 036E and 035K requirements, acceptance and reviews for the existing candidate, independent per-alias routing authority, protected exact-ID pilot and proven human journey.
5. `docs/OPERATIONS_HANDOFF.md`, `docs/TEST_AUTH_MAILBOX_AND_PROTECTED_PROCESS_RUNBOOK.md`, `docs/AUTH_RLS_PORTAL_ACCESS.md`, `planning/FINAL_PRODUCT_ACCEPTANCE_MATRIX.md`, `planning/EVIDENCE_INDEX.md`, `docs/WORKFLOW_PROFILE.md` and `docs/DESIGN_AND_MESSAGING_AUTHORITY.md`.
6. Current official Vercel CLI documentation for `inspect` and `alias set`, plus installed execution-time help. Current official pages include `https://vercel.com/docs/cli/inspect` and `https://vercel.com/docs/cli/alias`.

Official documentation is mechanism authority, not permission to broaden scope. Fresh execution-time evidence governs current deployment and routing facts. Historical evidence may prove immutable source/build ancestry only when exact equality remains executable.

## Task contract

**objective:** Restore stable, repeatable live Production trainer access through the existing exact Ready candidate, an exact five-alias cutover and two fresh private Production trainer journeys, without another Management-token lifecycle or fresh deployment.

**owns:** Canonical and direct-remote reconciliation; narrow branch-allowlist activation of the existing protected Sprint 035K verifier and deterministic proof; existing service-role protected entry for exact-ID retained-pilot Verify only; exact Product-byte and focused validation proof; current Vercel CLI/help reconciliation; exact existing candidate/rollback/project evidence; immutable candidate public/protected smoke; a timestamped five-alias routing ledger; exactly five candidate alias assignments; cache-busted canonical smoke; two fresh private human Production sign-ins; exact all-five rollback when required; proportional operations/planning/closeout records; and scoped 036G branch commit/push.

**must_not:** Create, rotate, receive, use or revoke a Supabase Management token; call any Supabase Management API endpoint; run `ManagementLifecycle`; use a classic PAT or substitute Management credential; enumerate Auth users; emit provider payloads or protected values; create or replace a service-role value; make a fresh Vercel deployment; use `vercel promote`, `vercel rollback`, redeploy or change domain auto-assignment; trust deployment-level alias inventory over independent per-alias routing; move an unlisted alias; modify Supabase/Auth configuration, callback, SMTP, template, OTP policy, Vercel environment/project settings, DNS, Product/runtime/package/configuration/migration/schema/RLS/role/permission/identity/fixture/data/Storage; inspect or automate a mailbox; activate Participants A/B/C; implement Sprint 029N; merge, open a PR, push `develop`, force-push, rewrite history or claim broad rollout/product-wide Done.

**acceptance:** The protected retained-pilot wrapper is activated only for the exact 035K and 036G branches with deterministic refusal elsewhere; its core remains byte-identical and focused proof passes; no Management lifecycle or API call occurs; exact-ID retained-pilot Verify passes before cutover; current Product bytes equal both the immutable Sprint 036 candidate source and accepted Sprint 035K application behavior; focused auth/dashboard/public validation passes; existing candidate `dpl_EmfhgcYxjNHk4W9LwH6ruTfASZmf` and rollback `dpl_EUJyBLQp7okgbPwtBU6nmMH88L6A` are exact-project, Production-targeted and Ready; all five aliases start on rollback; the candidate immutable URL passes safe pre-cutover smoke; five explicit alias assignments reach five/five candidate with full rereads after every step; canonical safety passes; the retained trainer completes two fresh private Production code sign-ins plus dashboard/workspace/action/denial/sign-out proof; final retained-pilot and five-alias invariants pass; and the all-five rollback remains exact and executable.

**verification:** Run Pack dry-run/four-target proof; canonical path/worktree/status/SHA/direct-remote checks; starting wrapper/core/test hashes; deterministic branch/refusal and retained 035K proof; protected wrapper SelfTest and exact-ID Verify; exact Product tree comparison to `38ab1acc2776124ba8b54fd33eb346bf7f28f99a` and accepted 035K correction `76f66f5f9803e5d1f85a6dd3f71adf302b8a1810`; focused authentication/dashboard/OTP/redirect/bootstrap/recovery/public controls; TypeScript, zero-warning lint and Production build or exact-byte equivalent stronger proof; current Vercel CLI/help and official-mechanism reconciliation; exact project/candidate/rollback inspection; five independent alias inspections at every checkpoint; cache-busted immutable-candidate and canonical smoke; two fresh private human sign-ins; final retained-pilot Verify; diff/secret/private-data/generated-artifact scans; final worktree/direct-remote proof; and one exact permitted outcome.

## Deliberate 036G decisions

### No Management lifecycle

Sprint 036F consumed its one permitted Management lifecycle and closed `production-management-access-revocation-blocked`. Exact token-row absence proves cleanup, not same-token invalidation. Sprint 036G neither retries that lifecycle nor makes its completion a release gate.

The fresh acceptance fact is effective live trainer access, not a metadata projection. Two successful current-code Production sign-ins, the bounded authenticated journey, exact-ID retained-pilot verification, unchanged Product bytes and zero provider-setting mutation provide direct end-to-end evidence for this outcome. They do not claim the exact hidden values of every provider setting.

No Supabase account/token page, Management endpoint, Management credential, Auth-user list or raw provider configuration may be opened or processed. If diagnosis requires a provider configuration mutation or privileged provider read not already available through the bounded user journey, roll back and close `production-access-source-or-contract-change-required-rollback-clean`.

### Reuse the existing exact candidate

The existing candidate is:

- deployment ID `dpl_EmfhgcYxjNHk4W9LwH6ruTfASZmf`;
- immutable URL `pnr-precision-performance-k4rhrxq9d-rankin007s-projects.vercel.app`;
- project `rankin007s-projects/pnr-precision-performance`;
- project ID `prj_6To7czLpCEGL6fInkQwE4egePPpq`;
- target `production`;
- recorded source SHA `38ab1acc2776124ba8b54fd33eb346bf7f28f99a`; and
- recorded state Ready.

Sprint 036 did not reject this candidate for a Product defect. It rolled back because deployment-level alias metadata contradicted the required no-auto-promotion evidence. Later independent proof established that deployment alias inventory can remain stale while direct alias resolution reports current routing. Sprint 036G uses independent per-alias resolution as authority and grants new exact release authority for this existing candidate.

Do not deploy a fresh candidate. Before mutation, prove the existing candidate is still exact-project, Production-targeted, immutable, Ready and application-byte-equal to accepted Sprint 035K behavior. Prove its immutable URL passes cache-busted `/`, `/sign-in`, `/api/health`, anonymous protected-route and unsafe-method checks that do not require a canonical callback. If any fact fails or route proof requires a source/configuration change, close `production-existing-candidate-preflight-blocked-clean` without alias mutation.

### Preserve the exact rollback

The known-safe rollback is:

- deployment ID `dpl_EUJyBLQp7okgbPwtBU6nmMH88L6A`;
- immutable URL `pnr-precision-performance-qpd244v93-rankin007s-projects.vercel.app`;
- recorded source SHA `3be6b6c3622150a3c2009ed8564795b14e3e6c2b`; and
- recorded state Ready.

Fresh inspection must prove it remains exact-project, Production-targeted and Ready before any alias mutation. Rollback uses explicit per-alias assignment to this immutable URL, not `vercel rollback` and not deployment-level promotion metadata.

## Protected retained-pilot verifier activation

Current starting hashes are:

- `scripts/Invoke-LiveTrainerAccess035K.ps1`: `6AB4B77F383BEB2B6AB46CC1C1E01D7BD6CF3A6D33ABAFF890307C435AC9E9A4`;
- `scripts/live-trainer-access-035K-core.mjs`: `603FF16B9F6EB30D2B5E26A39218E8307731D66008D4D384FB9696095CD3AB5A`; and
- `scripts/test-live-trainer-access-035K.mjs`: `38A570BC108D018C1B69DA52960B56615302E1018E232D6813BA913F30926A24`.

Builder may modify only the wrapper and deterministic test before protected Verify. The permitted change is to preserve the exact historical 035K branch while adding the exact 036G branch to a closed allowlist, refuse every other branch, and update deterministic assertions for that narrow behavior. The core must remain byte-identical.

The wrapper remains restricted to its existing operations and protections. Sprint 036G runs only `SelfTest` and `Verify`. It must continue to refuse non-interactive/transcribed consoles, process-level protected residue, wrong project, conflicts, untrusted runtime, malformed ownership ledger and protected output. The existing service-role value is privately entered only when the wrapper prompts, exists only in the child environment for the minimum Verify operation and is cleared afterward. No credential creation, rotation, storage or output is authorized.

The accepted sanitized Verify result is `state=verified`, application `8`, Auth `1`, Storage `0`, wrong-horse rows `0`, with exact-ID reads and no enumeration or mutation. A mismatch stops before Vercel mutation. Do not repair, recreate, retain, clean or otherwise mutate the pilot in Sprint 036G.

## Product and release preflight

Before alias mutation:

1. prove current application/runtime/package/configuration/migration bytes have zero difference from candidate source `38ab1acc2776124ba8b54fd33eb346bf7f28f99a` across the approved runtime paths;
2. prove the accepted Sprint 035K correction remains in ancestry and no unexplained application difference exists from `76f66f5f9803e5d1f85a6dd3f71adf302b8a1810`;
3. run the focused trainer, dashboard, OTP, redirect, bootstrap, recovery, public, TypeScript, lint and Production-build gates, accepting exact-byte inherited proof when it is at least as strong and recorded clearly;
4. pass exact-ID retained-pilot Verify;
5. reconcile installed Vercel CLI/help with current official `inspect` and `alias set` behavior;
6. prove exact project, existing candidate and rollback identity/state;
7. independently resolve all five aliases to the Ready rollback in a complete timestamped `baseline` snapshot; and
8. prove the immutable candidate URL passes safe cache-busted smoke.

No historical provider projection is promoted to fresh evidence. No fresh provider projection is required. No later gate begins from an incomplete earlier gate.

## Exact five-alias affected set

The five and only stable aliases are:

1. `precisionperformance.com.au`
2. `www.precisionperformance.com.au`
3. `pnr-precision-performance.vercel.app`
4. `pnr-precision-performance-rankin007s-projects.vercel.app`
5. `pnr-precision-performance-rankin007-rankin007s-projects.vercel.app`

Any additional Production alias, missing alias, third resolved deployment or target ambiguity stops before mutation. DNS is unchanged.

## Minimal Production cutover

The success path performs exactly five external writes: one explicit alias assignment for each accepted alias to the existing candidate immutable URL. It performs no build, deployment, promote, rollback, domain, DNS, environment, provider or data mutation.

Promotion order is fixed from lowest public importance to canonical apex:

1. `pnr-precision-performance-rankin007-rankin007s-projects.vercel.app`
2. `pnr-precision-performance-rankin007s-projects.vercel.app`
3. `pnr-precision-performance.vercel.app`
4. `www.precisionperformance.com.au`
5. `precisionperformance.com.au`

Use one current-help-confirmed exact per-alias assignment for each name. `vercel alias set <candidate-immutable-url> <exact-alias>` is the planned command shape. After every write, independently resolve all five aliases and require candidate/rollback counts `1/4`, `2/3`, `3/2`, `4/1`, `5/0` with no third deployment, non-Ready result or unexpected movement.

Deployment-level alias inventory and project Current/Promoted labels are corroborating only. They do not override direct per-alias resolution.

Any write error, unexpected alias movement, missing/extra alias, third deployment, non-Ready state or contradictory routing triggers immediate all-five rollback before human authentication.

## Public and protected safety

At `candidate-live`, use cache-busting values and prove:

- canonical `/`, `/pricing`, `/disclaimer`, one hero asset, `/sign-in` and `/api/health` return the accepted safe states;
- public enquiry remains visibly unavailable and commerce remains disabled-safe;
- anonymous `/portal`, `/portal/horses/<synthetic-or-nonexistent-id>` and `/admin` return safely to sign-in without loop, identity or existence leakage;
- unsafe methods and protected/API boundaries retain expected denial;
- canonical/robots/sitemap and public/authenticated separation remain intact; and
- no confidential, identity, mailbox, clinical or customer data enters evidence.

A public/protected regression triggers immediate all-five rollback before OTP.

## Human Production acceptance

Only after `candidate-live` proves five/five candidate and safety passes:

1. the retained tester privately opens canonical Production sign-in;
2. privately enters their own email, requests one current six-digit code and enters it without sharing it;
3. reaches `/portal` without redirect loop or bootstrap collision;
4. sees only the retained synthetic stable/horse and accurate workflow/action;
5. opens the synthetic horse workspace and reaches or completes the existing permitted action;
6. proves inaccessible-horse denial without identity, existence, state or count leakage;
7. signs out and proves protected access is gone; and
8. after the applicable cooldown, uses a fresh browser/application session and a fresh current code to reach `/portal` again with the same bounded assignment.

The tester controls mailbox and code entry privately. Builder must not inspect, automate or scrape the mailbox; receive an email, code, session or private identifier; or retain protected values in conversation, commands, URLs, logs, screenshots, Git or evidence.

One sanitized diagnosis and one cooldown-safe non-blind retry are permitted only for human input, expiry, cooldown or transient transport when no source/provider/configuration change is required. A material authentication, session, permission, privacy or integrity failure triggers all-five rollback.

Successful live authentication is the execution-time provider compatibility proof for this outcome. It does not authorize or claim a Management configuration projection.

## Final invariants

After both sign-ins:

1. rerun exact-ID retained-pilot Verify and require `8/1/0/0` with zero mutation;
2. reread all five aliases and require five/five Ready candidate;
3. rerun canonical public/protected/API safety;
4. prove no Management token/API activity and no provider/Vercel setting, DNS, Product, identity, fixture or data mutation;
5. prove protected process/environment/temp residue is zero;
6. reconcile the exact external-action ledger; and
7. close only when canonical worktree and scoped direct remote branch agree exactly.

## Exact rollback

Rollback order is fixed from canonical apex outward:

1. `precisionperformance.com.au`
2. `www.precisionperformance.com.au`
3. `pnr-precision-performance.vercel.app`
4. `pnr-precision-performance-rankin007s-projects.vercel.app`
5. `pnr-precision-performance-rankin007-rankin007s-projects.vercel.app`

Assign every alias explicitly to `pnr-precision-performance-qpd244v93-rankin007s-projects.vercel.app`, even when an earlier read appears safe, and independently reread all five after every step. Final recovery requires five/five Ready rollback plus canonical route safety.

Do not use `vercel rollback`, `vercel promote`, DNS changes or project-setting changes as substitutes. If exact all-five recovery cannot be proven, stop `production-cutover-partial-mutation-blocked` with complete manual intervention.

## Design, privacy and claims boundary

This sprint touches protected operations and Production release only. It crosses no new Product-design, public-content, clinical-claims, pricing, CMS, upload, voice, schema, permission or provider architecture gate.

Preserve truthful sign-in language, public/authenticated separation, mobile/desktop accessibility, no confidential-data display, identity-free denial and professional non-medical messaging. Status evidence uses text/classification, never color alone. No real horse, stable, trainer, mailbox, customer or clinical record may enter evidence.

## Approved files and external actions

Builder may change only:

- the four generated Sprint 036G files;
- `scripts/Invoke-LiveTrainerAccess035K.ps1`;
- `scripts/test-live-trainer-access-035K.mjs`;
- `planning/reviews/036G-immediate-trainer-access-recovery-and-minimal-production-cutover.md`;
- proportional current updates to `planning/STATE.md`, `planning/STATUS.json`, `planning/ROADMAP.md`, `planning/SPRINT_LIFECYCLE_LEDGER.md`, `planning/SPRINT_SCHEDULE.md`, `planning/EVIDENCE_INDEX.md`, `planning/ARCHITECT_BRIEFING.md`, `planning/DECISIONS.md`, `planning/RISKS.md` and `planning/QUESTIONS.md` only where final authority changes; and
- `docs/OPERATIONS_HANDOFF.md` and `docs/TEST_AUTH_MAILBOX_AND_PROTECTED_PROCESS_RUNBOOK.md` only for proven 036G release/rollback/protected-journey procedure changes.

Approved external actions are limited to sanitized Git/GitHub/Vercel documentation and read-only state checks; two exact-ID retained-pilot Verify operations using the existing protected service-role value; candidate immutable-URL and canonical HTTP smoke; exactly five candidate alias assignments; bounded private human OTP requests/verifications; exact all-five rollback when required; and intentional commit/push of only the scoped 036G branch.

No fresh deployment, promote, platform rollback, domain/DNS/environment/project-setting change, Management credential/API action, provider/Auth setting, callback, SMTP, template, OTP policy, core helper, Product, package, lockfile, runtime configuration, migration, schema, RLS, role, permission, identity, fixture, application data, Storage or unrelated provider resource may change.

## Evidence-proportional execution and manual intervention

Stop only for a material canonical/remote mismatch, wrong/unbounded target, protected-output failure, retained-pilot mismatch, candidate/rollback mismatch, alias-routing contradiction, secret exposure, authentication/privacy/integrity failure, unauthorized source/provider/schema/contract expansion, partial Production mutation, failed rollback, unexpected real data, destructive uncertainty or cleanup that cannot be proven safe.

Use equivalent or stronger safe proof when an optional supporting tool is unavailable. Keep in-scope wrapper allowlist, deterministic harness, reporter, timestamp, JSON parsing, formatting, encoding and validator corrections inside 036G when they preserve the contract. Do not create a follow-up solely because browser automation, renderer, audit log, deployment alias inventory, schema dump, optional CLI path or redundant verifier is unavailable.

Expected protected human actions are private entry of the existing service-role value into the verifier and the retained tester's private sign-ins. Builder owns fixed commands and sanitized verification; humans never disclose protected values.

If manual intervention remains necessary after safe alternatives are exhausted, record:

- what is blocked or not working;
- evidence already checked;
- the exact secret-free human action needed;
- step-by-step private instructions;
- the fixed booleans/classifications the human may report and every protected value they must not report; and
- what Builder will verify afterward.

Never ask for a service-role value, email, OTP, raw provider response, screenshot, account detail or private identifier in conversation. No manual intervention may create or rotate a Management token or broaden the external-action set.
