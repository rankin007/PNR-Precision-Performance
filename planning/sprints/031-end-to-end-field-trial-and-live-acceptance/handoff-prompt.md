# Builder Handoff — Sprint 031

You are Builder for Sprint 031 — End-to-End Field Trial And Live Acceptance.

Apply this Pack from the canonical project, verify the four generated files, then execute them in `C:\tmp\pnr-027b-completed-product-lineage-reconciliation`. Freeze and preserve the complete unstaged Sprint 025–030 lineage before edits. Do not clean, stage, commit, push, merge or open a PR.

Design the governed field-trial matrix and credential-safe harness locally first. Pass canonical validation/build, output-allowlist, fixture-ceiling, ownership and cleanup self-tests before external work. Use only visibly synthetic data and the smallest declared fixture set. Never use or expose real people/horse/stable/customer/payment data, mailbox identifiers, passwords, OTPs, links, tokens, cookies, credentials or full provider identifiers.

External work is authorized only against an exact generated non-production Vercel Preview mapped exclusively to the approved Singapore Supabase project with exact ledger `0001`–`0021`. Refuse the old/unexpected project, Production aliases/environments, ledger drift and any Stripe/payment path. You may deploy the validated candidate to Preview and create bounded exact-owned Auth/application/Storage fixtures for the matrix. Production deployment, alias movement, public reopening and Production mutation remain prohibited.

Run the integrated authenticated rendered matrix: role/access lifecycle; mobile/desktop capture, review and persistence; results/history/trends; stable dashboard and horse workspace; one governed evidence action chain; intentional unavailable boundaries; denial/revocation; accessibility/responsive/error cases; and exact cleanup. Reuse historical proof only when the target and unchanged contract are demonstrably equivalent, and never in place of the primary integrated rendered workflow.

Keep production thresholds/recommendations, clinical priority, application audio/transcription and commerce unavailable. Prove those limitations are safe and accurately presented. Do not activate fixture thresholds, infer clinical urgency, enable checkout, use Stripe, or add new schema/RLS/permission/provider behaviour.

Diagnose once and correct in-contract product/harness defects within Sprint 031, then rerun only affected slices plus shared safety/cleanup controls. Stop for material target, secret, privacy, authorization, integrity, migration, Production, scope or cleanup risk. Compensate partial creation immediately and delete Auth identities last. Final exact-owned Auth/application/Storage state must be `0/0/0`.

Follow the Evidence-Proportional Execution Standard: use equivalent or stronger safe proof for unavailable optional tools, keep deterministic tooling/validator/reporter corrections in scope, and use manual intervention only after safe alternatives are exhausted. If manual action is unavoidable, record all five required elements and limit the operator to protected sign-in/MFA/provider interaction; do not ask them to paste secrets or run technical commands.

Finish with exactly one accepted outcome, a launch-readiness recommendation, sanitised evidence, complete final accounting and refreshed durable planning state. Leave all work unstaged and uncommitted.
