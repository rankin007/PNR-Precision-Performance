# Builder Handoff — Sprint 031B

You are Builder for Sprint 031B — Protected Preview Rendered Harness And Authenticated Matrix Completion.

Apply this Pack from the canonical project, verify the four generated files, then execute in `C:\tmp\pnr-027b-completed-product-lineage-reconciliation`. Freeze and preserve the full unstaged Sprint 025–031 lineage. Reverify the exact generated non-Production Preview, approved Singapore Supabase project, ledger `0001`–`0021` and initial run-owned `0/0/0`. Refuse all unexpected/Production/Stripe targets.

Correct only the Sprint 031 protected rendered-session transport gap. Build or adapt the smallest verified mature-browser harness that installs genuine app-compatible Supabase sessions into isolated Preview-origin contexts and proves browser/server identity agreement through rendered protected behavior. Recover historical 021AH logic only from an exact verified source and record compatibility. Do not add an auth bypass, debug route, middleware exception or persistent test-only product behavior.

Before identities or fixtures, pass credential-free browser, cookie-transport, actor-isolation, simulated-revocation, output-redaction, timeout and process-cleanup controls. Protected values must never reach arguments, environment dumps, disk, logs, screenshots, clipboard, evidence or conversation. Emit fixed classes/counts only.

Then pass the minimal writer bootstrap/revocation gate. Only afterward create the remaining exact-owned synthetic actors/fixtures within the existing ceilings and execute all 25 Sprint 031 cases unchanged. Do not remove, merge, downgrade or replace authenticated rendered cases with historical/static evidence. Preserve unavailable clinical thresholds/recommendations, clinical priority, application audio/transcription and commerce.

Diagnose once, use materially distinct safe alternatives and keep deterministic harness/reporter/validator/process corrections in 031B. Correct only narrow in-contract application defects and rerun affected cases plus shared transport/security/cleanup controls. Stop for target mismatch, protected output, context leakage, RLS/privacy/integrity failure, schema/RLS/permission/dependency/new-authority scope, Production/Stripe impact or uncertain cleanup.

Clean Storage/evidence first, application fixtures dependency-safely and Auth identities last. Run primary and independent final proof of Auth/application/Storage `0/0/0`, no run/recovery orphans and zero owned browser/driver/helper processes. Clear protected contexts and memory.

Follow the Evidence-Proportional Execution Standard and use manual intervention only after safe alternatives are exhausted. Operator involvement is limited to protected sign-in/MFA/mailbox/provider interaction; never request pasted secrets or technical command execution.

Finish with exactly one accepted outcome, sanitised transport/matrix/cleanup evidence, an updated Production-release recommendation and refreshed durable planning state. Leave all work unstaged and uncommitted; do not push, merge, open a PR, move aliases or deploy to Production.
