# Sprint 036N Builder Handoff - Native Consumer and Credential Readback Recovery

## Task Contract

**Objective**

Implement and execute a provider-native consumer and predecessor-readback closure for exactly seven credential classes, prepare exact-source primary and rollback deployments, retire or protect predecessor-bearing Vercel deployments, and land only the exact target or an authorized truthful fallback.

**Owns**

- the bounded implementation, test, operating-document, registration, evidence, and closeout files named in the passed Builder plan
- exact provider/account/project inventory and lifecycle actions expressly required by the acceptance criteria
- at most two exact-source Vercel deployments, one fixed-five alias transaction, bounded pre-irreversible compensation, and exact-ID predecessor-deployment retirement
- sanitized evidence and mandatory closeout only after implementation/evidence review PASS

**Must not**

- contact or involve the trainer
- send or verify OTP, create or retire trainer graphs, or disposition excluded identities
- change Product behavior, schema, migrations, RLS, roles, permissions, DNS, email policy, commerce, enquiry behavior, dependencies, or lockfile
- store or output secrets, tokens, credentials, private provider rows, customer data, or raw provider errors
- use operator confirmation, screenshots, source inspection, or Vercel absence as a substitute for provider-native predecessor proof
- perform broad provider cleanup, dirty-tree deployment, unknown deployment deletion, commit, push, or PR

**Acceptance**

All AC-01..40 must be truthfully dispositioned. The target requires complete paginated consumer closure, exact primary and rollback compatibility, seven native terminal dispositions, predecessor-bearing deployment absence/incapability, exact aliases/bindings/source, and zero unauthorized residue. Any unmet material boundary uses the named fallback and must not be described as launch or trainer readiness.

**Verification**

Run the exact focused and retained arithmetic approved in the Builder plan, provider-native readbacks, adversarial tests, JSON/static/type/lint/build/syntax/privacy/scope/lock/migration/Git gates, then stop at a sanitized stable checkpoint for the same Architect's implementation/evidence review.

## Required Builder Sequence

1. Reread identity, AGENTS, STATE, decisions, domain, risks, questions, workflow profile, design authority, briefing, this sprint's four files, Sprint 036M closeout, Sprint 036L live authority, and relevant source/provider docs.
2. Verify canonical workspace and Git guard.
3. Return an exact no-edit plan with:
   - exact file set
   - exact focused and retained assertion arithmetic
   - complete consumer-manifest derivation
   - per-provider finite adapter and native readback method
   - primary/rollback/deletion transaction and compensation matrix
   - external mutation ceilings
   - AC-01..40 mapping
   - privacy and evidence design
   - known capability blockers and manual intervention, if any
4. Do not implement or mutate provider state until the same Architect returns PASS.
5. Implement red-first and keep Product/source boundaries unchanged unless a discriminating failure requires a reviewed correction.
6. Run local proof before any protected provider action.
7. Use a single protected, non-transcribed interactive window for provider actions; never place protected values in chat or durable output.
8. Stop immediately on incomplete pagination, ownership/source ambiguity, protected-value exposure, unsafe partial mutation, failed predecessor proof, or cleanup uncertainty.
9. After stable execution evidence, request the same Architect's full diff/evidence review.
10. Close out only after PASS and synchronize all mandatory ledgers.

## Class Matrix Required in the Plan

For each of the seven classes, name:

- exact authoritative provider/account/project
- complete current and still-addressable consumer set
- replacement create/select mechanism
- install targets
- harmless intended-runtime probe
- predecessor revoke/delete/deactivate mechanism
- independent absence or authentication-rejection oracle
- coupling and refusal conditions
- pre-irreversible compensation
- post-attempt landing

If any cell is unknown, report it as a planning finding or a runtime blocked-retained gate; do not invent the mechanism.

## Manual Intervention Rule

If provider UI is genuinely required, first document why API/CLI evidence is unavailable. Give the operator exact step-by-step instructions for the named provider action without protected values. After the operator completes it, independently verify the exact result through provider-native readback or predecessor authentication rejection. A manual attestation alone is never evidence.

## Stop Conditions

Stop and return `fix` or `ask` before implementation for a material scope contradiction, missing destructive-action authority, unbounded consumer inventory, missing predecessor oracle, or an exact-file/ceiling gap. During implementation, use the named clean/material fallbacks rather than widening scope.
