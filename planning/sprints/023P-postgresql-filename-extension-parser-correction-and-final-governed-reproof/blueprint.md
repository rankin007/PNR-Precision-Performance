# Sprint 023P - PostgreSQL Filename Extension Parser Correction And Final Governed Reproof Blueprint

## Phase 1 - Preserve And Apply

Verify the exact controlled continuation baseline and three-file 023O runtime delta. Apply and verify the four 023P sprint files without losing prior evidence.

## Phase 2 - Build Exact `0021`

Copy the exact migration `0020` initiation definition, change only the double-backslash extension regex to the single-backslash PostgreSQL form, retain `extensions.digest`, dependency check, search path and grants, then add exact semantic and input-matrix tests.

## Phase 3 - Validate And Commit Stop

Run the complete local matrix, record exact hash/manifest and stop `extension-parser-correction-ready-for-commit`. After separate instruction, stage only approved files, review/revalidate, commit once and report SHA/clean state.

## Phase 4 - Apply Only `0021`

From the clean commit, reconfirm target/ledger/zero state and dry run. Apply only `0021` once. Verify ledger alignment. Do not require redundant schema dump when committed semantics plus runtime proof cover the boundary.

## Phase 5 - Final Governed Reproof

Run exactly one synthetic governed path: initiation, exact intent, no-overwrite JPEG transfer, fail-closed finalisation, TLS export, SHA-256, DPAPI round trip, governed removal and verified absence.

## Phase 6 - Cleanup And Resume 023L

Clean exact fixtures with Auth last, prove `0/0/0/0` and zero orphans, validate evidence and close:

`extension-parser-corrected-final-governed-recovery-proven-clean`

Classify Sprint 023L ready to resume at protected Vercel Preview configuration. Do not configure or deploy Vercel in 023P. Keep final evidence uncommitted unless separately instructed.
