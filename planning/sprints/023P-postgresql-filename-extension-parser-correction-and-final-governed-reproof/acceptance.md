# Sprint 023P - PostgreSQL Filename Extension Parser Correction And Final Governed Reproof Acceptance

## Local Candidate

- [ ] Existing 023L/023N/023O evidence and approved runtime delta are preserved.
- [ ] Migration `0021` is the only new migration; `0001` through `0020` remain byte-identical.
- [ ] `0021` replaces only the initiation function and changes only the diagnosed regex/comment boundary.
- [ ] Single-backslash PostgreSQL regex correctly extracts approved final extensions.
- [ ] Valid JPEG/PNG/PDF and invalid/mismatched/CSV/unknown cases are tested.
- [ ] `extensions.digest`, dependency precondition, restricted search path, exact signature/return and grants remain.
- [ ] Exact/adversarial `0001` through `0021` ledger and complete local matrix pass.
- [ ] Candidate stops for a separate clean commit instruction.

## Remote And Runtime

- [ ] Exact clean candidate commit exists before remote access.
- [ ] Immediate preflight proves remote `0001` through `0020`, only `0021` pending and `0/0/0/0`.
- [ ] Supported dry run selects only `0021`; apply succeeds once; ledger aligns `0001` through `0021`.
- [ ] One governed initiation returns a valid exact intent.
- [ ] One synthetic JPEG transfers with overwrite disabled and remains fail-closed/unavailable.
- [ ] TLS export, SHA-256 and DPAPI restored-byte agreement pass.
- [ ] Governed object removal, local-copy deletion and verified absence pass.
- [ ] Final Auth/application/Storage/recovery is `0/0/0/0`; all orphan counts are zero.
- [ ] No CSV, availability bypass, safety fake, Vercel action, deployment, Production action, push or merge occurs.
- [ ] Evidence and durable state agree; JSON, privacy/secret, approved-path and diff checks pass.
- [ ] Sprint 023L is ready to resume at Preview configuration.

Stop only for a material failure. An unavailable redundant tool is not an acceptance blocker when equivalent or stronger committed/runtime proof passes.
