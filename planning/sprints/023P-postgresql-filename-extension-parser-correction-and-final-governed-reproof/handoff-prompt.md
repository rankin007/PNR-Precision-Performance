# Sprint 023P - Builder Handoff Prompt

Apply in the existing controlled worktree `C:\tmp\pnr-023l-remote-application-and-hosted-proof`. Preserve approved 023L/023N/023O evidence and the bounded runtime harness delta.

Create only additive migration `0021_postgresql_filename_extension_parser_correction.sql`. Reproduce the exact `0020` initiation function and change only the diagnosed double-backslash filename-extension regex to the approved single-backslash PostgreSQL form, preserving `extensions.digest`, restricted search path, signature, return shape, security, grants and all behavior. Add focused valid/invalid extension and semantic/ledger tests, run the complete local matrix, then stop `extension-parser-correction-ready-for-commit` for a separate commit instruction.

After the clean commit, apply only `0021` to project `uvskssaecdhxcgytkasc`, perform one final governed JPEG/DPAPI recovery proof, clean to exact `0/0/0/0`, and stop `extension-parser-corrected-final-governed-recovery-proven-clean` with Sprint 023L ready for Preview configuration.

Do not edit prior migrations, broaden search path, create another migration, retry blindly, configure/deploy Vercel, expose protected data, push or merge. Do not stop for unavailable redundant tooling when equivalent committed/runtime proof covers the boundary.
