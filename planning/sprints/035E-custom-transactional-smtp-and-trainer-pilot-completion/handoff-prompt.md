You are Builder for Sprint 035E — Custom Transactional SMTP And Trainer Pilot Completion.

Apply Architect Pack `planning/architect-packs/architect-pack-035E-custom-transactional-smtp-and-trainer-pilot-completion.md`, verify that it generates exactly four files under `planning/sprints/035E-custom-transactional-smtp-and-trainer-pilot-completion/`, and execute only from those generated sprint files.

Start from closed branch `codex/035D-prefetch-resistant-email-otp-authentication` at exact local SHA `83fc1a76b20a8b01711a2a2f63e551b49ffe15be`. Verify exact remote equality and a clean worktree before mutation; stop for baseline discrepancy rather than guessing or silently publishing prior work. Create `codex/035E-custom-transactional-smtp-and-trainer-pilot-completion`. Read `AGENTS.md`, the agent identity, all 035E files, Sprint 035–035D authorities and `planning/reviews/035D-email-otp-cutover-and-mailbox-checkpoint.md` from the exact baseline.

Deliver the missing boundary: configure an owner-selected production-capable custom transactional SMTP service for Supabase Auth, prove the existing OTP journey through actual protected delivery and session establishment, then complete the sequential A/B/C trainer dashboard pilot. Do not redesign authentication, dashboard behavior, schema, RLS, roles or permissions.

Before external mutation, obtain sanitized owner confirmation of provider selection, account/plan cost authority, sender identity, privacy posture, DNS authority and participant availability. The owner must enter credentials only through protected provider interfaces or approved child-only secret handling. Never request or expose SMTP credentials, inboxes, OTPs, links, tokens, Auth identifiers or mailbox details. If provider/account/DNS authority is not ready, produce the exact protected operator checklist, perform no external mutation and close `transactional-smtp-authority-pending-clean`.

Use only Supabase project `uvskssaecdhxcgytkasc`, `Precision Performance Clean Rebuild`, Singapore, with migration ledger exactly `0001`–`0021`; never contact `tagnbgkroihagjmvehlx`. Preserve the production Site URL, exact callback policy, OTP-only template, Confirm sign up, keys and unrelated settings. Configure only supported custom SMTP fields. Capture protected rollback state and sanitized classifications before mutation.

Where provider verification requires DNS, change only exact owner-approved records supplied by the selected provider. Read existing RRsets first, keep rollback values, preserve unrelated MX/DKIM/DMARC and website records, avoid duplicate SPF records and do not weaken DMARC. Prefer an approved dedicated Auth sending subdomain when compatible. Stop before mutation if target, ownership or safe merge behavior is ambiguous.

Preserve corrected production `dpl_9zVS8HSujThkFTP3hisyfBVknKWb`, OTP-compatible forward rollback `dpl_EUJyBLQp7okgbPwtBU6nmMH88L6A`, all five stable aliases and the current OTP template fingerprint unless exact readback supersedes identifiers safely. Use Vercel project `pnr-precision-performance`. Create a fresh exact-source generated Preview with zero production/custom aliases and rotate only its exact temporary callback. Do not redeploy production unless an in-scope deterministic OTP/auth correction is required and all release/rollback gates pass.

After provider and SMTP verification, run one bounded synthetic journey through the participant-facing application: unknown-recipient non-creation; confirmed exact-owned identity prepared without invitation email; provider-accepted OTP; protected recipient delivery; one code and no link; successful email `verifyOtp`; normal session; exact Preview `/portal`; dashboard permission isolation; wrong-horse denial; RLS agreement; safe invalid/expired/reused/superseded behavior; and application/Auth/Storage `0/0/0` cleanup with Auth last. Use bounded sanitized provider tooling that cannot emit protected fields. A settings-save, source check or constructed request is not delivery proof.

Only after synthetic acceptance passes, guardedly reverify Participant A. A completes OTP sign-in and the five-step trainer dashboard journey phone-first. Begin B only after A passes and state is reconciled; begin C only after B passes. Obtain phone-first completion for all and one larger-viewport pass across the group. Durable evidence uses A/B/C only. Preserve both ambiguous 035D Auth-only identities unless exact ownership and deletion authority are newly proven. Never coach past a product defect.

Run focused OTP/auth/session/redirect/plus-address/safe-failure tests, Sprint 035 dashboard and permission regressions, applicable canonical validation, lint, typecheck, production build, exact Preview and production/protected-route smoke, plus diff/encoding/staged-manifest/private-data/no-secret checks. Use equivalent or stronger safe proof for unavailable supporting tools and keep deterministic harness, validator, credential-entry and reporter corrections inside 035E. Stop only for a material target, authority, security, privacy, DNS, authentication, migration, production, integrity, scope or cleanup boundary. Use manual intervention only after safe alternatives are exhausted and document exact operator steps and subsequent verification.

At closeout, remove exact-owned application state dependency-safely and Auth last, remove the temporary callback, clear protected state, preserve not-owned identities with zero Sprint access, and reconcile provider/Supabase SMTP classification, DNS, template, Site URL, callbacks, five aliases, production and rollback. Refresh the 035E review, state, status, schedule, evidence index and Architect briefing. Commit a small intentional series and push only the scoped 035E branch. Do not merge, push `develop`, open a PR or rewrite history. Close with exactly one permitted 035E outcome and never claim product-wide Done.
