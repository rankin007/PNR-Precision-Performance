You are Builder for Sprint 035F — Resend Hosted Integration And Trainer Pilot Completion.

Apply Architect Pack `planning/architect-packs/architect-pack-035F-resend-hosted-integration-and-trainer-pilot-completion.md`, verify it generates exactly four files under `planning/sprints/035F-resend-hosted-integration-and-trainer-pilot-completion/`, then execute only from those generated files.

Start from closed Sprint 035E branch `codex/035E-custom-transactional-smtp-and-trainer-pilot-completion` at exact clean local/remote SHA `628bb69c29eb169dd80fe6d9d91d55c417e130b0`. Create `codex/035F-resend-hosted-integration-and-trainer-pilot-completion`. Read `AGENTS.md`, agent identity, all 035F files, Sprint 035–035E authorities and the 035E authority checkpoint. Do not reopen or rewrite prior sprint outcomes.

Use Resend's hosted Supabase integration as the exclusive provider path. The owner has established Resend and verified `precisionperformance.com.au`. Before mutation, obtain only sanitized confirmation that the current account/plan and provider privacy posture are accepted for the bounded pilot, the protected operator is ready and A/B/C remain available. Never request or receive account passwords, MFA codes, API keys, SMTP passwords, Supabase access/service keys, trainer inboxes, OTPs, links, Auth identifiers or mailbox headers.

The exact integration target is Supabase project `uvskssaecdhxcgytkasc`, `Precision Performance Clean Rebuild`, Singapore, migration ledger `0001`–`0021`; verified domain `precisionperformance.com.au`; sender name `Precision Performance`; sender address `no-reply@precisionperformance.com.au`; purpose Supabase Auth OTP only. Never contact old project `tagnbgkroihagjmvehlx`.

Guide the operator through Resend Integrations → Supabase → Connect to Supabase. The operator authenticates/MFA privately, selects only the exact project and verified domain, lets Resend create/transfer its integration API key directly, enters the exact sender identity and selects Configure SMTP Integration. The operator confirms masked custom-email configuration in the same Supabase project, changes no other setting, sends no test email and returns only:

Resend-Supabase integration configured; approved project confirmed; sender configured; credentials remained protected.

Manual SMTP host/port/username/password entry is prohibited. It is not a fallback. Stop if hosted integration is unavailable, requests DNS mutation, exposes a secret unsafely or cannot prove the exact target.

Before any email, capture sanitized pre/post configuration and executable rollback; verify OTP template one `.Token`, zero `ConfirmationURL`, zero links; preserve Confirm sign up, Site URL, callbacks, signup policy, keys and unrelated settings; verify minimum per-user interval `60` seconds and bounded hourly limits; and reverify OTP-capable production/rollback, five aliases and an exact alias-free Preview. Preserve Participant A and both ambiguous identities.

Then prepare one exact-owned, already-confirmed synthetic identity without invitation email and request exactly one OTP through exact Preview. Prove sanitized Resend acceptance, protected delivery to the intended synthetic recipient, one six-digit code/no link, normal session, exact `/portal`, dashboard isolation, wrong-horse denial and RLS agreement. Prove safe invalid/malformed/reused/superseded/expired behavior proportionally. Clean application state first and Auth last to `0/0/0`, then reverify all external invariants.

Only after synthetic acceptance passes, guardedly reverify A. A completes OTP sign-in and the five-step trainer journey phone-first. Begin B only after A passes and reconciliation completes; begin C only after B passes. Obtain phone-first completion for all and one tablet/desktop pass. Use A/B/C only in evidence and never coach past a defect.

Run applicable OTP/auth/session/redirect/plus-address/dashboard/permission regressions, canonical validation, lint, typecheck, production build, Preview/production smoke, diff/encoding/staged-manifest/private-data/no-secret checks. Use equivalent or stronger safe evidence for unavailable supporting tools and keep deterministic corrections in 035F. Stop only for material target, authority, security/privacy, provider, authentication, production, scope or cleanup risk.

At closeout, remove exact-owned application data dependency-safely and Auth last, remove temporary callback, clear protected state, preserve ambiguous/not-owned identities with zero Sprint access and reconcile Resend, sender, rate limits, template, Site URL/callbacks, five aliases, production and rollback. Refresh 035F review/state/status/schedule/evidence/briefing, commit intentionally and push only the scoped branch. Do not merge, push `develop`, open a PR or rewrite history. Close with exactly one permitted 035F outcome and never claim product-wide Done.
