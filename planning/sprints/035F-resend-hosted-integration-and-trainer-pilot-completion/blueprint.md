# Sprint 035F Blueprint

## 1. Open safely

1. Verify exact 035E SHA, local/remote equality and clean worktree; create the scoped 035F branch.
2. Mark only 035F active and preserve historical outcomes.
3. Record sanitized pre-mutation classifications for Supabase target/ledger, custom-email status, OTP template, rate limits, Site URL/callbacks, production/rollback deployments, five aliases, participant/ambiguous identity aggregates and exact-owned `0/0/0`.
4. Confirm the selected Resend domain is already verified without reading or changing DNS.

## 2. Confirm remaining owner authority

Obtain sanitized confirmation that the existing Resend plan and privacy posture are accepted for this bounded Auth pilot, the protected operator is ready and A/B/C availability remains current. Do not obtain secrets or participant identities.

## 3. Protected hosted integration

Builder leaves the protected browser at Resend Integrations and provides the operator these exact steps:

1. Open Resend Integrations and select Supabase.
2. Select Connect to Supabase and authenticate/MFA privately if required.
3. Select only project `uvskssaecdhxcgytkasc`.
4. Select verified domain `precisionperformance.com.au`.
5. Select Add API Key so Resend creates/transfers the key directly; do not copy or reveal it.
6. Enter sender name `Precision Performance`.
7. Enter sender address `no-reply@precisionperformance.com.au`.
8. Select Configure SMTP Integration.
9. Open the resulting Supabase Dashboard view and confirm the same project.
10. Confirm custom email delivery is enabled and credential fields are masked.
11. Change no template, URL, callback, signup policy, key, rate limit or unrelated setting.
12. Send no test email.
13. Return only the required sanitized confirmation.

If any target/domain/sender differs, secret becomes visible in an unsafe surface, DNS mutation is requested or manual SMTP entry is required, stop without completing the integration.

## 4. Builder post-integration preflight

1. Verify custom provider enabled through sanitized readback and exact target identity.
2. Capture a protected exactly restorable representation or provider-supported disconnect/rollback procedure plus sanitized fingerprint/classification.
3. Verify sender name/address classifications without exposing credentials.
4. Verify OTP template, Confirm sign up, Site URL, callbacks, signup policy, keys/security classifications and unrelated settings are unchanged.
5. Verify current rate limits. Keep minimum per-user interval `60` seconds; retain provider/default hourly limits unless bounded evidence requires a safe lower value. Never exceed the plan.
6. Reverify production, rollback, five aliases and exact alias-free Preview.
7. Do not send email until every preflight gate passes.

## 5. Synthetic delivered OTP acceptance

1. Prepare one fresh exact-owned, already-confirmed synthetic Auth identity without sending invitation/confirmation email.
2. Prove unknown-recipient generic non-creation without consuming a live send where deterministic evidence is stronger.
3. Confirm Resend/Supabase cooldown and provider readiness, then submit exactly one OTP through the exact Preview participant-facing path.
4. Prove sanitized Resend acceptance and protected Gmail delivery: exact intended synthetic recipient, one six-digit code, zero links and no unexpected branch.
5. Enter the OTP only in exact Preview; prove normal session, `/portal`, permitted dashboard/horse access, wrong-horse denial and RLS agreement.
6. Prove invalid/malformed/reused/superseded/expired behavior deterministically or with bounded provider-safe evidence.
7. Remove exact-owned application state dependency-safely and Auth last; prove `0/0/0`.
8. Reverify provider, Supabase, deployment and alias invariants.

## 6. Sequential trainer pilot

1. Guardedly reverify A; preserve exact plus-address semantics and sanitized reporting.
2. A requests one OTP and completes exact Preview sign-in plus the five-step dashboard journey phone-first.
3. Reconcile A and any material in-contract finding before B.
4. Repeat B, then C, never overlapping execution.
5. Obtain phone-first completion for all and at least one tablet/desktop pass across the group.
6. Record only A/B/C task outcomes, orientation, wording/viewport friction and permission agreement.

## 7. Cleanup and closeout

1. Remove exact-owned participant application access and synthetic fixtures dependency-safely; delete only exact Sprint-owned Auth identities last.
2. Preserve Participant A according to its ownership decision and preserve both ambiguous identities with zero 035F access.
3. Remove the temporary Preview callback when no longer required; clear protected sessions/mappings.
4. Prove final exact-owned application/Auth/Storage `0/0/0`, except preserved identities explicitly classified with zero Sprint access.
5. Reconcile Resend integration, sender, rate limits, template, Site URL/callbacks, five aliases, production and rollback.
6. Validate, update evidence/state/status/schedule/index/briefing, commit intentionally and push only the scoped branch.

## Rollback

Before the first email, establish how to disconnect/disable only the Resend hosted integration and restore the exact pre-035F custom-email state without changing OTP template, URLs, callbacks or keys. On material integration/authentication failure, clean exact-owned state Auth-last, execute the proven rollback, verify production invariants and close without participant acceptance.

## Approved files

Builder may edit only focused existing Auth/OTP helpers when runtime evidence requires an in-contract correction; focused 035F guarded proof/sanitizer/cleanup scripts; generated 035F sprint files; 035F review evidence and current lifecycle/index/briefing files. External mutation is limited to the exact Resend hosted integration and bounded Supabase rate-limit classification described here.
