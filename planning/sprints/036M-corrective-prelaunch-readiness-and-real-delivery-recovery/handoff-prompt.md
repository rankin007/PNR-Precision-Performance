# Sprint 036M Builder Handoff

## Task contract

objective: Recover prelaunch readiness through provider-native Supabase publishable/secret migration, independent seven-class dispositions, private identity authority and one normal real-trainer Production journey, with truthful partial/fallback and preserved 036L compatibility.

owns: Narrow 036M controllers/wrapper/tests/registrations, including one single-process `ProtectedWindow`, private JSON-lines transport, opaque in-memory state and finite provider/identity/trainer adapters; demonstrably required compatibility source in the passed plan; one exact-owned randomized system-temp export of accepted Git object `bfeb0b23c339b819f5dbcd4fe28d61c7a4dd9570` plus exact cleanup; docs/evidence; protected provider actions for seven classes; private two-identity dispositions; one trainer/phone-native message/verification/graph governed by the alive window but never bridged into the controller; at most one exact-source alias-free candidate/fixed aliases; closeout after diff pass.

must_not: Expose protected content; inspect contamination history; rotate JWT signing/sessions as key migration; guess/merge/delete unresolved identities; trigger business effects; change Product/schema/migrations/RLS/roles/permissions/DNS/email policy/commerce/enquiry/dependencies/lock; deploy dirty files, create a checkout/worktree, use redeploy as alias-free proof or delete any broad/unresolved path; exceed ceilings; stage/commit/push/merge/PR.

acceptance: AC-01..AC-40 truthfully dispositioned. Local readiness is withheld until `ProtectedWindow` and its ordered, interruption-safe, no-leak transaction pass. Target needs all seven classes, both identities, trainer, Production compatibility and cleanup. Trainer partial may retain unrelated blocked classes but is not launch readiness. Non-target landings are exact blocked/retained/compensated states.

verification: Executable protected transport/order/interruption/restart/no-leak, exact Git-tree export with only the committed placeholder-only `.env.example` permitted among `.env*` paths, protected-pattern cleanliness, project-link/`--skip-domain`/temp-cleanup, paired-key/predecessor/old-deployment/class/identity/session/trainer/alias/cleanup discriminators; exact arithmetic; sanitized ledger; private checkpoint only after corrected local gates; JSON/static/privacy/type/lint/build/diff/scope/lock/migration; same-Architect full diff read; closeout readback.

## Rules

Before implementation/external action, reread the directly amended sprint authority, official docs and source/tests; return exact contract/files/guards/AC map/commands/baselines/arithmetic/ceilings/protected transport/adapters/private steps/compensation/closeout/corrections/blockers; await the same lean Architect's `pass`.

Trust Git/provider readback, preserve dirty work, build red recording adapters first, use sanitized projections and non-transcribed private input. Protected/broad output stops materially.

Treat paired keys, each class and identity as explicit transactions. Verify before revoke, latch attempt before call, never compensate predecessor afterward, require independent readback and private append-only landing.

Do not convene people until the corrected local gates pass and the single-process protected window is ready. Number private steps and record only allowed results. Trainer has one normal phone-native message/verification while the window remains alive; controller receives no address/code/cookie/session and performs no retry/substitute.

Apply Evidence-Proportional execution. Use stronger safe substitutes; keep deterministic corrections in sprint; manual intervention last. Stop on material authority/security/privacy/destructive identity/Production/compensation/cleanup risk.

After checks return the full diff and real evidence to the same lean Architect. Only diff `pass` allows truthful closeout. Do not commit/push unless separately requested.
