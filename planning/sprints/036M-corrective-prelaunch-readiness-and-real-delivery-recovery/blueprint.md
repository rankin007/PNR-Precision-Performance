# Sprint 036M Blueprint

## Flight evidence

Risk boundary: Production API-key deactivation, Auth identities/sessions, exact-source redeploy/five aliases and one real authentication message can affect security, privacy, authorization, availability and irreversible state. The lean flight keeps the same durable gates without flight-class labels or decision budgets.

Invariant: no exposed configured credential remains silently valid; Supabase legacy keys are replaced without JWT-signing change or broken authorization; no unresolved identity is guessed/deleted; one trainer receives only synthetic least privilege; every landing is exact, blocked or compensated.

Trace:

- Official Supabase key authority -> project metadata -> private key creation -> complete Vercel bindings -> exact Git-object temporary export -> explicit-directory `--prod --skip-domain` deployment -> public/user/admin probes -> legacy deactivation -> provider status/old-deployment rejection.
- Seven names -> complete consumer graph -> native capability -> protected replace/revoke -> independent disposition.
- Private identity authority -> exact match -> anchored dependencies -> sessions -> retain or Auth-last delete.
- Alive protected window -> trainer privately enters address/code only in the normal phone UI -> one message/verification -> sanitized exact-ordinal/session readback plus phone-observed portal/synthetic horse/review/wrong-horse/sign-out/anonymous denial; controller receives no address, code, cookie or session.
- Mutations -> private append-only ledger -> compensation/accepted irreversible state -> zero residue/evidence.

Sources: current official Supabase docs govern keys/deactivation/JWT separation; installed packages and repository consumers govern compatibility; native metadata plus discriminating runtime behavior govern credentials; Sprint 036L plus fresh source/alias/binding readback governs Production; Phillip governs private identity disposition; current role/RLS governs access.

Discriminators:

1. New secret works but legacy enabled: fail.
2. Server migrates but browser uses legacy anon: fail before deactivation.
3. JWT signing changes: refuse.
4. Generic old page loads: insufficient; bound legacy authorization must fail.
5. Unrelated Stripe/Railway blocker preserves trainer evidence but withholds launch.
6. Old CRON accepts predecessor: fail.
7. Obsolete-labelled identity has one dependency: retain.
8. deleteUser succeeds but session works: fail.
9. Generated link/second OTP works: fail.
10. Candidate comes from redeploy/current dirty files, has an auto-assigned stable alias, differs from the accepted Git tree/project link, contains any `.env*` other than the exact committed placeholder-only `.env.example` blob, fails the protected-pattern check, or leaves its exact temporary export behind: fail/compensate before deactivation.

11. `SelfTest` and `CapabilityGate` pass but no executable `ProtectedWindow` transaction exists: remain `implementation-gap-blocked-clean`; do not convene people.
12. A protected value can reach argv, environment, clipboard, history, file, transcript, outer redirected I/O or child output, or the child pipe emits a non-allowlisted result: refuse before provider action.
13. Process exits after the legacy-deactivation latch and attempts ordinary compensation or retry: refuse; allow independent readback only.

Git authority: canonical HEAD d822c027c58ad88ec7472e35986e7a33d6a3d6c9, branch codex/025B-versioned-domain-authority-package, staged zero, accepted dirty tree. 036K closed blocked-clean at 943/943 and zero external/private actions; 036L remains live truth. Provider access/state, identities, old-consumer probes and delivery remain uncertain and fail closed.

## Sequence

1. Canonical guard; Architect directly amends and rereads the existing four 036M sprint files, roadmap and STATUS for this lean correction.
2. Builder independently reads authority/docs/source/tests and returns the exact correction plan, files, AC map, arithmetic, ceilings, protected transport, adapters, private steps, compensation and closeout.
3. The same Architect returns `pass`, `fix` or `ask`; only `pass` authorizes the correction build.
4. Implement the single-process `ProtectedWindow`, private JSON-lines transport, opaque in-memory state, finite adapters, ordered phases, interruption rules and sanitized evidence projection.
5. Red-first focused/retained plus privacy/static/JSON/type/lint/build/scope/lock/migration.
6. Announce private checkpoint and confirm 90-minute window without protected content.
7. Sanitized provider baseline; refuse broad/raw output.
8. Paired Supabase migration, exact-source redeploy, probes, aliases, deactivation, independent readback.
9. Resolve other classes independently; block trainer only for possible Auth/trainer/Production paths.
10. Privately disposition identities; session-first/Auth-last where exact.
11. Prepare graph; keep the protected window alive while the trainer performs one phone-native message/verification and reports only finite journey booleans; controller performs no Auth verification/session handoff.
12. Clean/reconcile every class, identity, fixture, source, alias, binding, ceiling and residue; rerun gates.
13. The same Architect reads the full correction diff against AC-01..AC-40 and the real-check evidence, returning `pass`, `fix` or `ask` under the lean two-round correction bound.
14. After diff `pass` only, closeout and disk readback.

## Verification and closeout

Exact plan must provide truthful arithmetic and executable protected-transport/phase/interruption, paired-key/partial-pair, legacy/JWT, old-deployment, seven-class, revoke ambiguity, identity/session/Auth-last, one-message/verification, source/candidate/five-alias/three-binding, cleanup/privacy/ceiling and retained 036K/036L/role/commerce/enquiry proof, plus JSON/static/type/lint/build/diff/scope/lock/migration/final ledgers. Source substrings alone cannot prove transport isolation, revocation, privacy, session or provider behavior.

After the same Architect's diff pass only, reconcile acceptance, lean review, evidence/report, STATE, STATUS, decisions/risks/questions, matrix, DoD, ROADMAP, schedule, lifecycle, index, briefing, deferred scope and delivery road. Record target/partial/fallback truth. Do not claim enquiry, commerce, under-60, full 035S, provider restoration or Product Done.
