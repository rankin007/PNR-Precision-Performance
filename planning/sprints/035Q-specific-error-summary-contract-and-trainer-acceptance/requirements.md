# Sprint 035Q — Specific Error Summary Contract And Trainer Acceptance

## Outcome

Complete roadmap Sprint 035 from the closed 035P baseline by correcting the hosted proof to target the existing `#biochemistry-error-summary` contract instead of asserting a page-wide alert count, passing all 14 maintained authenticated rendered checks, completing one bounded human trainer journey, and restoring all Sprint-owned temporary state cleanly.

The target outcome is `first-trainer-preview-access-and-core-journey-complete-clean`. This is a proof-harness correction, not authority to redesign accessibility or change product behavior. If the specific summary does not expose the existing alert, actionable-guidance, and intended-focus contract, stop `accessibility-contract-expansion-required-clean` without changing product source.

## Workflow profile and starting authority

Use `strict` controls for authentication configuration, participant privacy, synthetic hosted data, external mutation, cleanup, and production non-impact. Start only from clean branch `codex/035P-authorised-callback-disposition-and-trainer-acceptance` at exact closeout SHA `1f03578a4e53d9edd17614376dc5c4b7ffa21eee`. Create only `codex/035Q-specific-error-summary-contract-and-trainer-acceptance` in a clean scoped worktree.

Treat candidate `b1bf770dc6ab3839cfc24c53563f6ed6310e08ba`, Ready alias-free Preview `dpl_6NBk2VKPZZUS4QqdstqDHtGk8EVV`, 98 passing local checks, rendered proof stopped at 6/14 after two attempts, zero human attempts, resolved obsolete-callback disposition, production-only provider rollback state, and final Auth/application/Storage `0/0/0` as baseline. Read the 035P review and closeout authorities before action. Do not use dirty `develop`, an older 035 candidate, file recency, or production source as authority. Sprint 032 remains production; 035Q does not authorise production deployment.

## Exact proof correction

Before external mutation, add focused fixed-before-execution assertions proving the harness locates `#biochemistry-error-summary`, requires that element to expose `role="alert"`, verifies actionable validation guidance, and verifies the intended focus behavior. The harness must not require total page-wide `[role="alert"]` cardinality to equal one. Other alerts may coexist when they do not replace, hide, contradict, or prevent focus on the specific form summary.

Do not change `app/`, `components/`, `lib/`, `supabase/`, schema, migrations, auth/RLS, roles, permissions, or product behavior. If current source or rendered behavior does not satisfy the specific established contract, record exact sanitized evidence and stop for separate product/accessibility authority.

## Temporary callback, rendered proof, and participant contract

Safely reread provider configuration and require the 035P rollback state: unchanged production Site URL, exactly the production callback, no governing Preview callback, and no alternate callback. Make that sanitized state rollback authority. Add only the exact HTTPS callback for the governing alias-free Preview origin plus `/auth/callback`, dependency-track it, reread immediately, and prove rollback-state plus exactly one.

Confirm the Preview remains exact-source, Ready, non-production, alias-free, healthy, and anonymously denies protected routes. Create only minimum synthetic non-identifying trainer/stable/horse/workflow and inaccessible-horse state through existing contracts. Run all 14 authenticated rendered checks, including assigned-only visibility, wrong-horse/cross-stable denial without leakage, route/action agreement, specific error-summary semantics and focus, sign-out/revocation, responsive behavior, keyboard/focus behavior, and accessible status presentation.

Only after 14/14 passes, privately guide exactly one product-owner-designated trainer through approved-account sign-in; assigned synthetic stable and horse recognition; horse workspace opening; one existing permitted biochemistry action; dashboard return; sign-out; protected-route denial; and repeat sign-in with the same bounded assignment. Prove the full task on a supported phone and orientation on one larger viewport. The trainer controls their mailbox and enters authentication material privately. Permit at most two cooldown-safe participant attempts; diagnose once and make only an in-scope deterministic harness/reporting correction before a second attempt.

## Approved actions and boundaries

Builder may apply and verify this Pack; create the scoped branch/worktree; correct only the maintained 035P proof harness and focused tests/reporting; add 035Q planning, review, lifecycle, and sanitized evidence; temporarily add and remove the exact governing Preview callback; create and remove minimum synthetic Preview fixtures; coordinate one designated trainer; reuse the exact-source Preview; make deterministic credential, harness, reporter, renderer, formatting, or encoding corrections that do not change product behavior; commit intentionally; push only the scoped 035Q branch when required; and update canonical closeout records.

Out of scope: any `app/`, `components/`, `lib/`, or `supabase/` change; accessibility redesign; schema/migration/RPC/RLS/role/permission/assignment change; provider replacement; production deployment/promotion; production/custom aliases; DNS; Site URL change; production callback mutation; wildcard callbacks; whole-list replacement; production data; merge; `develop` push; PR creation; real participant/horse/stable data; auth bypass; mailbox automation; formulas/thresholds/clinical guidance; uploads; voice/OCR; trends; commerce; enquiry delivery; pricing; public work; broad onboarding; or a Core Product Done claim.

## Evidence-proportional execution and manual intervention

Stop only for material baseline/target ambiguity, protected-data or secret exposure, participant ambiguity, destructive uncertainty, unauthorised scope expansion, auth/RLS/privacy/integrity failure, unexpected real data, production impact, partial external mutation, genuine product-contract failure, or cleanup that cannot be proven safe. Substitute equivalent or stronger safe evidence when a supporting tool is unavailable. Keep in-scope credential, harness, validator, reporter, renderer, formatting, encoding, and deterministic non-product corrections in 035Q; do not create another follow-up solely for a supporting-tool limitation or redundant proof.

Manual intervention is last resort. Record what is blocked, evidence checked, exact private user action, step-by-step instructions, and what Builder will verify afterward. Never request or record callback URLs, credentials, tokens, cookies, mailbox values, authentication material, participant identity, or protected configuration.
