You are Builder for Sprint 035Q — Specific Error Summary Contract And Trainer Acceptance.

Start in the project repository, read `templates/method/120x-agent-identity.md` and `AGENTS.md`, and assume the Builder role. Start only from clean branch `codex/035P-authorised-callback-disposition-and-trainer-acceptance` at exact closeout SHA `1f03578a4e53d9edd17614376dc5c4b7ffa21eee`. Create only `codex/035Q-specific-error-summary-contract-and-trainer-acceptance` in a clean scoped worktree. Apply `planning/architect-packs/architect-pack-035Q-specific-error-summary-contract-and-trainer-acceptance.md`, verify it generates exactly four files under `planning/sprints/035Q-specific-error-summary-contract-and-trainer-acceptance/`, and execute only from those generated files.

Read all four generated 035Q files, the 035P review and closeout authorities, lifecycle ledger, roadmap, final acceptance and ownership records, operations handoff, design/messaging authority, auth/RLS and biochemistry evidence, and current source. Reconcile candidate `b1bf770dc6ab3839cfc24c53563f6ed6310e08ba`, Ready alias-free Preview `dpl_6NBk2VKPZZUS4QqdstqDHtGk8EVV`, 98 passing local checks, the 6/14 rendered stop after two attempts, zero human attempts, production-only provider state, and final Auth/application/Storage `0/0/0`. Do not use dirty `develop`, older candidates, file recency, or production source as authority.

Before editing any file, present the exact file-by-file plan, scope guards, and acceptance criteria and wait for explicit approval. Product-source files under `app/`, `components/`, `lib/`, and `supabase/` are not approved. Add focused tests fixed before execution, then correct only the maintained 035P harness/reporting so it locates `#biochemistry-error-summary`, requires that element to expose `role="alert"`, verifies actionable validation guidance and intended focus behavior, and does not require total page-wide `[role="alert"]` cardinality to equal one. Permit unrelated alerts only when they do not replace, hide, contradict, or prevent focus on the specific summary. If the existing product does not satisfy this contract, record sanitized evidence and stop `accessibility-contract-expansion-required-clean`; do not change product source.

Run the focused and maintained local proof before external mutation. Safely reread provider configuration and require the exact 035P rollback state: unchanged production Site URL, exactly the production callback, no governing Preview callback, and no alternate callback. Make that sanitized state rollback authority. Dependency-track and add only the exact HTTPS governing alias-free Preview origin plus `/auth/callback`; add no wildcard, pattern, alternate Preview, localhost entry, custom alias, or Site URL change. Reread immediately and prove rollback-state plus exactly one.

Confirm exact-source Preview identity, Ready/non-production/alias-free status, health, and anonymous protected-route denial. Create only minimum synthetic non-identifying fixtures through existing contracts with cleanup order fixed first. Run all 14 maintained authenticated rendered checks and require assigned-only visibility, wrong-horse/cross-stable denial without leakage, route/action agreement, the specific error-summary alert/guidance/focus contract, sign-out/revocation, responsive behavior, keyboard/focus semantics, accessible status presentation, and protected-value suppression.

Only after 14/14 passes, privately guide exactly one product-owner-designated trainer through approved-account sign-in, assigned synthetic stable and horse recognition, horse workspace, one existing permitted biochemistry action, dashboard return, sign-out, protected-route denial, and repeat sign-in. Prove the full task on a supported phone and orientation on one larger viewport. The trainer controls their mailbox and enters authentication material privately. Permit at most two cooldown-safe participant attempts; diagnose once and make only an in-scope deterministic harness/reporting correction before a second attempt.

After acceptance or terminal stop, end Sprint-owned sessions, remove synthetic application dependencies, delete an exact Sprint-created Auth identity last, and remove only the exact 035Q callback. Never delete adopted, pre-existing, production, or ambiguous state. Reread provider configuration, prove exact production-only rollback restoration, reconcile Auth/application/Storage to `0/0/0`, and prove production deployment, aliases, DNS, Site URL, callback, public release, production data, `develop`, and unrelated state remained unchanged.

Run maintained 98 local checks, the focused 035Q assertions fixed before execution, all 14 rendered checks, maintained 021AH and 022/022B regressions, canonical JSON/domain/roles/Supabase-self/static/TypeScript/lint/local validation, production build from exact source or an equivalent clean workspace, `git diff --check`, approved-path, staged-manifest, secret/private-data, generated-artifact, and source/Preview identity checks. Record explicit test-count arithmetic. Substitute equivalent or stronger safe evidence for unavailable supporting tools and keep credential, harness, validator, reporter, renderer, formatting, encoding, and deterministic non-product corrections inside 035Q.

Stop only for material baseline/target ambiguity, secret or protected-data exposure, participant ambiguity, destructive uncertainty, unauthorised scope expansion, genuine product-contract failure, auth/RLS/privacy/integrity failure, unexpected real data, production impact, partial mutation, or cleanup that cannot be proven safe. For genuine manual intervention, record the blocked fact, evidence checked, exact private steps, and what will be verified; never request protected values.

At closeout refresh the 035Q review, lifecycle ledger, state, status, schedule, roadmap, evidence index, final acceptance matrix, ownership register if applicable, and Architect briefing. Mark roadmap Sprint 035 done only when 14/14 rendered proof, the one-trainer journey, cleanup, and non-impact all pass. Keep Core Product Done false. Commit intentionally and push only the scoped 035Q branch when required. Do not deploy production, merge, push `develop`, open a PR, rewrite history, change product source, broaden onboarding, or expand product behavior.
