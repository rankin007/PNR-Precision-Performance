# Sprint 023O - Builder Handoff Prompt

Apply this Pack in the existing controlled worktree `C:\tmp\pnr-023l-remote-application-and-hosted-proof` on branch `codex/023L-remote-application-and-hosted-proof`. Preserve all approved 023L/023N evidence.

Create only additive migration `0020_schema_qualified_pgcrypto_initiation.sql`, preserving the exact migration `0019` initiation function except for `extensions.digest(...)`, dependency precondition, grants and comment. Keep `search_path = pg_catalog, public`. Add focused semantic, ledger and adversarial proof; run the full local matrix; then stop `pgcrypto-initiation-correction-ready-for-commit` for a separate commit instruction.

Only after that clean commit exists, apply only `0020` to project `uvskssaecdhxcgytkasc`, prove the function metadata and governed initiation, complete one fail-closed JPEG DPAPI recovery proof, and clean to `0/0/0/0`. Stop `pgcrypto-initiation-corrected-governed-recovery-proven-clean` with Sprint 023L ready to resume at Preview configuration.

Never edit `0019`, broaden search path, add another migration, bypass governed initiation, configure/deploy Vercel, expose secrets/payloads, push or merge.
