# Sprint 023O - Schema-Qualified Pgcrypto Initiation Correction And Governed Reproof Blueprint

## Phase 1 - Preserve And Apply Pack

Verify the controlled dirty-worktree manifest, clean index, 023N classification, exact linked target/ledger/zero state and immutable migration hashes. Apply/verify all four 023O files without losing 023L/023N evidence.

## Phase 2 - Build Additive Candidate

Create migration `0020` by reproducing the exact `0019` initiation function definition and changing only the digest reference to `extensions.digest`. Add dependency precondition, exact grants/comment and focused semantic-equivalence tests. Align the candidate ledger through `0020` with adversarial proof.

## Phase 3 - Local Validation And Commit Stop

Run the complete required local matrix. Prove approved paths, no dependencies, unchanged `0001` through `0019`, exact search path and no unrelated source change. Record migration hash and stop `pgcrypto-initiation-correction-ready-for-commit`.

After a separate commit instruction, stage only the approved combined candidate, review full manifest/diff, rerun staged gates, commit locally and verify clean status. Report the exact commit SHA before remote access.

## Phase 4 - Immediate Remote Preflight And Apply

From that exact clean commit, reconfirm target identity/health, remote ledger `0001` through `0019`, zero state and supported dry run containing only `0020`. Apply once through the supported mechanism. Stop without repair/retry on failure.

Verify remote/local ledger exactly `0001` through `0020`, function identity, restricted search path, schema-qualified dependency, grants and absence of unrelated catalog change using bounded read-only aggregate evidence.

## Phase 5 - Governed Initiation First

Create exact synthetic authority fixtures and invoke initiation once. If it fails, stop and clean; do not transfer bytes. If it succeeds, verify only safe intent/count classifications and continue.

## Phase 6 - One-Object Fail-Closed Recovery Proof

Upload one synthetic JPEG through the exact intent with overwrite disabled. Finalise to unavailable/blocked only. Export through TLS, hash, DPAPI-encrypt, restore/decrypt and compare. Remove through governed compensation/lifecycle, delete local copies and prove absence.

## Phase 7 - Cleanup And Closeout

Delete exact fixtures with Auth last. Prove `0/0/0/0` and zero orphans. Run all local evidence validations.

Successful outcome: `pgcrypto-initiation-corrected-governed-recovery-proven-clean`.

Record Sprint 023L as ready to resume at protected Vercel Preview configuration, not complete. Do not configure or deploy Vercel in Sprint 023O. Keep final evidence uncommitted unless separately instructed.
