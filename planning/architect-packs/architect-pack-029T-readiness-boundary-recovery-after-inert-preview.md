============================================================
FILE: planning/sprints/029T-readiness-boundary-recovery-after-inert-preview/requirements.md
============================================================

# Sprint 029T - Readiness Boundary Recovery After Inert Preview

## Outcome

Put every Vercel read and write behind one tested exact argument allowlist, re-establish the target-resource baseline while preserving the known inert Preview, and make one tightly bounded attempt to prove a new unaliased Production candidate ready through authenticated SMTP verification without sending email.

Target outcome: readiness-boundary-recovered-unaliased-clean.

Safe fallback: readiness-boundary-recovery-blocked-clean, after exact safe compensation of every newly created 029T credential and configuration resource.

Public enquiry remains unavailable. All five public aliases stay on accepted Sprint 036L. Sprint 029R remains conditional.

## Workflow and flight class

Use strict. The flight is critical because it handles private provider pages, creates one provider credential, writes Production settings, creates a Production-targeted deployment and may delete exact newly owned resources during compensation. A fresh reviewer must pass the exact Builder plan before changes, and a different fresh inspector must pass the result before closeout.

## Canonical start and inherited work

Work only in C:\Users\rrank\OneDrive\PNR Precision Performance Canonical from exact Git HEAD d822c027c58ad88ec7472e35986e7a33d6a3d6c9 on branch codex/029T-readiness-boundary-recovery-after-inert-preview. Before Pack application and again at Builder start, resolve the current directory and Git top level; both must normalize to the canonical path.

At discovery the worktree contained 29 modified tracked files, 32 untracked entries and no staged files. Preserve all inherited 029P, 029Q and 029S implementation/planning work, method files, user-owned AGENTS.md, .codex_work content, delivery_road_map.docx and unrelated files. Do not overwrite, revert, stage, commit, publish, move or copy them to another checkout.

The Architect creates only this Pack, the visible roadmap amendments and STATUS marker. Builder must dry-run and apply the Pack to exactly four traversal-free destinations inside planning/sprints/029T-readiness-boundary-recovery-after-inert-preview, then reread all four files. No commit or push is in scope.

## Current verified truth

- Sprint 029S accepted candidate-origin hardening and identity-blind provider projection. Focused proof passed 128/128 and retained proof passed 616/616, for 744/744.
- The PowerShell and JavaScript origin boundaries, temporary preflight authentication, shared-auth preservation and fixed provider projection are locally accepted and must remain unchanged.
- The 029S target sequence stopped when an ad-hoc read-only PowerShell helper declared its formal parameter as $Args. PowerShell replaced it with the automatic unbound-argument variable, the intended inspect vector became empty and bare Vercel created one Preview.
- A fresh local discriminating example reproduced the failure: a formal $Args parameter received zero elements, while the same four-element input bound exactly through a non-reserved CommandArgs parameter.
- Vercel CLI documents JSON format for deployment, environment list, alias list, inspect and deployment list; deploy supports Production target, skip-domain and yes flags.
- Retained Preview dpl_7MTexxU6RecGHZvCE9BukUwZU6Hx is Ready, non-Production, has zero expected public aliases and is inert. It is evidence, not a 029T target.
- Exact current baseline is dedicated SMTP rows zero, temporary auth rows zero, local live credential absent, five generic SMTP rows unchanged and all five public aliases on accepted Ready Sprint 036L.
- Safe provider projection reports one verified expected domain, two existing sending-access keys, zero full-access keys, no 029T target key and no raw secret.
- Owner scope permits the exact least-privilege Resend and Vercel actions listed here. Passwords, tokens and secret values must never pass through chat or agent-visible output.

## Task contract

**objective:** Prove exact non-reserved Vercel command construction and use it with the accepted identity-blind provider boundary for one least-privilege authenticated no-send readiness attempt, without changing public traffic, Product data, existing credentials or live routing.

**owns:** New 029T PowerShell controller, readiness harness, provider control projector, focused tests, package registrations and operations document; one Resend sending-access key restricted to precisionperformance.com.au; four dedicated Production-only SMTP rows; three temporary Production-only verifier rows; one new fixed Windows Credential Manager target; one Production-targeted deployment with automatic domains disabled and public metadata pp_sprint=029T; one authenticated no-send check; one post-expiry denial; exact cleanup or compensation; critical reviews and closeout.

**must_not:** Use $Args or another automatic/reserved variable as a formal command-vector parameter; run bare/default Vercel; run an ad-hoc direct Vercel command outside the reviewed controller; display, return, log, persist, inspect, paste into chat, put in shell arguments or manually re-enter any password, token, key, bearer, cookie, provider identity, protected address or existing environment value; delete, invoke, promote, alias or modify the retained 029S Preview; change shared CRON_SECRET, existing Resend keys or generic SMTP rows; create a full-access or all-domain key; call /api/enquiries; send email; inspect mailboxes or delivery logs; change enquiry/Supabase data, migration 0023, Auth, trainer fixtures, commerce, DNS or aliases; retry prior candidates; create a second 029T deployment; commit, push, merge, open a PR or rewrite history.

**acceptance:** Empty, missing, reordered, duplicated, extra, mutation-capable or bare/default Vercel vectors are refused before any runner call; every permitted read/write/deploy vector is exact and reaches a synthetic runner byte-for-byte unchanged through a non-reserved parameter; all controller output is fixed and sanitized. Fresh provider/Vercel/alias projections match the approved baseline including the unchanged retained Preview and zero 029T deployment. Controls for restricted-key creation, blind transfer, target-key deletion/confirmation cancellation, Vercel Sensitive save and clipboard clearing are proved before token creation. At most one restricted key, four dedicated rows, three temporary rows, one local credential and one new Production-targeted zero-alias deployment are created. Exactly one authenticated no-send check returns finite Resend ready, one same-bearer post-expiry request is denied before handler execution, temporary resources are removed, and no public/email/data/alias effect occurs.

**verification:** A001-A034; canonical/Pack/branch/diff gates; exact no-edit Builder plan; fresh critical plan review; observed 744/744 baseline; red-first command-vector, projector and controller tests; retained 029S/029Q/029P/029O/029N suites; PowerShell parse/self-test; typecheck, zero-warning lint, Production build, static/JSON/encoding/privacy/scope scans; combined scanner-clean candidate manifest; fixed safe Resend/Vercel/alias/deployment projections; Windows Credential Manager proof; one exact deployment; one finite no-send result; one expired denial; cleanup and zero-side-effect proof; distinct fresh inspection; final disk/Git/provider/Vercel/live-routing readback.

## Exact Vercel command boundary

Create one Sprint 029T controller that is the only path for Vercel CLI calls after implementation begins. Its formal array parameter must use a non-reserved name such as CommandArgs. It must reject null or empty arrays before starting a process.

Every operation has a closed exact vector. At minimum:

- Environment metadata: env ls production --format json --no-color
- Alias metadata: alias ls --format json --limit 100 --no-color
- Exact deployment inspection: inspect followed by one allowlisted opaque deployment ID, then --format json --no-color
- 029T deployment baseline: list --meta pp_sprint=029T --format json --no-color
- Deployment: deploy --prod --skip-domain --yes --format json --no-color --meta pp_sprint=029T
- Environment add/remove: only exact approved 029T-owned names, Production target and reviewed yes/sensitive/stdin flags.

The order, count and spelling are part of the contract. No prefix matching, subset matching, free-form passthrough, empty default, force flag, token argument, arbitrary metadata or caller-supplied extra value is permitted.

SelfTest must use a synthetic runner and prove no process starts for empty, $Args-lost, reordered, missing, duplicated, extra, wrong-target, force, bare deploy or unrelated vectors. It must prove each permitted vector reaches the runner unchanged, stdin is used only for an exact approved environment add, the input value never enters output, and only fixed projection/count/class output is returned.

Set NO_UPDATE_NOTIFIER only inside the captured child process. Capture stdout and stderr separately. Parse only command-specific JSON from stdout. Never strip arbitrary prefixes from mixed output or return raw CLI text. Any nonzero/null exit, malformed JSON, unknown field/shape, unexpected target or extra resource fails closed with a fixed code.

## Target-resource baseline

Target-resource zero means no 029T key, no dedicated SMTP rows, no temporary verifier rows, no local 029T credential and no deployment carrying pp_sprint=029T. It does not mean historical deployments are absent.

Fresh baseline must also prove:

- exact linked Vercel project;
- retained 029S Preview exists, is Ready, non-Production and has zero expected public aliases;
- 029N and 029O remain without expected public aliases;
- accepted 036L is Ready and holds all five public aliases;
- five existing generic SMTP rows have unchanged name/type/target metadata without values;
- one verified precisionperformance.com.au domain;
- two existing Resend sending-access keys, zero full-access keys, no 029T target key and no raw-secret shape.

The retained 029S Preview stays untouched throughout 029T. It is excluded from the 029T creation count but included in every final containment readback.

## Identity-blind controls and secret transfer

Reuse the accepted 029S provider projection for domain/key/environment baselines. Add only the minimum 029T fixed-output projection needed to prove target-key delete/menu/confirm/cancel controls and any missing 029T form state. Protected canary snapshots must remain invariant and unknown shapes fail closed.

Before key creation, prevalidate through fixed projections:

- exact Resend restricted-key name, sending-access and domain controls;
- Copy and dismiss controls for the one-time token;
- an existing-row delete menu and confirmation/cancel path without confirming deletion;
- exact Vercel project/environment form, Production-only blank-branch scope, Sensitive pass field, paste/save/navigation controls;
- a no-read clipboard-clear action.

Create exactly one key named Precision Performance public enquiry 029T with sending access restricted to precisionperformance.com.au. While its token is visible, perform only the prevalidated Copy, direct paste to the prepared Vercel Sensitive PUBLIC_ENQUIRY_SMTP_PASS field, save, token dismissal/navigation and clipboard clearing. Do not snapshot, screenshot, evaluate page content, inspect logs, read the clipboard or expose any token fragment during this interval.

After the token is no longer visible and clipboard is cleared, fixed metadata may resume. Require exactly one target key and the Sensitive pass row before adding the three public structural rows through the controller: PUBLIC_ENQUIRY_SMTP_HOST=smtp.resend.com, PUBLIC_ENQUIRY_SMTP_PORT=465 and PUBLIC_ENQUIRY_SMTP_USER=resend.

If the blind path or exact deletion control cannot be prevalidated, stop before key creation. Never ask the user to send or type a secret through chat.

## Temporary authentication, candidate and no-send proof

Use a new fixed 029T Windows Credential Manager target. Generate at least 256 random bits without human input, keep the raw bearer only in that secure target/process memory, and create exactly three Sensitive Production-only blank-branch rows through stdin:

- PUBLIC_ENQUIRY_PREFLIGHT_AUTH_SHA256
- PUBLIC_ENQUIRY_PREFLIGHT_AUTH_NOT_BEFORE
- PUBLIC_ENQUIRY_PREFLIGHT_AUTH_EXPIRES_AT

The window must be ordered and no longer than fifteen minutes. The raw bearer never reaches the candidate.

Only after fixed projections prove four dedicated and three temporary rows may the controller run the one exact deployment vector. Require one new deployment carrying pp_sprint=029T, exact canonical 029T source, Production target, Ready state and zero public aliases. Automatic domains remain disabled. No second deployment is in scope.

Run the nine immutable public/protected/API/disabled-commerce checks without storing data. Invoke exactly one authenticated smtp-preflight. Require result smtp-preflight, status ready, providerClass resend and errorClass null with no protected detail.

After expiry, use the same bearer exactly once and require sanitized HTTP 404 before the SMTP handler. Delete the three temporary rows and fixed local credential, then prove both absent. On success retain the new restricted key, four dedicated rows and unaliased candidate for future 029R. Public enquiry stays unavailable.

## Failure, compensation and manual intervention

No local/privacy/baseline/control pass means no external mutation. Any unexpected protected output, unapproved vector or process start stops immediately.

If key/configuration/candidate/no-send result is non-target, do not retry and do not create a second deployment. After exact ownership proof, remove only the newly created 029T key, four dedicated rows, three temporary rows and local credential. A new unaliased 029T candidate may remain inert. The retained 029S Preview and all existing resources stay untouched. Claim clean fallback only when cleanup, five-alias safety and both inert deployment states are proved.

Stop only for a material wrong target, privacy/secret exposure, destructive uncertainty, unauthorized scope expansion, partial provider state, failed integrity/security behaviour, Production impact or cleanup that cannot be proved. A supporting tool failure alone is not a blocker when equal or stronger safe proof exists. Deterministic controller, projector, parser, test, validator, reporter, formatting and encoding corrections remain inside 029T when Product scope is unchanged.

Manual intervention is last. If required, record what is blocked, evidence already checked, exact user action, numbered steps and what Builder will verify afterward. Never request a secret through chat or command arguments.

## Approved implementation and closeout files

The Builder plan must narrow this candidate set:

- scripts/PreflightAuth029T.ps1
- scripts/autonomous-public-enquiry-029T.mjs
- scripts/provider-browser-projection-029T.mjs
- scripts/test-autonomous-public-enquiry-029T.mjs
- scripts/test-provider-browser-projection-029T.mjs
- package.json for exact script registration only; no dependency change
- docs/PUBLIC_ENQUIRY_READINESS_BOUNDARY_RECOVERY_029T.md
- the four applied 029T sprint files for within-intent corrections and closeout
- standard directly affected closeout files: planning/STATUS.json, planning/STATE.md, planning/ROADMAP.md, planning/ARCHITECT_BRIEFING.md, planning/SPRINT_LIFECYCLE_LEDGER.md, planning/SPRINT_SCHEDULE.md, planning/EVIDENCE_INDEX.md, planning/FINAL_PRODUCT_ACCEPTANCE_MATRIX.md, planning/DECISIONS.md, planning/RISKS.md, planning/QUESTIONS.md and delivery_road_map.md.

The accepted 029S and 029Q Product/authentication/origin/projector files remain unchanged unless the exact reviewed Builder plan proves a smaller necessary within-intent correction. No migration, public route/form, database, Auth, trainer, alias, generic environment or commerce file may change.

## Combined protected-content scanning

The 029T scanner and manifest must cover the seven 029T implementation files plus every inherited uncommitted runtime/auth/provider file that enters the deployment source, including app/api/internal/enquiries/route.ts, lib/enquiries/env.ts, lib/enquiries/provider.ts, lib/enquiries/preflight-auth.ts and the relevant accepted 029P/029Q/029S controller/projector files.

Scan before every content-emitting diff or inspection. Scanner output is limited to scan name, pass/fail, aggregate count and filename only when containment requires it. Never output matching lines, context, fragments or captures. Unexpected positives enter no-output containment. Only scanner-clean files may enter a diff, manifest or closeout reread.

============================================================
FILE: planning/sprints/029T-readiness-boundary-recovery-after-inert-preview/blueprint.md
============================================================

# Sprint 029T Blueprint

## Execution sequence

1. Prove canonical workspace, exact start HEAD, exact branch and inherited dirty classification. Dry-run, apply and reread exactly four Pack files.
2. Builder reads the applied sprint, 029S implementation/closeout, exact PowerShell failure example, Vercel JSON command help, current provider/Vercel baseline and live-routing truth. Builder returns one exact no-edit critical plan with task contract, files, guards, A001-A034, literal commands, baseline/target arithmetic, closeout, Pack corrections and blockers.
3. A genuinely fresh reviewer judges the exact plan under the three-decision critical budget. Amend applied sprint authority for any accepted within-intent Pack correction before pass.
4. Observe the complete 744/744 inherited baseline. Add red-first tests for lost $Args, empty/bare/default/reordered/missing/extra/force vectors, exact unchanged safe vectors, separated stdout/stderr, fixed JSON output and no runner call on refusal.
5. Add the 029T provider-control projector and protected canaries for target deletion, confirm/cancel and required form controls.
6. Implement only the approved new 029T controller, harness, projector, tests, package registrations and operations document. Preserve Product/auth/provider/origin history.
7. Run focused and retained suites, exact arithmetic, PowerShell parse/self-test, typecheck, zero-warning lint, Production build and static/JSON/encoding/privacy/scope gates. Scan before content diffs and create the combined scanner-clean manifest.
8. Run the controller Baseline operation. Require exact project, target-resource zero, retained 029S Preview unchanged, prior candidates without public aliases, zero 029T deployment and five public aliases on 036L.
9. Use only tested live projections to prove the Resend baseline and prevalidate restricted-key, Copy/dismiss, existing-row delete/cancel, Vercel Sensitive paste/save and no-read clipboard-clear controls.
10. Create one restricted 029T Resend key and blindly save its token to the Sensitive pass row. Dismiss the token and clear clipboard before metadata inspection resumes.
11. Prove one target key and pass row, then add the three public SMTP rows through the controller. Require four exact dedicated rows and unchanged generic rows.
12. Provision the random local bearer and three temporary hash/window rows through stdin. Require exact bounded metadata.
13. Run the controller's one exact Production skip-domain deployment vector. Require one pp_sprint=029T deployment, exact source, Ready and zero aliases; keep the retained 029S Preview unchanged.
14. Run immutable checks, then exactly one authenticated no-send smtp-preflight. Require finite Resend ready.
15. After expiry, perform exactly one same-bearer denial, delete three temporary rows and the local credential, and prove absence.
16. Prove zero public request, email, data/migration and alias effects. On non-target, perform exact safe compensation with no retry. On target, retain only the restricted key, four SMTP rows and inert candidate.
17. A distinct fresh inspector judges every applicable acceptance item, exact diff, real evidence and closeout plan under the three-decision budget.
18. After inspection pass, update durable truth, leave 029R conditional, set STATUS sprint-closed, run final ordered scan and reread disk/Git/provider/Vercel/candidate/live routing. Do not commit or push.

## Architecture trace

Vercel command safety:

operation -> exact closed vector -> non-reserved CommandArgs parameter -> allowlist and target check -> synthetic or real runner -> separate stdout/stderr -> command-specific JSON parser -> fixed safe projection

Provider secret:

verified domain -> one restricted Resend key -> blind Copy/paste -> Sensitive Production pass row -> token dismissed and clipboard cleared -> metadata-only readback

Temporary authentication and readiness:

random bearer -> fixed 029T Credential Manager target -> hash plus <=15-minute timestamps -> three Production rows -> one exact pp_sprint=029T Production skip-domain deployment -> exact smtp-preflight only -> expiry denial -> delete temporary rows/local target

Live routing stays separate:

five aliases -> accepted 036L; retained 029S Preview stays non-Production and unaliased; no enquiry request, email, data or alias change.

## Flight evidence

1. **Class and reason:** critical; provider credential creation, secret transfer, Production configuration, deployment, authentication and compensation are in scope.
2. **Acceptance invariant at risk:** no missing or malformed argument vector can start Vercel; no protected identity/secret becomes agent-visible; one exact candidate verifies without send; live routing and data stay unchanged.
3. **Affected layers and paths:** new 029T PowerShell controller, JavaScript harness, provider-control projector, tests, package registration and operations document; accepted 029S provider/origin boundary; Resend controls; Vercel environment/deployment control plane; Windows Credential Manager; one new candidate.
4. **Source, transformation and sink:** a named operation becomes one exact allowlisted array and fixed JSON projection; raw provider DOM becomes only classes/counts; a one-time restricted token moves blindly to a Sensitive binding; random bearer becomes only hash/window candidate config; candidate performs verify only.
5. **Discriminating examples:** the same four arguments bind as zero through formal $Args but as four exact elements through CommandArgs; deploy without --skip-domain or with --force is refused before runner; changed protected canaries yield identical projection; a target key with missing delete/cancel controls blocks creation; complete dedicated SMTP returns ready while any non-target gets no retry.
6. **Durable verification source:** exact branch/HEAD/dirt; local PowerShell binding reproduction; Vercel CLI help for JSON and deploy flags; applied four-file sprint; counted tests/build; combined scanner-clean hashes; fixed provider/Vercel projections; one deployment metadata tag; no-send/expiry/cleanup results; five-alias readback.
7. **Known uncertainty:** signed-in UI may be the only safe least-privilege key creation/transfer path. SMTP verify cannot prove sender acceptance or delivery. The retained Preview is historical evidence and stays untouched. Existing keys and generic rows cannot change by inference. 029R remains separate.

## Failure and compensation matrix

| Boundary | Required result | Non-target handling |
| --- | --- | --- |
| Canonical/Pack | Exact workspace, start, branch and four files | Stop before Builder changes |
| Plan | Fresh pass within three decisions | Correct within Pack intent or ask |
| Command vectors | Red-first exact arrays; empty/bare/wrong vectors never call runner | Correct locally; no external action |
| Projection/local gates | Protected canaries, 744 baseline plus new arithmetic, build and scans pass | Correct locally; no external action |
| Target baseline | Zero 029T resources; known historical resources exact | Stop before creation |
| Control prevalidation | Restricted create, blind transfer, delete/cancel, save and clear all ready | Stop before key creation |
| Key/SMTP configuration | One restricted key and four exact rows, no observation | Remove exact new resources if safe; stop |
| Temporary auth | Fixed target, hash-only <=15-minute config | Remove exact rows/credential; stop |
| Candidate | One exact Ready zero-alias pp_sprint=029T Production deployment | No second deployment; compensate |
| No-send | One finite Resend ready | No retry; compensate |
| Expiry/cleanup | Same bearer 404; temporary rows/local target absent | Stop if cleanup cannot be proved |
| Side effects | Zero public/email/data/alias effects; retained Preview unchanged | Contain and stop |
| Inspection/close | Distinct pass and consistent final truth | Do not claim landing |

## Evidence arithmetic

Observe and report 744/744 before edits: retained 029S focused 128 plus earlier retained 616. New 029T command-vector/controller and provider-control assertions add to that baseline. Builder must state the exact new counts and total in its reviewed plan. PowerShell parser and Credential Manager self-test remain separate evidence.

============================================================
FILE: planning/sprints/029T-readiness-boundary-recovery-after-inert-preview/acceptance.md
============================================================

# Sprint 029T Acceptance

- [ ] A001 Canonical current directory and Git top level equal the permanent repository; start HEAD is d822c027c58ad88ec7472e35986e7a33d6a3d6c9; branch is exact 029T; inherited dirt is classified and preserved.
- [ ] A002 The one Pack dry-runs, applies and rereads exactly four traversal-free 029T files.
- [ ] A003 Builder returns the exact no-edit critical plan with task contract, files, guards, A001-A034, literal commands, observed 744 baseline and target arithmetic, closeout, Pack corrections and blockers.
- [ ] A004 A genuinely fresh reviewer returns pass on the exact plan within three decisions; stable findings are recorded and resolved.
- [ ] A005 Only reviewed 029T implementation and standard closeout files change; accepted Product/auth/migration/data/generic SMTP/Auth/trainer/commerce/alias/origin files remain unchanged unless a required within-intent correction passes review.
- [ ] A006 No Vercel command-vector formal parameter uses $Args or another reserved/automatic variable; a discriminating test reproduces zero lost arguments versus the exact safe non-reserved vector.
- [ ] A007 Every permitted Vercel read, environment write/remove and deploy command is a closed exact ordered vector; no prefix/subset/free-form passthrough exists.
- [ ] A008 Empty, missing, reordered, duplicated, extra, force, wrong-target, bare/default deploy and unrelated vectors fail before the synthetic runner is called.
- [ ] A009 Every permitted vector reaches the synthetic runner byte-for-byte unchanged; stdin is accepted only for an exact approved environment add and never appears in output.
- [ ] A010 Real runner capture separates stdout/stderr, uses child-only update-notifier suppression, parses command-specific JSON only and returns fixed sanitized output on success or failure.
- [ ] A011 Accepted 029S JavaScript/PowerShell origin, temporary-auth, shared-auth and baseline provider-projection behaviour remains unchanged and retained tests pass.
- [ ] A012 The 029T provider-control projector has a fixed closed schema for required form and target delete/confirm/cancel controls; protected-canary changes do not change output and unknown shapes fail closed.
- [ ] A013 The complete inherited 744/744 baseline is observed before edits and exact new 029T arithmetic passes without replacing retained coverage.
- [ ] A014 PowerShell parse/self-test, focused/retained tests, typecheck, zero-warning lint, Production build and mapped static/JSON/encoding/privacy/scope checks pass.
- [ ] A015 Combined protected scans precede content-emitting diff/inspection and the scanner-clean exact manifest covers all 029T files plus inherited uncommitted runtime/auth/provider files entering deployment source.
- [ ] A016 Fresh identity-blind Resend preflight proves one verified expected domain, two existing sending-access keys, zero full-access keys, no 029T target key, no raw-secret shape and no protected output.
- [ ] A017 Fresh controller baseline proves exact project, dedicated rows zero, temporary rows zero, local credential absent, generic rows five unchanged, zero pp_sprint=029T deployments, retained 029S Preview Ready/non-Production/zero expected public aliases, prior candidates without expected public aliases and five aliases on 036L.
- [ ] A018 Before token creation fixed projections prove exact restricted-key controls, Copy/dismiss, existing-row delete plus confirmation/cancel without deletion, exact Vercel Sensitive Production blank-branch paste/save/navigation controls and no-read clipboard clearing.
- [ ] A019 Exactly one Resend key named Precision Performance public enquiry 029T is created with sending access restricted to precisionperformance.com.au; existing keys remain unchanged.
- [ ] A020 While the token is visible no snapshot, screenshot, evaluation, log or clipboard read occurs; blind transfer saves only the Sensitive pass row, then token view is dismissed and clipboard cleared before metadata resumes.
- [ ] A021 Exactly four new Production-only blank-branch dedicated SMTP rows exist; pass is Sensitive, public structural values match Resend authority and five generic rows remain unchanged.
- [ ] A022 A new fixed 029T Credential Manager target proves absence, encrypted synthetic round trip, exact deletion and fixed sanitized output.
- [ ] A023 The live bearer has at least 256 random bits, is never human-entered/output/file/argument data and creates exactly three Sensitive Production hash/window rows through stdin with a window no longer than fifteen minutes.
- [ ] A024 Exactly one deployment command uses the exact reviewed vector and creates one pp_sprint=029T Production-targeted Ready zero-alias deployment from the exact canonical 029T branch/workspace; the retained 029S Preview is unchanged.
- [ ] A025 Nine immutable public/privacy/protected/API/disabled-commerce checks pass without storing data or calling /api/enquiries.
- [ ] A026 Exactly one authenticated smtp-preflight returns result smtp-preflight, status ready, providerClass resend and errorClass null with no protected detail.
- [ ] A027 After expiry exactly one same-bearer request returns sanitized HTTP 404 before the SMTP handler; no second no-send attempt occurs.
- [ ] A028 The three temporary project rows and fixed local credential are removed and proved absent; retained candidate config is expired and unusable.
- [ ] A029 Evidence proves zero /api/enquiries calls, email/notification attempts, mailbox/log inspection, enquiry/bucket/Supabase/migration changes and alias changes.
- [ ] A030 On success retain only the new restricted key, four dedicated rows and unaliased 029T candidate for 029R; public enquiry stays unavailable and all five aliases stay on 036L.
- [ ] A031 On any non-target result no retry or second deployment occurs; only exact newly owned 029T resources are compensated, and clean fallback is claimed only after complete proof while historical resources remain unchanged.
- [ ] A032 A fresh inspector distinct from Builder and plan reviewer returns pass within three decisions after judging criteria, diff, real evidence and closeout plan.
- [ ] A033 Durable closeout records simple plain-English outcome, complete Flight evidence, plan/inspection decisions, findings/substitutions/residuals, 029R conditional state, roadmap/briefing reconciliation and STATUS sprint-closed.
- [ ] A034 Final ordered scanner-clean disk/Git/provider/Vercel/candidate/live-routing readback is consistent; no commit, push, merge, PR or history rewrite occurs.

The target proves only protected, authenticated, no-send transport readiness. It does not prove sender acceptance, delivery, inbox receipt, public enquiry behaviour or activation.

============================================================
FILE: planning/sprints/029T-readiness-boundary-recovery-after-inert-preview/handoff-prompt.md
============================================================

# Sprint 029T Builder Handoff

You are Builder for critical Sprint 029T in C:\Users\rrank\OneDrive\PNR Precision Performance Canonical.

First prove canonical current directory and Git top level, exact HEAD d822c027c58ad88ec7472e35986e7a33d6a3d6c9, exact 029T branch and inherited dirty classification. Dry-run and apply the one 029T Pack to exactly four traversal-free paths, then reread all four applied files. They are your scope.

Do not edit implementation yet. Read the accepted 029S controller, harness, projector, focused tests and closeout; reproduce the $Args versus non-reserved binding distinction locally; read the exact Vercel JSON/deploy option evidence and current safe provider/Vercel/live-routing truth. Return one exact plan with the five-part task contract, exact files, exclusions, A001-A034 mapping, literal commands, observed 744/744 baseline, exact target arithmetic, external ceilings/compensation, standard closeout, Pack corrections (None is valid) and blockers. A genuinely fresh reviewer must pass before implementation.

## Task contract

**objective:** Prove exact non-reserved Vercel command construction and use it with the accepted identity-blind provider boundary for one authenticated no-send readiness attempt without changing public traffic, data, existing credentials or live routing.

**owns:** New 029T controller/harness/provider-control projector/tests/docs/package registration; one restricted Resend key; four dedicated SMTP rows; three temporary verifier rows; one fixed local credential target; one exact pp_sprint=029T Production skip-domain zero-alias candidate; one no-send check; one expiry denial; exact cleanup/compensation; critical reviews and closeout.

**must_not:** Use $Args as a vector parameter or run bare/ad-hoc Vercel; expose identity/address/secret data; alter the retained 029S Preview, existing keys, generic SMTP, shared auth, Product/data/migration/Auth/trainer/commerce/DNS/aliases; submit or send; inspect mailbox/logs; retry prior candidates; create a second 029T deployment; commit or publish.

**acceptance:** A001-A034, including red-first exact-vector proof, fixed JSON capture, retained 029S behaviour, protected control canaries, full local gates, fresh target baseline, prevalidated blind transfer and deletion controls, one restricted key, four SMTP rows, three temporary rows, one exact Ready zero-alias deployment, one finite ready result, one post-expiry 404, temporary cleanup and zero public/email/data/alias effect.

**verification:** Observed 744/744 baseline plus exact new arithmetic; PowerShell parse/self-test; focused/retained suites; typecheck/lint/build/static/JSON/encoding/privacy/scope gates; combined scanner-clean manifest; fixed safe provider/Vercel projections; credential proof; one exact candidate/no-send/expiry result; cleanup; five-alias and retained-Preview readback; fresh inspection; durable final reread.

Use evidence-proportional execution. Stop only for a material target, privacy/secret, destructive, scope, partial-provider, integrity/security, Production or cleanup boundary. Use equal or stronger safe proof when a supporting tool fails. Keep deterministic controller/projector/test/parser/validator/reporter corrections inside 029T when Product scope is unchanged. Manual intervention is last and must record the five required parts. Never request a secret through chat or arguments. Write every user-facing report in plain simple English.
