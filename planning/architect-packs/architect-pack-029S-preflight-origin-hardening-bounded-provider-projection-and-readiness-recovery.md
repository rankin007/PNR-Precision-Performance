============================================================
FILE: planning/sprints/029S-preflight-origin-hardening-bounded-provider-projection-and-readiness-recovery/requirements.md
============================================================

# Sprint 029S - Preflight Origin Hardening, Bounded Provider Projection and Readiness Recovery

## Outcome

Close INSPECT-001, establish a tested identity-blind provider inspection path, and make one tightly bounded attempt to prove a new unaliased Production candidate ready through authenticated SMTP verification without sending email.

Target outcome: preflight-origin-provider-projection-and-readiness-recovered-unaliased-clean.

Safe fallback: preflight-origin-provider-projection-and-readiness-recovery-blocked-clean, with every newly created 029S credential and configuration resource removed when safe ownership and cleanup can be proved.

Public enquiry remains unavailable. All five public aliases stay on accepted Sprint 036L. Sprint 029R remains conditional.

## Workflow and flight class

Use strict. The flight is critical because it handles privacy-sensitive provider pages, creates one provider credential, writes encrypted Production configuration, creates a Production-targeted deployment and may delete exact newly owned resources during compensation. One fresh reviewer must pass the exact Builder plan before changes, and a different fresh inspector must pass the result before closeout.

## Canonical start and inherited work

Work only in C:\Users\rrank\OneDrive\PNR Precision Performance Canonical from exact Git HEAD d822c027c58ad88ec7472e35986e7a33d6a3d6c9 on branch codex/029S-preflight-origin-hardening-bounded-provider-projection-and-readiness-recovery. Before Pack application and again at Builder start, resolve the current directory and Git top level; both must normalize to the canonical path.

At discovery the worktree contained 29 modified tracked files, 24 untracked files and no staged files. Preserve inherited Sprint 029P/029Q work, method files, the user-owned AGENTS.md, .codex_work content and delivery_road_map.docx. Do not overwrite, revert, stage, commit, publish, copy to another checkout or use legacy repositories. The Architect Pack, applied four-file sprint, 029S roadmap/STATUS changes and later Builder files are 029S-owned.

Dry-run the Pack to exactly four traversal-free destinations inside planning/sprints/029S-preflight-origin-hardening-bounded-provider-projection-and-readiness-recovery, apply it once and reread all four files. No commit or push is in scope.

## Current verified truth

- Sprint 029Q closed blocked-clean. Its local implementation passed 616/616 plus typecheck, lint, Production build, static/JSON checks and a clean nine-file privacy scan.
- Sprint 029Q stopped before provider mutation because a broad signed-in provider snapshot displayed protected account identity. Zero new keys, bindings, credentials, deployments, requests, emails, data changes or aliases resulted.
- Fresh 029S discovery used a fixed, identity-blind read-only projection. It reported one expected verified domain, two sending-access keys, no full-access keys, no target 029S key and no raw-secret shape. It returned no account identity, address, key name or token.
- The JavaScript 029Q candidate-origin validator does not reject a non-default port. The PowerShell 029Q origin validator does not reject URL user information or a non-443 port.
- The exact Vercel project has zero dedicated SMTP rows and zero temporary preflight-auth rows. Existing generic SMTP rows remain outside scope.
- All five aliases remain on accepted Ready Sprint 036L. Public enquiry is unavailable.
- Owner scope permits the exact least-privilege Resend and Vercel actions listed here through signed-in/provider access. Passwords, tokens and secret values must never pass through chat or agent-visible output.

## Task contract

**objective:** Reject unsafe candidate origins, create a durable identity-blind provider projection, and use it to support one least-privilege no-send readiness attempt while leaving public traffic, data, existing credentials and live routing unchanged.

**owns:** New sprint-specific JavaScript and PowerShell readiness controllers; one pure provider-projection module and its canary tests; narrow package registrations and operations document; one Resend sending-access key restricted to precisionperformance.com.au; four dedicated Production-only SMTP bindings; three temporary Production-only preflight-verifier bindings; one fixed new Windows Credential Manager target; one Production-targeted deployment with automatic domains disabled; one authenticated no-send readiness check; one post-expiry denial; exact cleanup/compensation; critical reviews and durable closeout.

**must_not:** Display, return, log, persist, inspect, paste into chat, put in shell arguments or manually re-enter a password, token, key, bearer, cookie, provider account identity, protected address or existing environment value; change shared CRON_SECRET; change existing Resend keys or generic SMTP rows; create a full-access or all-domain key; call /api/enquiries; send email; inspect a mailbox or delivery logs; change enquiry/Supabase data, migration 0023, Auth, trainer fixtures, commerce, DNS or aliases; retry 029N/029O/029Q; create a second deployment; commit, push, merge, open a PR or rewrite history.

**acceptance:** Both origin validators reject credentials and every port other than HTTPS 443; the provider projection has a fixed allowlist and synthetic protected canaries prove it cannot return identity, address or token text; fresh live projections prove the exact baseline without protected output; all local and privacy gates pass before external mutation; at most one exact restricted Resend key, four dedicated SMTP rows, three temporary verifier rows and one zero-alias candidate are created; exactly one authenticated no-send check returns finite Resend ready; the same bearer is rejected after expiry before handler execution; temporary rows and local credential are removed; target success retains only the new restricted key, four SMTP rows and inert unaliased candidate for 029R; any non-target result gets no retry and exact safe compensation.

**verification:** A001-A032; canonical/Pack/branch/diff gates; exact no-edit Builder plan; fresh critical plan review; observed 616/616 baseline; red-first origin and projection-canary tests; retained 029O/029P/029Q suites; PowerShell parse/self-test; typecheck, zero-warning lint, Production build, static/JSON/encoding/privacy/scope scans; fixed safe browser projections; Vercel name/type/target counts without values; Windows Credential Manager sanitized proof; exact one-deployment/zero-alias result; one finite no-send result; one expired denial; cleanup and zero-side-effect proof; distinct fresh inspection; final disk/Git/provider/Vercel/live-routing readback.

## Origin hardening

Create Sprint 029S controller and harness files instead of changing historical 029Q evidence unless the approved Builder plan proves a smaller within-intent correction is required.

The JavaScript candidate origin must be a plain HTTPS origin: no user name, password, path, query, fragment or explicit port. The host must meet the existing deployment-host allowlist.

The PowerShell candidate origin must use HTTPS, contain no user information, path other than slash, query or fragment, use only port 443 and match the existing deployment-host allowlist.

Red-first tests must include HTTPS with explicit 443, HTTPS with a non-default port, user information, path, query, fragment, wrong scheme and wrong host. A plausible implementation that merely checks scheme and host must fail.

## Identity-blind provider projection

Add one pure Sprint 029S projection module with a closed input/output contract. Its outputs may contain only fixed projection name, fixed field count, exact-page boolean, safe row/count numbers and approved status/access-class counts. It must never return free-form text, account identity, address, provider key name, token, secret fragment, DOM, accessibility tree, error detail or unmatched fields.

Synthetic tests must feed a realistic provider snapshot containing canary account names, email addresses, addresses and token-shaped strings. Projection output must be identical whether those protected canaries change, and none may appear in serialized output. Wrong page, duplicate target, unexpected access class, unexpected field count or unknown shape fails closed before mutation.

For live browser work, obtain the signed-in DOM snapshot only inside the browser runtime, immediately pass it through the tested projector, discard the raw value, and return only the allowlisted projection. Do not display or send a raw snapshot, screenshot, console output or broad page state to the agent.

Before any one-time token appears, use the same bounded method to prove the exact Resend and Vercel controls. During the token-visible interval perform only the prevalidated create, Copy, direct paste into the prepared Vercel Sensitive field, save, dismissal/navigation and clipboard-clear actions. Do not take a snapshot, screenshot, evaluate page content, inspect logs or read the clipboard. Metadata projection may resume only after the token is no longer visible and the clipboard is cleared.

If this non-observation transfer cannot be proved safe, stop before key creation and record the five-part manual intervention report. Never ask the user for a secret.

## External ceilings and success path

Fresh preflight must prove exactly one verified precisionperformance.com.au domain, exactly two existing sending-access keys, zero full-access keys, no 029S target key, no raw-secret shape, exact Vercel project, zero dedicated SMTP rows, zero temporary preflight rows, unchanged generic-row metadata and all five aliases on 036L.

Create exactly one Resend key named Precision Performance public enquiry 029S, with sending access restricted to precisionperformance.com.au. Transfer it directly into a new Sensitive, Production-only, blank-branch PUBLIC_ENQUIRY_SMTP_PASS row. Add only these three public structural rows beside it: PUBLIC_ENQUIRY_SMTP_HOST=smtp.resend.com, PUBLIC_ENQUIRY_SMTP_PORT=465 and PUBLIC_ENQUIRY_SMTP_USER=resend. Do not expose any value through evidence.

Use a new fixed 029S Windows Credential Manager target. Generate at least 256 random bits without human input, keep the raw bearer only in that secure target/process memory, and provision exactly three Production-only blank-branch rows through stdin or an equally strong process API: PUBLIC_ENQUIRY_PREFLIGHT_AUTH_SHA256, PUBLIC_ENQUIRY_PREFLIGHT_AUTH_NOT_BEFORE and PUBLIC_ENQUIRY_PREFLIGHT_AUTH_EXPIRES_AT. The window must be at most fifteen minutes.

Create exactly one Production-targeted deployment from the exact canonical 029S branch/workspace with automatic domain assignment disabled. Require Ready and zero aliases. Run only immutable checks, then exactly one authenticated smtp-preflight. Require result smtp-preflight, status ready, providerClass resend and errorClass null with no protected detail.

After expiry, use the same bearer exactly once and require sanitized HTTP 404 before the SMTP handler. Delete the three temporary rows and fixed local credential, then prove both absent. On success retain the new restricted Resend key, four dedicated rows and unaliased candidate for later 029R. Public enquiry stays unavailable.

## Failure, compensation and manual intervention

No local/privacy/preflight pass means no external mutation. Any unexpected protected output stops immediately.

If key/configuration/candidate/no-send result is non-target, do not retry and do not create a second deployment. After exact ownership proof, remove only the newly created 029S key, four SMTP rows, three temporary rows and local credential. An unaliased candidate may remain inert. Claim clean fallback only when cleanup and five-alias safety are proved.

Stop only for a material wrong target, protected-data exposure, destructive uncertainty, unauthorized scope expansion, partial provider state, failed integrity/security behaviour, Production impact or cleanup that cannot be proved. A supporting tool problem alone is not a blocker when equal or stronger safe proof exists.

Manual intervention is last. If required, record: what is blocked, evidence already checked, exact user action, numbered steps and what Builder will verify afterward. Never request a secret through chat or command arguments.

## Approved implementation and closeout files

The Builder plan must narrow this candidate set:

- scripts/PreflightAuth029S.ps1
- scripts/autonomous-public-enquiry-029S.mjs
- scripts/provider-browser-projection-029S.mjs
- scripts/test-autonomous-public-enquiry-029S.mjs
- scripts/test-provider-browser-projection-029S.mjs
- package.json for exact script registration only; no dependency change
- docs/PUBLIC_ENQUIRY_PREFLIGHT_PRIVACY_AND_SMTP_READINESS_029S.md
- the four applied 029S sprint files for within-intent corrections and closeout
- standard directly affected closeout files: planning/STATUS.json, planning/STATE.md, planning/ROADMAP.md, planning/ARCHITECT_BRIEFING.md, planning/SPRINT_LIFECYCLE_LEDGER.md, planning/SPRINT_SCHEDULE.md, planning/EVIDENCE_INDEX.md, planning/FINAL_PRODUCT_ACCEPTANCE_MATRIX.md, planning/DECISIONS.md, planning/RISKS.md, planning/QUESTIONS.md and delivery_road_map.md.

The existing 029Q authentication and Product source should stay unchanged unless the exact reviewed Builder plan proves a necessary within-intent correction. No migration, public route/form, database, Auth, trainer, alias, generic environment or commerce file may change.

## Protected-content scanning

Scan every candidate file before any content-emitting diff or inspection. Scanner output is limited to scan name, pass/fail, aggregate count and filename when containment requires it. Never output matching lines, context, fragments or captures. Unexpected positives enter no-output containment. Only scanner-clean files may enter a content diff, candidate manifest or closeout reread.

============================================================
FILE: planning/sprints/029S-preflight-origin-hardening-bounded-provider-projection-and-readiness-recovery/blueprint.md
============================================================

# Sprint 029S Blueprint

## Execution sequence

1. Prove canonical workspace, exact start HEAD, exact branch and inherited dirty classification. Dry-run, apply and reread exactly four Pack files.
2. Builder reads the applied sprint, 029Q implementation/closeout, exact validators, provider controls, Vercel metadata and live-routing truth. Builder returns one exact no-edit plan with task contract, files, guards, A001-A032, commands, baseline/target arithmetic, closeout, Pack corrections and blockers.
3. A genuinely fresh reviewer judges the exact plan. Correct stable within-intent findings and obtain pass within three decisions before edits.
4. Observe the 616/616 baseline. Add red-first JavaScript and PowerShell cases that reject explicit/non-default ports and user information.
5. Add the pure identity-blind projection and protected-canary tests. Prove unknown shape, wrong page, unexpected access class/count and duplicate target fail closed.
6. Implement only the approved new 029S harness/controller/tests/package/doc files. Preserve historical Product/auth/provider code unless the reviewed plan proves a smaller required correction.
7. Run focused/retained tests, exact arithmetic, PowerShell parse/self-test, typecheck, zero-warning lint, Production build and static/JSON/encoding/privacy/scope gates. Scan before content diffs and create an exact scanner-clean manifest.
8. Use only the tested live projection to prove the Resend/Vercel/alias zero-resource baseline and prevalidate the secret-blind controls.
9. Create one restricted Resend key and blind-transfer it to the Sensitive pass row. Add the three public SMTP rows and prove exact name/type/target counts without values.
10. Create the random local bearer, three temporary hash/window rows through stdin and one Ready Production-targeted zero-alias deployment with automatic domains disabled.
11. Run immutable checks and exactly one authenticated no-send SMTP preflight. Require finite Resend ready.
12. Wait for expiry, perform exactly one same-bearer denial, remove all three temporary rows and the local credential, and prove absence.
13. Prove zero public requests, email, data/migration and alias effects. On non-target, perform exact safe compensation with no retry. On target, retain only the restricted key, four SMTP rows and inert candidate.
14. A distinct fresh inspector judges every acceptance item, exact diff, local/external evidence and closeout plan within three decisions.
15. After inspection pass, update durable truth, leave 029R conditional, set STATUS sprint-closed, run final ordered scan and reread disk/Git/provider/Vercel/candidate/live routing. Do not commit or push.

## Architecture trace

Safe provider read:

signed-in browser page -> raw snapshot held only inside browser runtime -> strict pure projector -> fixed booleans/counts only -> agent evidence

Readiness path:

verified domain -> one restricted Resend key -> blind Copy/paste -> Sensitive Production SMTP pass row -> four dedicated rows -> one zero-alias candidate -> authenticated Nodemailer verify only -> finite ready

Temporary authentication:

random bearer -> fixed 029S Windows Credential Manager target -> SHA-256 plus <=15-minute timestamps -> three Production rows -> exact smtp-preflight only -> expiry denial -> delete three rows and local target

Live routing stays separate:

five aliases -> accepted Sprint 036L; no enquiry request, email, data or alias change.

## Flight evidence

1. **Class and reason:** critical; private provider UI, secret transfer, Production configuration, deployment and compensation are in scope.
2. **Acceptance invariant at risk:** no protected identity or secret becomes agent-visible or durable; unsafe origins fail; one candidate verifies without send; live routing and data do not change.
3. **Affected layers and paths:** new 029S JavaScript harness, PowerShell controller, provider projection, tests, package registration and operations document; Resend domain/API key controls; Vercel environment/deployment controls; Windows Credential Manager; one unaliased candidate.
4. **Source, transformation and sink:** raw provider DOM is reduced inside the browser to fixed safe counts; a one-time restricted token moves blindly to a Sensitive binding; a random bearer becomes only hash/window candidate config; the candidate performs verify only and later rejects the expired bearer.
5. **Discriminating examples:** HTTPS on an explicit non-default port fails; URL user information fails; protected canary identity/address/token changes do not change projection output; an unknown provider field fails; complete dedicated SMTP reports ready while a no-send failure gets no retry.
6. **Durable verification source:** exact branch/HEAD/dirt, applied sprint, counted tests/build, scanner-clean hashes, fixed live projections, Vercel metadata, credential-controller result, one deployment ID/origin/zero-alias projection, no-send result, expired 404, cleanup and five-alias readback.
7. **Known uncertainty:** Resend key creation may be UI-only and requires prevalidated blind transfer. SMTP verify cannot prove delivery or sender acceptance. Existing keys and bindings cannot be changed by inference. 029R remains separate.

## Failure and compensation matrix

| Boundary | Required result | Non-target handling |
| --- | --- | --- |
| Canonical/Pack | Exact workspace, start, branch and four files | Stop before Builder changes |
| Plan | Fresh pass within three decisions | Correct within Pack intent or ask |
| Origin/projection | Red-first cases and protected canaries pass | Correct locally; no provider change |
| Full local gates | 616 baseline plus exact new arithmetic, build and scans pass | Correct locally; no provider change |
| Provider baseline | Fixed safe expected counts and zero new resources | Stop before creation |
| Blind transfer | One restricted key and four exact rows, no observation | Remove exact new resources if safe; stop |
| Temporary auth | Fixed target, hash-only <=15-minute config | Remove exact rows/credential; stop |
| Candidate | One exact Ready zero-alias deployment | No second deployment; compensate |
| No-send | One finite Resend ready | No retry; compensate |
| Expiry/cleanup | Same bearer 404; temp rows/local target absent | Stop if cleanup cannot be proved |
| Side effects | Zero public/email/data/alias effects | Contain and stop |
| Inspection/close | Distinct pass and consistent final truth | Do not claim landing |

## Evidence arithmetic

Observe and report the 616/616 inherited baseline before edits. New 029S origin and projection assertions add to that coverage. Report focused, retained and whole-Product results separately; do not hide a failed group inside a total.

============================================================
FILE: planning/sprints/029S-preflight-origin-hardening-bounded-provider-projection-and-readiness-recovery/acceptance.md
============================================================

# Sprint 029S Acceptance

- [ ] A001 Canonical current directory and Git top level equal the permanent repository; start HEAD is d822c027c58ad88ec7472e35986e7a33d6a3d6c9; branch is exact 029S; inherited dirt is classified and preserved.
- [ ] A002 The one Pack dry-runs, applies and rereads exactly four traversal-free 029S files.
- [ ] A003 Builder returns the exact no-edit critical plan with task contract, files, guards, A001-A032, commands, observed baseline/target arithmetic, closeout, Pack corrections and blockers.
- [ ] A004 A genuinely fresh reviewer returns pass on the exact plan within three decisions.
- [ ] A005 Only reviewed 029S implementation and standard closeout files change; Product/auth/migration/data/generic SMTP/Auth/trainer/commerce/alias source remains unchanged unless a required within-intent correction is approved.
- [ ] A006 JavaScript accepts only a plain HTTPS allowlisted origin with no user information, path, query, fragment or explicit port.
- [ ] A007 PowerShell accepts only HTTPS, no user information, slash-only path, no query/fragment, allowlisted host and port 443.
- [ ] A008 Red-first cases distinguish valid origin from explicit 443, non-default port, user information, path, query, fragment, wrong scheme and wrong host; a scheme-and-host-only implementation fails.
- [ ] A009 The provider projector has a fixed closed output schema containing only approved booleans/counts/classes and rejects unknown page/shape/count/access state.
- [ ] A010 Synthetic full snapshots containing protected identity, address and token canaries yield no canary text, fragment, name or extra field; changing canaries does not change the safe projection.
- [ ] A011 Live signed-in snapshots remain only inside the browser runtime and only tested projections reach agent output; no raw DOM, screenshot, console, broad page state or protected account detail is emitted.
- [ ] A012 The 616/616 inherited baseline is observed before edits and exact new 029S target arithmetic passes without replacing retained coverage.
- [ ] A013 PowerShell parse/self-test, focused/retained tests, typecheck, zero-warning lint, Production build and mapped static/JSON/encoding/privacy/scope scans pass.
- [ ] A014 Protected scans precede content-emitting diff/inspection and emit only fixed scan metadata; scanner-clean files form the exact candidate manifest.
- [ ] A015 Fresh identity-blind preflight proves one verified expected domain, two existing sending-access keys, zero full-access keys, no target key, no raw-secret shape and no protected output.
- [ ] A016 Fresh Vercel preflight proves the exact project, zero dedicated SMTP rows, zero temporary auth rows, unchanged generic-row metadata and five aliases on accepted 036L without values.
- [ ] A017 Resend and Vercel create/Copy/paste/save/dismiss/clear controls are prevalidated before token creation through the bounded projection.
- [ ] A018 Exactly one Resend key named Precision Performance public enquiry 029S is created with sending access restricted to precisionperformance.com.au; existing keys remain unchanged.
- [ ] A019 While the token is visible no snapshot, screenshot, evaluate, log or clipboard read occurs; direct blind transfer ends in a Sensitive Production-only blank-branch pass row, then token view is dismissed and clipboard cleared.
- [ ] A020 Exactly four new Production-only blank-branch dedicated SMTP rows exist; public structural values match Resend authority and existing generic rows remain unchanged.
- [ ] A021 A new fixed 029S Credential Manager target proves absence, encrypted synthetic round trip and deletion with fixed sanitized output.
- [ ] A022 The live bearer has at least 256 random bits, is never human-entered/output/file/argument data and provisions exactly three hash/window Production rows through stdin/process API with a window at most fifteen minutes.
- [ ] A023 Exactly one Production-targeted deployment is created from exact 029S branch/workspace with automatic domains disabled; it is Ready and zero aliases.
- [ ] A024 Immutable public/privacy/protected/API/disabled-commerce checks pass without storing data or calling /api/enquiries.
- [ ] A025 Exactly one authenticated smtp-preflight returns result smtp-preflight, status ready, providerClass resend and errorClass null with no protected detail.
- [ ] A026 After expiry exactly one same-bearer request returns sanitized HTTP 404 before the SMTP handler; no second no-send attempt occurs.
- [ ] A027 The three temporary project rows and fixed local credential are removed and proved absent; retained candidate config is expired and unusable.
- [ ] A028 Evidence proves zero /api/enquiries calls, email/notification attempts, mailbox/log inspection, enquiry/bucket/Supabase/migration changes and alias changes.
- [ ] A029 On non-target result no retry occurs; only exact newly owned 029S resources are compensated and clean fallback is claimed only after complete proof.
- [ ] A030 A fresh inspector distinct from Builder and plan reviewer returns pass within three decisions after judging criteria, diff, real evidence and closeout plan.
- [ ] A031 Durable closeout records simple plain-English outcome, Flight evidence, findings/substitutions/residuals, 029R conditional state, roadmap/briefing reconciliation and STATUS sprint-closed.
- [ ] A032 Final ordered scanner-clean disk/Git/provider/Vercel/candidate/live-routing readback is consistent; no commit, push, merge, PR or history rewrite occurs.

The target proves only protected, authenticated, no-send transport readiness. It does not prove email delivery, inbox receipt, public enquiry behaviour or public activation.

============================================================
FILE: planning/sprints/029S-preflight-origin-hardening-bounded-provider-projection-and-readiness-recovery/handoff-prompt.md
============================================================

# Sprint 029S Builder Handoff

You are Builder for critical Sprint 029S in C:\Users\rrank\OneDrive\PNR Precision Performance Canonical.

First prove the canonical current directory and Git top level, exact HEAD d822c027c58ad88ec7472e35986e7a33d6a3d6c9, exact 029S branch and inherited dirty classification. Reread all four applied 029S sprint files; they are your scope.

Do not edit yet. Read the 029Q implementation and closeout, both origin validators, current tests/harness, Credential Manager pattern, safe provider projection requirements, Vercel metadata and live-routing truth. Return one exact plan with the five-part task contract, implementation file list, scope guards, A001-A032 mapping, exact commands, observed 616/616 baseline and target arithmetic, external ceilings/compensation, standard closeout actions, Pack corrections (None is valid) and blockers. A genuinely fresh reviewer must pass that exact plan before edits.

## Task contract

**objective:** Reject unsafe candidate origins, prove identity-blind provider inspection and make one least-privilege no-send readiness attempt without changing public traffic, data, existing credentials or live routing.

**owns:** New 029S origin/controller/harness/projection/tests/docs/package registration; one restricted Resend key; four dedicated SMTP rows; three temporary verifier rows; one fixed local credential target; one Production-targeted zero-alias candidate; one authenticated no-send check; one expiry denial; exact cleanup/compensation; critical reviews and closeout.

**must_not:** Expose identity/address/secret data; alter existing keys, generic SMTP, shared auth, Product/data/migration/Auth/trainer/commerce/DNS/aliases; submit or send; inspect mailbox/delivery logs; retry earlier sprints; create a second deployment; commit or publish.

**acceptance:** A001-A032, including both origin fixes, protected-canary projector proof, fixed live projections, all local gates, prevalidated blind transfer, one restricted key, four SMTP rows, three temporary rows, one Ready zero-alias candidate, one finite ready result, one post-expiry 404, exact temporary cleanup and zero public/email/data/alias effect.

**verification:** Observed 616/616 baseline plus exact new arithmetic; red-first origin/projection tests; retained suites; PowerShell parse/self-test; typecheck/lint/build/static/JSON/encoding/privacy/scope gates; scanner-clean manifest; fixed safe provider/Vercel projections; credential proof; one candidate/no-send/expiry result; cleanup; five-alias readback; fresh inspection; durable final reread.

Use evidence-proportional execution. Stop only for a material target, privacy/secret, destructive, scope, partial-provider, integrity/security, Production or cleanup boundary. Use equal or stronger safe proof when a supporting tool fails. Keep deterministic controller/browser bridge/test/validator/reporter fixes inside 029S when Product scope is unchanged. Manual intervention is last and must record the five required parts. Never request a secret through chat or arguments. Write every user-facing report in plain simple English.
