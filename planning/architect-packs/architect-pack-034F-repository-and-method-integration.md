# Architect Pack - Sprint 034F Repository and Method Integration

============================================================
FILE: planning/sprints/034F-repository-and-method-integration/requirements.md
============================================================

# Sprint 034F Requirements - Repository and Method Integration

## Objective

Establish a coherent, locally verified repository/method layer on a fresh branch from exact current `main`, without changing Product, database, provider, Production or publication state.

## Context

- Canonical repository: `C:\Users\rrank\OneDrive\PNR Precision Performance Canonical`.
- Exact base: remote `main` at `5a70b6a9876e699eac2ab44f472c361e37bc2595`.
- Branch: `codex/034F-repository-and-method-integration`.
- Continuity source: remote branch `codex/034E-repository-convergence-and-publication` at its accepted closeout lineage.
- Current `main` has no tracked 120x method or planning substrate.
- The continuity branch remains a recovery reference, not a merge recommendation.

## Required outcome

1. Bring forward the applicable direct-root 120x method substrate as an exact, hash-verified allowlist rather than replaying historical commits wholesale.
2. Install canonical workspace guards and the ordinary direct-root Architect, Builder, Fly, Fly Lean and Onboard entry points.
3. Preserve current `main` Product/runtime/database/dependency content byte-for-byte.
4. Create only the minimum current planning/briefing/review substrate needed to govern this slice; do not import historical planning or evidence.
5. Merge `.gitignore` semantically: preserve current-main exclusions, add applicable repository hygiene, keep the exact tracked method command paths visible, and leave `delivery_road_map.docx` untouched and untracked.
6. Add one focused current-interface method integration test. Do not import the historical stale repository-baseline test.
7. Record exact checks, diff scope, source hashes, invariance and next-slice boundaries.

## Scope

Builder may change only:

- `.120x/method-manifest.json`;
- exact 120x entries under `.agents/skills/` and `.claude/commands/`;
- `AGENTS.md` and the current root `architect-chat-starter-prompt.md`;
- method files under `templates/`;
- `scripts/apply-architect-pack.js`, `scripts/update-method.js`, and one focused `scripts/test-method-integration-034F.mjs`;
- the semantic `.gitignore` merge;
- minimum current-only records under `planning/` and `docs/` required by this sprint, including its Pack, applied sprint files, review, state, decisions, risks, questions, domain, briefing, status, workflow profile and briefing specification.

## Out of scope

- All files under `app/`, `components/`, `content/`, `lib/`, `public/`, `supabase/` and `evidence/`.
- `package.json`, lock files, dependencies and runtime configuration.
- Product behavior, database/schema/migrations, RLS/Auth, provider access, credentials, data, trainer actions, Production, deployment or aliases.
- Historical planning/evidence import or acceptance-matrix reconciliation; that belongs to Sprint 034I.
- Commit, push, pull request, merge, rebase, cherry-pick, force update or remote branch creation.
- Reading, hashing, parsing, staging, ignoring, moving or deleting `delivery_road_map.docx`.
- Inspecting or changing unrelated untracked `.claude`, `.codex_work`, `supabase/.branches` or `supabase/.temp` content.

## Evidence-Proportional execution

- Stop only for a material target, authority, security, privacy, destructive, integrity, scope or cleanup risk.
- Substitute equivalent or stronger safe evidence when a preferred supporting tool is unavailable, and record the substitution.
- Keep deterministic method, importer, validator, formatting, encoding, reporter and harness corrections inside this sprint when the approved outcome is unchanged.
- Do not create a follow-up solely because a supporting tool, renderer, optional CLI or redundant verification path is unavailable.
- Use manual intervention only after safe in-scope alternatives are exhausted. If it becomes necessary, record what is blocked, evidence checked, exact operator steps and the post-action verification.

============================================================
FILE: planning/sprints/034F-repository-and-method-integration/blueprint.md
============================================================

# Sprint 034F Blueprint - Repository and Method Integration

## Implementation design

### 1. Establish exact source and invariants

- Recheck canonical cwd/Git root and exact branch/base before every mutation phase.
- Read the continuity tree through Git objects only.
- Build a traversal-free allowlist of the complete direct-root method substrate required by its manifest.
- Verify every source blob/content SHA before materialising it.
- Capture pre/post hashes for Product/runtime, database, dependency and configuration surfaces to prove invariance.

### 2. Materialise a coherent method substrate

- Bring forward the manifest, direct-root Codex skills, Claude command bodies, method templates, prompt templates, Pack importer and updater as one internally coherent set.
- Preserve the accepted direct-root `new-project` delivery-layout mode as a tooling-layout label; do not reinterpret it as Product age or scope.
- Add canonical `AGENTS.md`, a current root `architect-chat-starter-prompt.md`, and the minimum current planning/briefing files needed to operate the method on this branch.
- Do not import historical sprint directories, reviews, evidence or Product acceptance claims beyond this current flight's records.

### 3. Merge repository hygiene semantically

- Preserve every current-main ignore rule unless a concrete conflict requires correction.
- Add applicable `.validation-logs`, `.logs`, Supabase temp/branch, desktop and legacy release exclusions.
- Ignore unrelated `.claude` content while explicitly unignoring only the exact tracked method command files.
- Do not add an ignore rule for the excluded DOCX and do not inspect its content.

### 4. Prove behavior

- Add one dependency-free Node test covering Pack dry-run/application isolation, traversal/protected-target refusal, updater import without network, same-version local-correction retention, manifest/content-hash agreement, coherent class-scaled launch authority, safe direct-root behavior and unsafe-path/workspace refusal.
- Run syntax checks, the focused test, JSON parsing, manifest agreement, `git diff --check`, exact scope classification and pre/post Product/database/dependency invariance.
- Do not rerun a Product build or Product suites because this slice cannot reach Product/runtime/package content.

### 5. Close proportionally

- Have an independent Architect context inspect the full diff and real check evidence against every acceptance criterion.
- After pass, mark only 034F done, refresh current-only state/briefing/review/status and reread all closeout files and the final diff.
- Leave publication and later slices planned, not implied.

## Flight evidence

1. **Class and reason:** `cross-layer`; the authority flows through manifest, updater, templates, launchers, repository guard and executable validation, while no critical Product/auth/data/payment/Production or irreversible external action is included.
2. **Acceptance invariant at risk:** current `main` gains a self-consistent, fail-closed method layer without any Product/runtime/database/dependency change or historical evidence being mistaken for integration.
3. **Affected layers and verified paths/symbols:** manifest file allowlist and SHA-256 values; `update-method.js` path/mode/placement functions; Architect/Builder/Fly launcher bodies; `AGENTS.md` canonical guard; Pack importer path validation; focused test and current-only planning sinks.
4. **Source, transformations and sink:** the accepted 29-entry continuity manifest is the base. Final classification is 17 unchanged Git blobs, six Git-derived local transformations, four unchanged manifest-bound source-absent Claude launchers, and two transformed source-absent Claude launchers. The manifest records the exact eight-file v18 transformation delta in `localCorrections`; semantic `.gitignore`/current-planning adaptation completes the transformation; the fresh current-main branch and its executable method/test surface are the sink.
5. **Discriminating examples:** a valid relative sprint path is reported/applied inside the project while `../outside` and protected `.gitignore` targets fail; a valid direct-root method set passes manifest hashes while an omitted/changed method file fails; exact method command paths remain visible while unrelated `.claude` content stays ignored.
6. **Durable verification source:** exact base SHA, continuity Git object IDs/content hashes, final Git diff and actual command output.
7. **Known uncertainty:** the discovery-stage stale-test concern exists only on the continuity lineage; current `main` contains no such test. The sprint therefore creates one focused current-interface test and records that correction rather than importing stale machinery.

============================================================
FILE: planning/sprints/034F-repository-and-method-integration/acceptance.md
============================================================

# Sprint 034F Acceptance - Repository and Method Integration

| ID | Acceptance criterion | Required evidence |
| --- | --- | --- |
| AC-01 | Cwd and Git root equal the permanent canonical path; branch and base equal the approved 034F values. | Exact command output. |
| AC-02 | Pack dry-run reports exactly four traversal-free destinations under one 034F sprint folder; application creates exactly those files. | Importer output and reread. |
| AC-03 | The 29-path direct-root allowlist starts from the accepted continuity manifest; the final manifest records exactly eight bounded current-v18 local transformations, and its `localCorrections` set equals the exact upstream-to-current hash delta. | Machine-readable source-classification and hash-delta test. |
| AC-04 | Manifest entries resolve to present files and every SHA-256 matches. | Focused test, zero mismatches. |
| AC-05 | Architect, Builder, Fly, Fly Lean and Onboard launchers plus `AGENTS.md` form a coherent direct-root operating path. | Static assertions and targeted reread. |
| AC-06 | Pack importer permits safe in-root sprint targets and refuses traversal, absolute and protected targets without outside writes. | Red/green behavioral assertions. |
| AC-07 | Updater imports without network, validates allowlists/placement, and refuses unsafe workspace/path cases. | Red/green behavioral assertions. |
| AC-08 | `.gitignore` preserves current-main rules, adds approved hygiene and exposes only the exact tracked `.claude` method commands. | Exact diff and `git check-ignore` assertions. |
| AC-09 | Product/runtime, database, package/lock and deployment configuration surfaces are byte-identical to the opening base. | Pre/post tree/hash comparison. |
| AC-10 | No provider, credential, data, trainer, Production, deployment, alias or remote write occurs. | Action ledger and Git/external invariance. |
| AC-11 | Historical planning/evidence is not imported; current planning contains only the integration road and 034F current-flight records. | Path allowlist inspection. |
| AC-12 | The excluded DOCX remains untouched, untracked and absent from index/diff without content access. | Metadata-free Git path checks only. |
| AC-13 | Syntax, focused behavior, JSON, manifest, whitespace and scope checks pass with exact counts/results. | Actual command/exit-code ledger. |
| AC-14 | A context that did not write the code independently inspects the full diff/evidence and passes every criterion or returns bounded fixes. | Independent review decision. |
| AC-15 | Closeout marks only 034F done, leaves 034G-I planned, records Product Done false/no Product change, sets STATUS `sprint-closed`, and final reread proves consistency. | Roadmap, briefing, state, review, status and final diff reread. |

## Verification commands

Builder must run the exact applicable equivalents of:

```powershell
$pwd.Path
git rev-parse --show-toplevel
git branch --show-current
git rev-parse HEAD
node --check scripts/apply-architect-pack.js
node --check scripts/update-method.js
node --check scripts/test-method-integration-034F.mjs
node scripts/test-method-integration-034F.mjs
npm run validate:json
git diff --check
git status --short --branch --untracked-files=all
```

The focused test must additionally perform exact manifest-hash, ignore-boundary, scope-invariance and unsafe-path assertions. A Product build is intentionally not required because the approved file set cannot affect Product/runtime/package content.

============================================================
FILE: planning/sprints/034F-repository-and-method-integration/handoff-prompt.md
============================================================

# Builder Handoff - Sprint 034F Repository and Method Integration

**objective:** Establish a coherent, fail-closed repository/method layer on the exact current-main branch with no Product, database, provider, Production or publication change.

**owns:** The exact method/launcher/template/importer/updater allowlist; canonical `AGENTS.md`; the current root `architect-chat-starter-prompt.md`; semantic `.gitignore`; one focused 034F test; and minimum current-only planning/docs records enumerated by the sprint requirements.

**must_not:** Touch Product/runtime/database/dependency/deployment surfaces; import historical planning/evidence; inspect the excluded DOCX or unrelated untracked content; access providers or credentials; stage unrelated files; commit, push, open a PR, merge, rebase or change any remote ref.

**acceptance:** AC-01 through AC-15 in `acceptance.md` pass with real output, exact invariance, independent cross-layer diff review and complete current-only closeout.

**verification:** Run the canonical/branch checks, three Node syntax checks, focused method test, JSON validation, manifest/hash and ignore/scope assertions, `git diff --check`, exact status/scope proof, independent diff review and final closeout reread.

## Builder instructions

1. Recheck canonical cwd/Git root, branch, base and unrelated untracked exclusions before implementation.
2. Work only from the applied sprint files and exact source Git objects; do not cherry-pick or replay historical commits wholesale.
3. Preserve unrelated work and stop for any unexpected collision with an owned path.
4. Use the Evidence-Proportional Execution Standard in `requirements.md`; diagnose once and use an equivalent or stronger safe proof when a supporting tool fails.
5. Keep deterministic importer, updater, validator, formatting, encoding, reporter and test corrections inside 034F when they do not change the approved outcome.
6. If manual intervention becomes genuinely necessary, record the blocker, evidence checked, exact step-by-step action and what will be verified afterward.
7. Do not commit or publish. Finish only after independent review passes and all current-only closeout records are reread from disk.
