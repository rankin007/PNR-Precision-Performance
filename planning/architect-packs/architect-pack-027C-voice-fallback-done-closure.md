# Architect Pack — Sprint 027C Voice/Fallback Done Closure

Created: 2026-08-11

This Pack closes the current-MVP voice/fallback acceptance boundary from the already approved and implemented Sprint 027 typed/device-keyboard contract. It does not authorize application-controlled audio, transcription, a provider, Product-source changes, schema changes, deployment or external action.

============================================================
FILE: planning/sprints/027C-voice-fallback-done-closure/SPRINT.md
============================================================

# Sprint 027C — Voice/Fallback Done Closure

## Outcome

Close the current-MVP notes fallback honestly from the retained canonical Product. Ordinary typed notes are the accepted launch fallback. A user may optionally use device-keyboard dictation into the same textarea, but Precision Performance does not request microphone permission, handle audio or use a transcription provider. Every non-empty note remains editable and requires review before submission.

This is an evidence, acceptance and planning-reconciliation sprint. Product implementation already exists from Sprint 027 and was retained through Sprint 027B and later Product work. No implementation-file edit is expected or authorized.

Successful outcome: `voice-fallback-done-closure-complete-clean`.

## Workflow profile

`standard`, with the existing strict privacy and no-audio boundary retained. The sprint uses synthetic local evidence only and makes no remote, Production or provider change.

## Governing authority

Builder must preserve, not reinterpret:

- `planning/DECISIONS.md` decisions dated 2026-07-29: typed notes are the permanent fallback; device-keyboard dictation is optional; application microphone/provider capability is unavailable; substantive notes require review; notes never populate structured fields.
- `docs/VOICE_ASSISTED_CAPTURE_027.md` as the canonical Q&A contract.
- `planning/reviews/027-voice-assisted-capture-closeout.md` and `planning/reviews/027B-product-lineage-reconciliation-closeout.md` as qualified historical evidence.
- `docs/DESIGN_AND_MESSAGING_AUTHORITY.md` for authenticated mobile, privacy, accessibility and no-architecture-expansion boundaries.
- Sprint 035R's retained horse-aware capture, note-review and repeat-tap safeguards.

The current canonical repository wins over historical hashes or prose. Later legitimate changes may alter file hashes without altering the accepted behavior. Reconcile semantics freshly.

## Task contract

### objective

Prove the current canonical typed-note/manual fallback and optional device-keyboard dictation boundary, capture fresh synthetic rendered evidence, and reconcile current-MVP Done and acceptance records without changing Product behavior.

### owns

Builder may create or edit only:

- `planning/sprints/027C-voice-fallback-done-closure/SPRINT.md` as generated by this Pack;
- `planning/reviews/027C-voice-fallback-done-closure.md`;
- `evidence/professional-engineering/027C-voice-fallback-done-closure/**` using synthetic, non-private evidence only;
- `planning/STATE.md`;
- `planning/STATUS.json`;
- `planning/DECISIONS.md`, only for the 027C closure outcome;
- `planning/RISKS.md` and `planning/QUESTIONS.md`, only to reconcile the retained voice/fallback boundary;
- `planning/FINAL_PRODUCT_ACCEPTANCE_MATRIX.md`;
- `planning/DEFINITION_OF_DONE.md`;
- `planning/ROADMAP.md`;
- `planning/SPRINT_SCHEDULE.md`;
- `planning/SPRINT_LIFECYCLE_LEDGER.md`;
- `planning/EVIDENCE_INDEX.md`;
- `planning/ARCHITECT_BRIEFING.md`; and
- `delivery_road_map.md`.

The existing Sprint 035R local evidence harness may be generated and served transiently under `.codex_work/` only. It is not a deliverable and must be removed safely after capture if this sprint creates it.

### must_not

Builder must not:

- edit `app/**`, `components/**`, `lib/**`, `scripts/**`, `package.json`, `package-lock.json`, configuration, migrations, Supabase, RLS or any other implementation/test file;
- add or call `getUserMedia`, `MediaRecorder`, `SpeechRecognition`, `webkitSpeechRecognition`, an audio API, provider, model, SDK, endpoint, secret or dependency;
- record, upload, store, transmit, replay or request real or synthetic audio;
- claim device dictation is on-device, private, offline, secure, accurate, always available or retained nowhere;
- treat device dictation as application functionality or require it for current-MVP acceptance;
- parse note text into horse identity, date/time, readings, scores, classifications, recommendations or another structured field;
- use real horse, trainer, customer, stable, veterinary, personal or confidential data;
- submit a test, mutate local/remote data, apply migrations, deploy, change aliases/domains, create credentials, send email or submit enquiries;
- stage, commit, push, merge or open a pull request; or
- move Sprint 023Q/private photos/PDFs back into the current MVP.

If a material Product-source defect is found, stop `voice-fallback-authority-drift-blocked-clean`; do not repair it under this no-Product-change sprint. The next valid corrective suffix would require a new Architect Pack.

### acceptance

All acceptance criteria below must pass or the sprint must close with a truthful blocked-clean outcome.

### verification

Run the exact maintained commands and evidence steps under `## Verification`. Equivalent or stronger safe proof may replace a supporting tool, but not the acceptance fact.

## Required work

1. Verify current working directory and Git top both equal `C:\Users\rrank\OneDrive\PNR Precision Performance Canonical`. Record HEAD, staged state and the pre-existing dirty file set. Preserve unrelated user work.
2. Apply this Pack only after its dry-run passes. Reread the generated `SPRINT.md` and execute from it, not from the Pack.
3. Record baseline SHA-256 hashes for the read-only Product/contract proof set:
   - `components/ops/biochemistry-capture-workflow.tsx`;
   - `components/ops/biochemistry-workflow-state.ts`;
   - `app/(ops)/data-entry/biochemistry/actions.ts`;
   - `scripts/test-biochemistry-voice-027.mjs`;
   - `scripts/test-trainer-cockpit-035R.mjs`;
   - `docs/VOICE_ASSISTED_CAPTURE_027.md`;
   - `package.json`; and
   - `package-lock.json`.
4. Freshly inspect the canonical source and prove:
   - typed entry is always available in one textarea;
   - device-keyboard dictation is optional and accurately bounded;
   - the application records/stores no audio and exposes no microphone/provider control;
   - non-empty notes require initially unchecked review confirmation;
   - editing invalidates confirmation;
   - empty notes do not require confirmation;
   - notes persist only through the established manual-note path;
   - notes never populate structured test fields; and
   - server access, horse boundary and 2,000-character validation remain authoritative.
5. Run the maintained focused and retained proof. The focused Sprint 027 script executes 34 assertions: 29 assertion call sites plus five additional executions from its two-case note-preservation loop. Sprint 035R contributes 64/64 retained assertions. The required counted baseline is therefore `34 + 64 = 98` assertions, plus typecheck/lint/JSON/build gates.
6. Generate and serve the existing Sprint 035R synthetic local evidence harness. Use only the supplied synthetic horse/readings and a short synthetic note. Do not submit.
7. Capture fresh rendered evidence at a `414 × 896` viewport:
   - capture view showing typed-notes availability, device-keyboard guidance and editable textarea;
   - review view showing the exact synthetic note and initially unchecked review confirmation; and
   - blocked-review view after an attempted submit without confirmation, showing the accessible correction message and no submission.
8. Capture one `1440 × 900` rendered view proving the notes guidance/textarea remains readable without unintended horizontal overflow. Record viewport/client/scroll widths and applicable touch-target geometry.
9. Do not require an actual microphone or actual device dictation. The accepted current-MVP fact is the clear, always-available typed fallback plus optional device-controlled text entry, not application transcription reliability.
10. Reconcile durable acceptance truth:
   - P03 remains `deferred-by-approved-scope` for application voice-to-text; its current-MVP fallback limitation becomes explicit and evidence-backed.
   - O02 becomes `passed-with-accepted-limitation` because an always-available typed fallback, optional device-keyboard entry and mandatory review are proven; application audio/transcription remains unavailable.
   - P47 remains `not-proven` for its other full-contract gaps; its voice portion is explicitly deferred/closed by the accepted fallback rather than left ambiguous.
   - `planning/DEFINITION_OF_DONE.md` must state that current-MVP notes are typed editable text with optional device-keyboard dictation and mandatory review; application-controlled voice/transcription is future separately approved scope.
11. Record the exact visual manifest, commands, results, assertion arithmetic, limitations, read-only hash match, file changes and `0/0/0` staged/external/residue counts.
12. Recompute the remaining current-MVP estimate and advance the next outcome to Sprint 021AI only if every 027C criterion passes.
13. End the Builder report with a plain-language user-action section. If no manual action is genuinely required, write exactly: `I need nothing from you.`

## Acceptance criteria

| ID | Criterion | Required evidence |
|---|---|---|
| AC-01 | Canonical CWD/Git top match exactly; pre-existing dirt is recorded and preserved. | Commands and scoped before/after status. |
| AC-02 | The applied Sprint file is byte-derived from this Pack and reread before execution. | Dry-run/apply output and generated-file hash. |
| AC-03 | The 2026-07-29 decisions and `VOICE_ASSISTED_CAPTURE_027.md` remain the controlling authority. | Fresh authority trace. |
| AC-04 | Product/contract proof-set hashes are identical before and after 027C. | SHA-256 manifest. |
| AC-05 | Typed notes are always available in one ordinary editable textarea. | Focused assertions plus rendered mobile/desktop evidence. |
| AC-06 | Device-keyboard dictation is optional, device-controlled and not overclaimed. | Exact copy assertion and rendered evidence. |
| AC-07 | No application microphone, audio, recorder, recognition API, provider, endpoint, SDK, secret or dependency exists in the scoped path. | Static scan plus dependency/source inspection. |
| AC-08 | Non-empty notes require initially unchecked review; unconfirmed submit is blocked. | Focused assertions and blocked-review render. |
| AC-09 | Editing invalidates confirmation; empty notes remain unaffected. | Focused executable proof. |
| AC-10 | Exact note text is reviewed and remains editable before submit. | Focused assertions and review render. |
| AC-11 | Notes stay plain text and never alter structured values. | Two-case synthetic non-extraction proof. |
| AC-12 | Manual-note persistence, authenticated horse access and 2,000-character server/client boundaries remain intact. | Source trace and boundary assertions. |
| AC-13 | No real/private data or audio is used or emitted. | Evidence inspection and manifest. |
| AC-14 | Mobile capture, review and blocked-review views are complete at 414 × 896. | Three exact PNGs plus geometry. |
| AC-15 | Desktop notes view is complete at 1440 × 900 without unintended horizontal overflow. | Exact PNG plus geometry. |
| AC-16 | Labels, descriptions, confirmation, error announcement, focus and 44px-class targets remain accessible. | Source trace and rendered/browser geometry. |
| AC-17 | Focused/retained counted proof passes exactly `34 + 64 = 98`. | Command output and arithmetic ledger. |
| AC-18 | Typecheck, zero-warning lint, JSON and optimized build pass, or a documented equivalent safe supporting-tool substitution proves the same fact. | Exact command ledger. |
| AC-19 | P03, O02 and the voice portion of P47 are reconciled exactly as scoped; no provider capability is claimed. | Acceptance-matrix diff. |
| AC-20 | Done, State, Status, Roadmap, schedule, briefing, lifecycle ledger and delivery roadmap agree. | Cross-file consistency scan. |
| AC-21 | Sprint 023Q/private photos/PDFs remain in MVP 2. | Planning scan. |
| AC-22 | No Product/test/package/schema/configuration file changes. | Approved-path and hash proof. |
| AC-23 | No submission, data mutation, migration, provider/configuration, deployment, credential, email, enquiry, alias/domain or Git-publication action occurs. | External-effects ledger `0`. |
| AC-24 | Final staged/external/generated-residue counts are `0/0/0`. | Final safety checks. |
| AC-25 | Closeout report ends with an explicit user-action statement. | Report inspection. |

## Verification

Run from the canonical root:

```powershell
npm run test:voice-027
npm run test:cockpit-035r
npm run validate:json
npm run typecheck
npm run lint
npm run build
git diff --check
```

Run a privacy/static refusal scan over the current capture/action path. The command must return zero forbidden implementation matches after excluding explanatory user-facing text and historical planning/evidence files. Inspect matches manually; do not treat a broad raw substring count as proof.

Start the existing local-only evidence harness:

```powershell
npm run evidence:cockpit-035r
```

Before and after serving, prove port `3135` listener count and `agent-browser` process count return to zero. If the harness creates `.codex_work/evidence-035r`, verify the resolved path remains inside canonical `.codex_work`, remove only that generated directory after capture, and prove it absent. Do not delete any other `.codex_work` content.

The optimized build is a required Product-integrity fact. If OneDrive/cache behavior blocks the preferred command after one diagnosed attempt, use an equivalent clean local build mechanism already accepted by the repository, record the original failure and substitution, and do not weaken Product, privacy or scope assertions.

## Evidence-Proportional Execution Standard

Builder must stop only for a material target, authority, security, privacy, migration, destructive, integrity, Production, scope or cleanup risk. Substitute equivalent or stronger safe evidence when a preferred supporting tool is unavailable. Keep in-scope harness, validator, formatting, encoding, reporter and deterministic evidence corrections inside 027C. Do not create a follow-up sprint solely because browser automation, a renderer, clipboard control, a schema dump or another supporting tool is unavailable. Use manual intervention only after safe in-scope alternatives are exhausted.

If manual intervention is genuinely required, record:

- what is blocked;
- evidence already checked;
- the exact user action needed;
- step-by-step instructions; and
- what Builder will verify afterward.

Do not ask the user to dictate, supply a real note, enable a microphone, expose private content or perform a Product submission for this sprint.

## Acceptable outcomes

- `voice-fallback-done-closure-complete-clean` — every criterion passes; O02 is accepted with the exact limitation; P03/provider voice remains deferred; current MVP advances to 021AI.
- `voice-fallback-authority-drift-blocked-clean` — current Product behavior materially contradicts the accepted no-audio/manual-fallback contract; no Product repair is attempted.
- `voice-fallback-evidence-blocked-clean` — required Product/privacy/rendered evidence cannot be established by a safe equivalent method; claims remain unchanged.

## Builder handoff

Apply this Pack with the standard dry-run-first command, verify the one generated Sprint file, and execute only from that file. This sprint is explicitly no-Product-change. Use the current canonical Product and existing synthetic harness to prove the already accepted fallback, then reconcile the current-MVP records and close cleanly. Do not stage, commit, push, deploy or perform external actions.
