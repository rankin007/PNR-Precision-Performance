============================================================
FILE: planning/sprints/029W-vercel-inventory-projection-and-final-readiness-attempt/requirements.md
============================================================

# Sprint 029W Requirements — Privacy-Safe Vercel Inventory Projection and Final Readiness Attempt

## Outcome

Deliver exactly one terminal result:

- `final-readiness-recovered-clean`: the isolated inventory projection passes, automatic custom-domain assignment is persistently false, one exact-source Ready Production 029W candidate remains unaliased, the disabled public gate and dedicated SMTP verify-only sequence pass, and public submission remains disabled; or
- `public-enquiry-parked-clean`: the first material gate fails without unsafe residue, every safely compensable 029W-owned resource is removed, accepted Sprint 036L remains five/five, and public enquiry is explicitly parked. No further 029 transport-recovery suffix is planned; the road advances to 025B then 035R; or
- `final-readiness-blocked-material`: setting, routing, deployment ownership, active-queue, credential, environment, candidate or cleanup state cannot be proved safe. Preserve the safest proven state, perform only independently safe non-routing compensation, record exact sanitized residue and stop for fresh human authority.

Sprint 029W never activates public enquiry, sends email or submits/stores an enquiry.

## Canonical and dirty-work boundary

- Before every mutation phase, current directory and Git root must both equal `C:\Users\rrank\OneDrive\PNR Precision Performance Canonical`.
- HEAD starts and remains `d822c027c58ad88ec7472e35986e7a33d6a3d6c9`; staged count remains zero; no commit is created.
- Create/switch to `codex/029W-vercel-inventory-projection-and-final-readiness-attempt` while preserving the complete user-owned dirty workspace.
- Reconcile pre/post branch tracked and untracked name/SHA-256 manifests. Do not stash, reset, clean, copy from a legacy clone, use `C:\tmp`, create a worktree or normalize unrelated files.

## Verified discovery truth

- Installed Vercel CLI is exactly 50.42.0. The installed list-command source SHA-256 is `8376a6d957c6fe20a9a1d4738000eb60a519bd079c70f0a82c9e7b59ba9ee367`.
- One bounded read-only discovery paged nine pages and 168 deployment rows. It emitted field names, types and counts only; raw value output count was zero.
- The one top-level shape is `contextName:string`, `deployments:array`, `pagination:object`.
- Pagination has `count:number`, `next:number|null`, `prev:number`.
- The three deployment shapes contain `url`, `name`, `state`, `target`, `createdAt`, `ready`, optional `buildingAt`, plus protected `creator` and `meta` objects. All 168 rows contained those protected containers. `target` is string or null.
- Live rows omit `id`. Installed CLI source constructs `id: dep.id`; the current API leaves that member undefined and JSON serialization omits it. The same source always emits `url` from `dep.url`.
- Sprint 029V correctly failed closed before dashboard access or mutation. Its accepted local proof is 110/110; accepted Sprint 036L remains Ready and five/five; zero 029V resources exist.

## Five-field task contract

1. Objective: isolate raw Vercel list JSON behind an exact value-free child projector, prove stable full/owned inventory and candidate ownership from deployment hostname to independently inspected canonical ID, then perform the single final guarded no-send readiness attempt.
2. Inputs: this Pack and generated files; Sprint 029V closeout/controller/tests; the verified discovery facts; installed CLI 50.42.0 source contract; canonical Git state.
3. Outputs: narrow 029W projector/controller/tests/doc changes, exact critical reviews, proportional evidence, one terminal outcome and fully reconciled closeout.
4. Permitted external mutations after every prior gate passes: one value-free dashboard transition of Production automatic custom-domain assignment from true to persistently false; one restricted Resend sending key; four dedicated and three temporary Sensitive Production-only rows; one Production-targeted `--skip-domain` candidate with `pp_sprint=029W`; exact independently owned cleanup.
5. Stop conditions: wrong workspace/HEAD/manifest; protected or secret output; raw list JSON reaching the controller/evidence; projector/source/version drift; unstable or active pre-freeze queue; inability to prove five/five routing; more than one transfer/deploy/gate/readiness/expiry attempt; alias movement; email/enquiry/data/migration/generic-SMTP/activation effect; unsafe cleanup or scope expansion.

## Isolated inventory contract

- Add a new child projector; do not weaken `scripts/PreflightAuth029V.ps1` or rewrite closed 029V evidence.
- The child alone may capture raw stdout from exactly `vercel list --format json --no-color`, the same vector plus a decimal `--next`, or the exact owned variants with `--meta pp_sprint=029W`. No other command/flag is permitted.
- The controller must receive only fixed-code failure or sanitized JSON containing exact projection version, rows of deployment hostname/state/target/created timestamp, and bounded pagination. It must never receive raw `contextName`, project name, `creator`, `meta`, or any nested protected content.
- The projector validates the exact top-level fields. Each row permits only source-declared `id`, `url`, `name`, `state`, `target`, `customEnvironment`, `createdAt`, `buildingAt`, `ready`, `creator`, and `meta`. Unknown fields fail closed.
- `creator` and `meta` may be object, null or absent as the CLI source permits. Their properties and values are never enumerated, copied, interpolated, logged, serialized, placed in an error or returned. Sentinel tests must prove zero leakage on pass and refusal paths.
- `id`, when present, must be an exact deployment-ID shape but is not required and is not the inventory key. `url` is required, must be a bare lowercase HTTPS-compatible hostname with no scheme, credentials, port, path, query or fragment, and becomes the in-memory inventory key. `name` is validated as a non-empty string but never returned.
- State is restricted to the accepted closed/active set; target is exact production, preview or null-as-preview; non-null custom environment fails closed; timestamps are bounded integers.
- Pagination is exact, page count equals row count, cursors are null or non-negative integers, pages are limited to 10 and rows to 200, duplicate hostnames and loops fail closed, and first-page head revalidation permits one complete restart only.
- Full snapshots use hostname/state/target/created time only. Owned reconciliation uses the server-side `pp_sprint=029W` filter, then independently inspects the one returned hostname. Only exact inspect ID, metadata, source SHA, Production, Ready and zero-alias proof may establish ownership or authorize cleanup.
- An unknown/missing list ID can never be guessed. Cleanup uses only the independently inspected canonical ID.

## Retained final-attempt boundary

- Reuse the accepted 029V agent-envelope, durable phase/counter, per-request origin-binding, provider projection and alias-isolation controls without product redesign.
- Pin both CLI version and installed list-source hash before the first external gate; drift stops clean before dashboard access.
- Before credentials, prove exact project, accepted Sprint 036L five/five, zero 029W resource, stable full inventory and zero active `BUILDING`, `INITIALIZING` or `QUEUED` deployment.
- Change only the exact Production Branch Tracking `Auto-assign Custom Production Domains` toggle from true to false through the already accepted value-free autosave/manual-save state machine. Hard reload must prove persistent false; stable inventories across transition must show no new deployment and routing remains five/five.
- Once false, never turn assignment true in Sprint 029W. Re-enabling remains a separately approved promotion action.
- Only after local proof, critical inspection, stable empty queue, persistent false and identity-blind baselines may the operator privately create one Resend key named `Precision Performance public enquiry 029W`, restricted to sending for `precisionperformance.com.au`, and paste it directly into the prepared Sensitive Production-only password field without agent observation. The operator reports only `transfer-complete` or `transfer-failed`.
- Add exact dedicated host/port/user plus password rows and three temporary preflight rows through stdin-only vectors. Generic SMTP and activation rows remain untouched.
- Deploy at most once with `--prod --skip-domain --meta pp_sprint=029W`. Exact agent-envelope parsing remains mandatory; independent hostname-to-inspect ownership is authoritative.
- Only an exact-source, exact-metadata, Production, Ready, zero-alias candidate with unchanged five/five routing may receive one disabled public-gate probe, one authenticated SMTP verify-only request and one same-bearer post-expiry denial. No `sendMail` call is permitted.
- Remove temporary rows before close. Success retains only the exact restricted key, four dedicated rows and unaliased candidate. Clean fallback removes every independently safe owned resource. Alias/domain/promote/rollback commands are forbidden.
- Any clean non-ready result parks the public-enquiry channel. Do not recommend or create another 029 transport-readiness recovery sprint; set the next delivery direction to 025B then 035R.

## Approved files

- `scripts/PreflightAuth029V.ps1` read-only source
- `scripts/PreflightAuth029W.ps1`
- `scripts/autonomous-public-enquiry-029V.mjs` read-only retained source
- `scripts/provider-browser-projection-029V.mjs` and its test read-only retained sources
- `scripts/vercel-alias-isolation-projection-029V.mjs` and its test read-only retained sources
- `scripts/vercel-deployment-inventory-projection-029W.mjs`
- `scripts/test-vercel-deployment-inventory-projection-029W.mjs`
- `scripts/test-autonomous-public-enquiry-029W.mjs`
- `package.json` only for exact 029W test commands
- `docs/PUBLIC_ENQUIRY_FINAL_READINESS_ATTEMPT_029W.md`
- `planning/sprints/029W-vercel-inventory-projection-and-final-readiness-attempt/**`
- `evidence/professional-engineering/029W-vercel-inventory-projection-and-final-readiness-attempt/**`
- standard state/status/decision/question/risk/roadmap/schedule/lifecycle/evidence/matrix/briefing/archive closeout files and `delivery_road_map.md`

Every product route, enquiry runtime file, schema, migration, generic SMTP row, activation row, closed sprint file and other external resource is out of scope.

## Evidence and closeout

- Retain the Evidence-Proportional Execution Standard: stop only for a material boundary; use equal or stronger safe substitute proof for supporting-tool failure; keep deterministic projector/controller/test corrections in this sprint; use manual intervention only for unavoidable private secret transfer.
- Run exact 029W projector/controller tests plus retained 029V suites, PowerShell parse/SelfTest, typecheck, zero-warning lint, Production build, JSON/static checks, protected scan and exact manifest proof. Record actual arithmetic; do not claim the historical 1004 as newly rerun unless it was.
- Critical plan review and critical inspection each have at most three sequential decisions and stable finding ledgers.
- Closeout records discovery, projection boundary, setting transitions, aliases, deployment/request counts, resource residue, compensation, tests, reviews and Final Product Acceptance Matrix changes.
- Archive boot files under the existing archive law, set STATUS to `sprint-closed`, read every closeout back and leave staged count zero.
- Do not commit, push, open a pull request, activate public enquiry or move aliases.

============================================================
FILE: planning/sprints/029W-vercel-inventory-projection-and-final-readiness-attempt/blueprint.md
============================================================

# Sprint 029W Blueprint — Privacy-Safe Vercel Inventory Projection and Final Readiness Attempt

## Execution sequence

1. Re-prove canonical workspace, fixed HEAD, staged count and complete dirty-work manifests; create/switch to the exact 029W branch without losing user work.
2. Apply and reread all four generated files plus Sprint 029V closeout. Produce the exact no-edit critical Builder plan with file list, criteria, baselines, commands, closeout and Pack corrections.
3. Obtain sequential fresh critical plan review. Proceed only on PASS within the three-decision budget.
4. Implement the isolated list projector, its adversarial fixtures, the 029W controller integration and focused controller tests. Do not edit Product/runtime or closed sprint files.
5. Prove the projector with the three observed live shapes, missing-ID/hostname identity, protected sentinel non-leakage, unknown-field refusal, invalid/duplicate host refusal, pagination, loops, bounds, active-state classification and owned hostname-to-inspect reconciliation.
6. Run all local and retained gates. Obtain sequential fresh critical inspection and correct only through the required re-plan/review path.
7. Re-prove CLI 50.42.0 and list-source hash. Run the isolated full inventory, exact alias and resource Baseline. Stop clean and park if the queue is active/unstable or routing/resource baselines fail without residue.
8. Project the exact dashboard state. If true, transition once to false through the accepted value-free branch; hard-reload and prove persistent false, identical inventory and five/five routing. If already false, accept only exact persisted-false proof and record zero transition.
9. After all pre-secret gates, prepare the Sensitive password surface and pause observation. Give the operator the exact private transfer steps. Resume only on `transfer-complete` or `transfer-failed`.
10. On transfer-complete, prove metadata only, add exact structural/temporary rows, and re-prove persistent false, stable empty queue and five/five routing.
11. Take the pre-deploy inventory, invoke one exact deployment, independently reconcile owned hostname, inspect to canonical ID and require exact source/metadata/Production/Ready/zero aliases. Any direct-response failure still reconciles without retry.
12. Only on full candidate acceptance, perform one disabled public gate, one verify-only readiness request and one expiry denial, with setting/routing/ownership checks before each.
13. Remove temporary rows. Retain success resources only on full readiness; otherwise compensate every independently safe 029W-owned resource without alias action.
14. Close one exact terminal outcome. On any clean non-ready outcome, park public enquiry and advance roadmap direction to 025B then 035R. Run final proof, archive, reconcile and reread without staging or commit.

## Flight evidence

1. **Class and reason:** `critical`. The local correction mediates raw provider deployment metadata and gates credentials, Production settings, deployment and authenticated requests; failure could expose protected data or move live routing.
2. **Acceptance invariant:** raw Vercel list values and protected containers never reach controller output/evidence; stable deployment identity and active-queue state remain exact; accepted Sprint 036L stays five/five and public enquiry remains disabled.
3. **Affected layers and verified paths:** installed Vercel 50.42.0 list command -> raw JSON inside isolated child -> exact schema validator -> sanitized hostname/state/target/time pagination -> PowerShell stable walk -> owned hostname inspect -> canonical ID/source/meta/Ready/Production/zero-alias -> retained 029V setting/routing/readiness controller.
4. **Source of truth, transformations and sink:** local installed source SHA-256 `8376a6d957c6fe20a9a1d4738000eb60a519bd079c70f0a82c9e7b59ba9ee367` lines constructing JSON, plus the schema-only nine-page/168-row live discovery. Raw creator/meta are discarded; deployment URL becomes the in-memory inventory key; only sanitized projections reach controller memory and count-only evidence.
5. **Discriminating examples:** a live-shaped row with no `id` and sentinel creator/meta must pass and emit its strict hostname but no sentinel, while the 029V direct parser fails it; adding one unknown row field must refuse; two rows with one hostname must refuse; an active state must block freeze; an owned hostname whose inspect result has wrong ID/meta/source/target/readiness/aliases must never be adopted or deleted.
6. **Durable verification state:** canonical Git HEAD `d822c027c58ad88ec7472e35986e7a33d6a3d6c9`, staged count zero, closed 029V 110/110 evidence, schema-only discovery output, installed-source hash and generated sprint files.
7. **Known uncertainty:** live active-queue state, dashboard setting and all provider/readiness facts must be re-proved at execution time. No discovery fact authorizes credentials, deployment or activation by itself.

## Architecture trace

Vercel list child process
  -> raw stdout held only inside isolated projector
  -> exact known schema and fixed-code errors
  -> creator/meta never enumerated or emitted
  -> sanitized hostname/state/target/created/pagination
  -> stable bounded full walk and head revalidation
  -> zero active queue

owned filtered hostname
  -> exact `vercel inspect <hostname>`
  -> canonical deployment ID plus exact 029W metadata/source/target/readiness/aliases
  -> adoption or cleanup authority only after every field passes

accepted pre-freeze state
  -> five/five Sprint 036L routing
  -> dashboard true-to-false or already-false proof
  -> stable inventory across transition
  -> one private key transfer, one deploy and three no-send/denial requests maximum
  -> success retained or exact compensation

## Failure and compensation matrix

| Failure | Immediate action | Terminal rule |
| --- | --- | --- |
| Projector/version/source/local proof/review fails | Stop before external mutation | Park clean after zero-resource proof |
| Raw/protected value reaches output or evidence | Stop immediately | Material privacy stop; do not repeat |
| Active/unstable queue before freeze | Stop before dashboard/credentials | Park clean with original setting |
| Setting cannot be proved or changed safely | Stop before credentials | Park clean if unchanged/proven; otherwise material |
| Provider transfer/row gate fails | No retry; remove exact safe owned resources | Park clean only with zero residue |
| Deploy response fails | No retry; reconcile via owned hostname then inspect | Clean only after exact deletion/final zero; otherwise material |
| Candidate/routing/readiness/expiry fails | Stop downstream work; compensate exact owned non-routing resources | Park clean only with safe terminal state |
| Alias movement, competing deployment, ownership or cleanup ambiguity | Stop; no alias command | Material; preserve safest state and request fresh authority |
| Supporting tool fails but equal/stronger proof exists | Diagnose once and substitute | Continue and record |

## Manual intervention

Only the private Resend transfer may require the user. Explain in plain English that all pre-secret gates passed; create exactly one restricted 029W sending key; copy and paste it directly into the prepared Sensitive Production password field; save, dismiss token surfaces, clear clipboard; report only `transfer-complete` or `transfer-failed`. Run no observation tool during the secret interval. If this gate is never reached, require nothing from the user.

============================================================
FILE: planning/sprints/029W-vercel-inventory-projection-and-final-readiness-attempt/acceptance.md
============================================================

# Sprint 029W Acceptance — Privacy-Safe Vercel Inventory Projection and Final Readiness Attempt

Status at generation: pending Builder execution.

- [ ] A001 Canonical CWD/Git root, exact 029W branch, fixed HEAD, zero staged files and complete dirty-work manifests reconcile before every mutation phase.
- [ ] A002 Builder rereads all four generated files and Sprint 029V closeout before producing the no-edit critical plan.
- [ ] A003 Flight is recorded `critical` with the complete Flight evidence and no invented Product behavior.
- [ ] A004 Fresh critical plan review passes within three sequential decisions and every finding uses a stable ledger ID.
- [ ] A005 Product routes/runtime, schema, migrations, generic SMTP, activation and closed sprint evidence remain unchanged.
- [ ] A006 Installed Vercel is exactly 50.42.0 and list-source SHA-256 is exact before external gates; drift stops clean.
- [ ] A007 Raw list stdout is captured only inside the isolated child projector and never reaches PowerShell controller memory, chat, logs, files or evidence.
- [ ] A008 Exact top-level, pagination and source-declared row fields/types are validated; unknown fields, malformed JSON, excessive output and non-object/array shapes refuse with fixed codes.
- [ ] A009 Known `creator` and `meta` containers are never enumerated, copied, interpolated, logged, serialized or returned; sentinel fixtures prove zero leakage on accepted and refused paths.
- [ ] A010 Missing list `id` is accepted only because strict deployment hostname is the inventory key; optional present ID is grammar-validated but never guessed or required.
- [ ] A011 Hostname grammar rejects scheme, credentials, port, path, query, fragment, uppercase/invalid labels and duplicates.
- [ ] A012 State, target, custom-environment and timestamp validation remain closed; active states are counted exactly.
- [ ] A013 Pagination is bounded to 10 pages/200 rows, requires row-count agreement, numeric/null cursors, no loops/duplicates and one-restart head stability.
- [ ] A014 Owned list output is adopted only through independent hostname inspection proving canonical ID, exact 029W metadata/source, Production, Ready and zero aliases; cleanup uses only that inspected ID.
- [ ] A015 Controller never treats unknown direct deploy output or list hostname alone as success/cleanup ownership.
- [ ] A016 Three observed live row shapes, missing-ID case, protected sentinels, unknown/extra fields, bad host, duplicate host, active state, pagination/head drift and ownership mismatch have discriminating red-first tests.
- [ ] A017 Exact 029W and retained 029V test arithmetic, PowerShell parse/SelfTest, typecheck, zero-warning lint, Production build, JSON/static checks, protected scan and manifest proof pass.
- [ ] A018 Fresh critical inspection passes within three sequential decisions with no unresolved blocker before external mutation.
- [ ] A019 Pre-freeze Baseline proves exact project, accepted Sprint 036L Ready/five-of-five, zero 029W resource, stable full inventory and zero active deployment.
- [ ] A020 Dashboard proof uses only the exact value-free Production Branch Tracking projection; automatic assignment becomes or remains persistently false without raw page/API/environment-value capture.
- [ ] A021 Stable inventories across the setting checkpoint contain no new deployment and accepted routing remains five/five; once false, Sprint 029W never turns assignment true.
- [ ] A022 No credential work starts until local proof, inspection, stable empty queue, persistent false and identity-blind baselines pass.
- [ ] A023 If reached, exactly one private restricted 029W Resend key transfer occurs without raw secret observation; only fixed status phrase returns.
- [ ] A024 Exact four dedicated and three temporary Sensitive Production-only rows are written through stdin; generic SMTP remains five and activation remains zero.
- [ ] A025 Deploy occurs at most once with Production, `--skip-domain` and `pp_sprint=029W`; no alias/domain/promote/rollback command executes.
- [ ] A026 An accepted candidate is exact-source, exact-metadata, Production, Ready and zero-alias through independent hostname-to-ID inspection while aliases remain five/five.
- [ ] A027 At most one disabled public-gate request, one authenticated SMTP verify-only request and one same-bearer expiry request occur; no `sendMail`, email, enquiry or data effect occurs.
- [ ] A028 Temporary rows are removed before close; success retains only exact key/four dedicated rows/unaliased candidate, while fallback removes every safely compensable owned resource without retry.
- [ ] A029 A clean non-ready result explicitly parks public enquiry, creates no further 029 transport-recovery suffix and makes 025B then 035R the next delivery direction.
- [ ] A030 Any alias movement, competing deployment, setting ambiguity, ownership ambiguity or cleanup residue is classified material and never mislabeled clean.
- [ ] A031 Final aggregate proof records setting transitions, inventory/alias checkpoints, deployment/request counts, resources, compensation, zero prohibited effects, tests/reviews and acceptance-matrix changes.
- [ ] A032 Closeout archives/reconciles/rereads every required planning, roadmap, evidence, matrix and briefing file; STATUS is `sprint-closed`; staged count remains zero; no commit, push, PR or public activation occurs.

## Outcome record

Builder selects exactly one:

- [ ] `final-readiness-recovered-clean`
- [ ] `public-enquiry-parked-clean`
- [ ] `final-readiness-blocked-material`

Record the exact first failing gate, projection class, setting/routing/inventory checkpoints, deployment/request counts, resource mutations and residue, compensation, test/review arithmetic, Final Product Acceptance Matrix changes and exact user action if any. End the final report with the required action or `I need nothing from you.`

============================================================
FILE: planning/sprints/029W-vercel-inventory-projection-and-final-readiness-attempt/handoff-prompt.md
============================================================

# Sprint 029W Builder Handoff

You are Builder for one strict critical Fly in the permanent canonical repository.

## Task contract

**objective:** Isolate raw Vercel list JSON behind an exact value-free child projection, prove hostname-to-canonical-ID ownership, and perform the single final guarded public-enquiry no-send readiness attempt.

**owns:** Only the approved 029W projector/controller/tests/package command/doc, generated sprint/evidence files and standard planning/roadmap/briefing/archive closeout files listed in requirements.

**must_not:** Do not edit Product/runtime, schema, migrations, generic SMTP, activation or closed sprint evidence. Do not expose raw list/provider/secret values; send email; submit/store an enquiry; access mailbox/log content; execute alias/domain/promote/rollback; turn assignment back on after freeze; stage, commit, push or open a PR.

**acceptance:** A001-A032 and exactly one terminal outcome. Any clean non-ready outcome parks public enquiry and advances the road to 025B then 035R without another 029 transport-recovery suffix.

**verification:** Run `npm run test:inventory-projection-029w`, `npm run test:enquiries-harness-029w`, `npm run test:enquiries-harness-029v`, `npm run test:provider-projection-029v`, `npm run test:alias-isolation-029v`, `powershell -NoProfile -ExecutionPolicy Bypass -File scripts/PreflightAuth029W.ps1 -Operation SelfTest`, PowerShell parse proof, `npm run typecheck`, `npm run lint`, `npm run build`, `npm run validate:json`, `npm run validate:static`, the repository protected-data scan and exact approved-file manifest reconciliation. Record real exit codes and counts.

## Required order

1. Verify canonical path/Git state, create/switch exact 029W branch and preserve the dirty manifest.
2. Apply/re-read the Pack and 029V closeout; return the exact no-edit critical plan with file list, guards, every criterion, test baseline/target arithmetic, commands, closeout and `Pack corrections`.
3. Proceed only after fresh critical plan PASS. Implement only that passed plan.
4. Run local proof and fresh critical inspection. No external mutation precedes inspection PASS.
5. Execute the bounded live sequence exactly as requirements/blueprint specify. Stop at the first material gate; never retry deployment or protected transfer.
6. Close one outcome, archive/reconcile/reread, and leave zero staged files.

## Evidence-Proportional Execution Standard

Stop only for material target, authority, security, privacy, migration, destructive, integrity, Production, scope or cleanup risk. Diagnose a supporting-tool failure once and use equal/stronger safe proof when available. Keep deterministic projector/controller/harness/format/encoding/report corrections inside this sprint. Manual intervention is last and limited to the unavoidable private secret transfer if every earlier gate passes.

## Private transfer, only if reached

Tell the operator in plain English: all prior gates passed; privately create exactly one Resend sending key named `Precision Performance public enquiry 029W` restricted to `precisionperformance.com.au`; copy it; paste only into the prepared Sensitive Production `PUBLIC_ENQUIRY_SMTP_PASS` field; save; dismiss token surfaces; clear clipboard; report only `transfer-complete` or `transfer-failed`. Run no observation tool during this interval.

## Final report

State the outcome first. Give exact tests/reviews, setting/routing/inventory truth, mutations/requests/residue, public-enquiry disposition, next delivery direction and whether user action remains. End with the exact action or `I need nothing from you.`
