# Architect Pack - Sprint 036L Privacy-Safe Negative-Path Production Trainer Acceptance

============================================================
FILE: planning/sprints/036L-privacy-safe-negative-path-production-trainer-acceptance/requirements.md
============================================================

# Sprint 036L - Privacy-Safe Negative-Path Production Trainer Acceptance

## Outcome

Complete the Production trainer acceptance journey that Sprint 036J safely rolled back. Reuse the proven process-only binding repair, exact authoritative retained trainer identity, unchanged `8/1/0/0` membership graph, generated-link session, five-alias transaction and automatic compensation. Run the corrected discriminating wrong-horse verifier live, then complete sign-out and anonymous denial. Do not ask a person for a password, email code, OTP, service-role value or PowerShell secret.

Target outcome: `negative-path-trainer-access-live-accepted-clean`.

This is one consolidated correction and acceptance sprint. A deterministic verifier, test, reporter, command-envelope or other non-product correction needed to complete this exact journey stays inside 036L. Do not create another micro-sprint for a supporting-tool limitation when equivalent or stronger safe proof exists.

## Workflow profile and Flight class

Use `strict`. Flight class is `critical` because the flight handles authentication, authorization and privacy evidence and may replace three encrypted Production bindings, create a Production candidate and reassign five live aliases. Critical treatment requires an exact Builder plan, fresh Architect plan review, real checks, a distinct fresh final inspector and automatic compensation.

## Canonical starting authority

Work only in `C:\Users\rrank\OneDrive\PNR Precision Performance Canonical` from exact SHA `5a99c98fffd095bcf44166d049026c217951f7b1`. Resolve the current directory and `git rev-parse --show-toplevel`; both must normalize to the canonical path before Pack application, Builder implementation and external action.

Create only branch `codex/036L-privacy-safe-negative-path-production-trainer-acceptance` from the exact starting SHA. The existing uncommitted method/template changes under `.120x/`, `.agents/`, templates and method-maintenance scripts belong to the user. Preserve them and exclude them from 036L edits, staging and commits. Do not use a legacy repository, temporary checkout, alternate history or worktree.

The Pack must dry-run to exactly four traversal-free destinations inside one 036L sprint folder, then apply and reread all four files. Those applied files become the Builder authority.

## Source authority

Use, in order:

1. `AGENTS.md`, the 120x Fly doctrine and these four applied files.
2. Sprint 036J closeout and its live evidence for the proven bindings, exact identity, graph, session, candidate cutover and completed rollback.
3. Current git source, especially `scripts/autonomous-trainer-access-036J.mjs`, its test, the portal layout/horse route, `lib/domain/horses.ts`, `lib/auth/app-context.ts`, `middleware.ts` and `lib/supabase/middleware.ts`.
4. Current `docs/OPERATIONS_HANDOFF.md`, `docs/AUTH_RLS_PORTAL_ACCESS.md`, `planning/ARCHITECT_BRIEFING.md` and relevant acceptance/evidence records.
5. Fresh installed CLI help and executable readback for mutable Vercel/Supabase state. Historical prose never substitutes for current target/routing/binding proof.

## Task contract

**objective:** Prove one fully autonomous Production trainer session through dashboard, owned horse, permitted workflow, privacy-safe wrong-horse denial, sign-out and anonymous denial, leaving the approved binding set and exact candidate live only on complete target success and otherwise restoring all three original bindings plus all five aliases.

**owns:** Canonical/branch/source proof; the existing 036J autonomous harness and tests; the horse denial route/domain query only if a discriminating red test proves actual protected leakage; exact three-binding process-only repair/restoration; exact retained identity and graph read/verify with idempotent exact-owned repair only if drift is observed; one scoped checkpoint commit/push when source changes exist and otherwise an exact branch/source push; one Production-target `--skip-domain` candidate; exact five-alias cutover/rollback; generated-link session without delivery; live complete session journey; final binding/graph/routing/privacy/residue proof; fresh reviews; proportional closeout and scoped closeout commit/push.

**must_not:** Prompt for, display, persist, log or commit any secret, key, password, raw email, token hash, access/refresh token, cookie, protected response body or identity row; send an email/OTP or use a mailbox/human code; select an identity by position or guessed email; modify/delete either excluded Auth identity; change non-owned data, schema, migrations, RLS, role/permission contracts, Storage, provider/SMTP/template/callback/rate-limit settings, DNS, Stripe/commerce or Sprint 029N; change a Vercel variable outside the exact three Production Supabase bindings and compensation; make a second deployment; move an unlisted alias; weaken wrong-horse denial to mere route echo absence; stage inherited method changes; merge, PR, push `develop`, force-push or rewrite history.

**acceptance:** The corrected verifier accepts a generic denial even when the caller-generated requested UUID appears only in framework route transport, but fails on owned horse/stable names, status, counts or an actual unauthorized grant. Local tests and retained gates pass. Fresh binding, identity and graph proof matches the 036J foundation. Exactly one source-proven Ready candidate reaches five/five only after immutable safety. A real in-memory session renders portal, exact retained horse and permitted workflow; a generated absent/non-owned horse returns only the generic denial contract; sign-out clears the session; signed-out and anonymous portal requests redirect to sign-in. Complete success retains approved bindings and five/five candidate. Every non-target outcome restores all five aliases when moved and all three original Production bindings, then proves canonical safety.

**verification:** Canonical/SHA/dirty-path classification; Pack dry-run/application/readback; exact plan and fresh critical review; focused corrected harness plus retained 035K/036H tests; route/domain discriminating tests if Product code changes; syntax, TypeScript, zero-warning lint, Production build, diff/JSON/encoding/privacy scans; installed CLI help; sanitized binding/identity/graph projections; direct-remote equality; exact project/rollback/five-alias baseline; one deployment and immutable smoke; per-write independent alias resolution; live session journey; final binding/graph/routing/public/protected/API/disabled-commerce/privacy/residue proof; fresh critical inspection; closeout/readback; and one exact final outcome.

## Privacy-safe denial contract

The client chooses a generated UUID and places it in the requested URL. Next.js may serialize that same caller-controlled value in route/transport state. Its mere presence is not protected horse leakage and must not fail acceptance.

The rendered denial must have HTTP 200 and visible generic `Horse not available` language. It must not contain the retained horse name, retained stable name, any unauthorized horse/stable identity, profile fields, `Status:`, `Count:` or protected record fields. A false implementation that grants the wrong horse, returns owned identity/state/count data, or loses generic denial must fail deterministic tests and live proof.

Current source already uses RLS-scoped Supabase queries and renders a generic response when no accessible horse is returned. Product/runtime code remains unchanged unless the Builder first proves a real source or rendered leak with a discriminating red test. Transport echo alone is not such proof.

## Protected execution boundary

Continue using `vercel env run` so approved Production bindings enter only the child process. Use the already-tested `repair-production-bindings`, `binding-status`, `classify-reconcile`, `session-proof`, `verify` and `restore-production-bindings` modes unless a focused deterministic correction is required. Standard Vercel Production environment values are encrypted at rest and written via stdin only. Never use a command argument, file export, transcript or evidence artifact for a protected value.

The retained ledger remains `%TEMP%\pnr-035k-live-trainer-access-owned.json`. Require exact project `uvskssaecdhxcgytkasc`, `state=retained`, `authOwnership=adopted`, retained Auth ID plus email hash, eight exact-owned application IDs and complete contracts. Report only sanitized counts/classes. Exactly one of the three Auth identities must match; the other two remain excluded and unchanged.

Repair exactly `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY` and `SUPABASE_SERVICE_ROLE_KEY` in Vercel Production from the prohibited old project to the approved project using the proven one-read/stdin transaction. Build the complete transaction before writing. A partial failure restores the original complete set immediately. On every non-target sprint outcome, restore the original three-value set from the unchanged Development copy and prove the prohibited projection.

## Approved implementation and closeout files

Primary implementation scope:

- `scripts/autonomous-trainer-access-036J.mjs`
- `scripts/test-autonomous-trainer-access-036J.mjs`

Conditional Product scope, only after a discriminating failure proves it necessary:

- `app/(portal)/portal/horses/[horseId]/page.tsx`
- `lib/domain/horses.ts`
- the narrowest existing horse/domain test file needed to prove the denial contract
- `lib/auth/app-context.ts`, `middleware.ts` or `lib/supabase/middleware.ts` only if the real sign-out/anonymous path demonstrates a defect and a red test identifies that exact layer

Closeout scope may update the four 036L files and `planning/STATUS.json`, `planning/STATE.md`, `planning/ROADMAP.md`, `planning/ARCHITECT_BRIEFING.md`, `planning/SPRINT_LIFECYCLE_LEDGER.md`, `planning/SPRINT_SCHEDULE.md`, `planning/EVIDENCE_INDEX.md`, `planning/FINAL_PRODUCT_ACCEPTANCE_MATRIX.md`, `planning/DECISIONS.md`, `planning/RISKS.md`, `planning/QUESTIONS.md`, `docs/OPERATIONS_HANDOFF.md` and `docs/AUTH_RLS_PORTAL_ACCESS.md` only as directly required by the real result. Preserve inherited method/template dirt.

## Release and compensation authority

Freshly verify exact Vercel project `prj_6To7czLpCEGL6fInkQwE4egePPpq` (`rankin007s-projects/pnr-precision-performance`) and exact Ready rollback `dpl_EUJyBLQp7okgbPwtBU6nmMH88L6A`. The only affected aliases are:

1. `precisionperformance.com.au`
2. `www.precisionperformance.com.au`
3. `pnr-precision-performance.vercel.app`
4. `pnr-precision-performance-rankin007s-projects.vercel.app`
5. `pnr-precision-performance-rankin007-rankin007s-projects.vercel.app`

Candidate order is legacy-team, team-project, project, `www`, apex. Rollback order is apex, `www`, project, team-project, legacy-team. Resolve all five independently after each assignment and require `1/4` through `5/0`; deployment alias metadata is corroborating only.

Create no more than one Production-target `--skip-domain` candidate. Any automatic alias movement, wrong project/source, non-Ready candidate, immutable safety failure, partial binding state or unprovable routing state triggers compensation and prohibits a second deployment. After cutover begins, any non-target result first restores all five aliases, then restores all three bindings, then proves safety.

## Evidence-proportional execution

Stop only for a material wrong target, authority gap, secret exposure, privacy/security failure, unexpected protected data, non-owned identity/data conflict, partial Production mutation, unauthorized scope expansion, failed rollback/integrity or unprovable cleanup. Substitute equivalent or stronger safe evidence when a preferred supporting tool is unavailable. Keep deterministic harness, validator, formatting, encoding, reporter and command-envelope corrections inside 036L. Do not create a follow-up solely for browser automation, optional CLI paths, renderers, clipboard control, schema dumps or redundant proof. Manual intervention is the last safe option after process-only and equivalent mechanisms are exhausted.

============================================================
FILE: planning/sprints/036L-privacy-safe-negative-path-production-trainer-acceptance/blueprint.md
============================================================

# Sprint 036L Blueprint

## Execution sequence

1. Re-prove canonical path, exact starting SHA, current branch and inherited dirty-path classification. Dry-run/apply/reread the exact Pack.
2. Create the exact 036L branch. Read the applied sprint, 036J closeout, current harness/tests and negative-path source before proposing implementation.
3. Builder returns the exact no-edit critical plan: task contract, exact files, guards, all numbered acceptance criteria, commands, baseline/target arithmetic, closeout actions, `Pack corrections`, and blockers.
4. A genuinely fresh Architect reviews that exact plan under the three-decision budget. Amend applied files only for accepted within-intent Pack corrections. No implementation starts without `pass`.
5. Run the current 036J deterministic harness test first. Confirm the corrected transport-echo pass case and protected name/stable/status/count and wrong-grant red cases.
6. Trace the live denial origin-to-sink. Change Product code only if an executable discriminating test proves actual protected output; otherwise record `Product change not required` and keep runtime unchanged.
7. Run focused and retained tests, syntax, TypeScript, zero-warning lint, Production build, diff/JSON/encoding/scope/privacy checks. Record exact counts and exits.
8. Establish an exact remote-backed source checkpoint. If implementation changed, commit/push only scoped files. If no source changed, push the exact 036L branch pointer and prove local/direct-remote equality. Never stage inherited method changes.
9. Reconcile installed CLI help, exact Vercel project, Ready rollback, the five independent alias targets and the exact three-binding before-state.
10. Repair exactly the three Production bindings through the proven process-only compensated transaction; require an approved projection before the first clean-project query.
11. Run `classify-reconcile`; require exactly three identities, one authoritative, two excluded unchanged, exact `8/1/0/0`, active trainer membership and write permission. Exact-owned repairs may be idempotent only; any ownership conflict triggers compensation.
12. Create exactly one Production-target `--skip-domain` candidate from the exact source; require exact project/source/branch, Ready state and immediate five/five rollback.
13. Run immutable candidate public/protected/API/disabled-commerce safety, then assign the exact five aliases in candidate order with a complete independent reread after every write.
14. At five/five candidate, run the complete `session-proof` through `https://precisionperformance.com.au`: real session, portal, retained horse, permitted workflow, generated wrong horse generic denial, sign-out, signed-out denial and anonymous denial.
15. On target success, run final approved bindings, read-only graph `verify`, five/five candidate, canonical safety, privacy/residue and exact-source proof. Retain approved bindings and candidate only when every boundary passes.
16. On any non-target outcome after movement, restore all five aliases in fixed rollback order with a full reread after every write; restore the original three bindings; prove five/five exact Ready rollback, prohibited binding projection and canonical safety. Do not make a second deployment.
17. Present the applied sprint, approved plan, complete diff and real evidence to a new fresh critical inspector distinct from the plan reviewer. Follow the stable ledger for at most three inspection decisions; any repair returns through Builder planning and fresh Architect review.
18. After inspection `pass`, update proportional closeout records, only change the 036L roadmap row to `done`, refresh the complete Architect briefing, set STATUS phase `sprint-closed`, commit/push only scoped closeout and reread every required result from disk.

## Command envelopes

Confirm exact installed syntax before use. Intended protected modes are:

```text
vercel env run -e production -- node scripts/autonomous-trainer-access-036J.mjs repair-production-bindings
vercel env run -e production -- node scripts/autonomous-trainer-access-036J.mjs binding-status
vercel env run -e production -- node scripts/autonomous-trainer-access-036J.mjs classify-reconcile
vercel env run -e production -- node scripts/autonomous-trainer-access-036J.mjs session-proof --origin https://precisionperformance.com.au
vercel env run -e production -- node scripts/autonomous-trainer-access-036J.mjs verify
```

Every non-target outcome restores with the unchanged Development source:

```text
vercel env run -e development -- node scripts/autonomous-trainer-access-036J.mjs restore-production-bindings
vercel env run -e production -- node scripts/autonomous-trainer-access-036J.mjs binding-status
```

Capture only allowlisted JSON state/classes/counts. Never echo environment values, raw command output containing them, identities, cookies, token hashes or response bodies.

## Five-alias transaction

Candidate writes: legacy-team -> team-project -> project -> `www` -> apex.

Rollback writes: apex -> `www` -> project -> team-project -> legacy-team.

After each write, resolve all five aliases independently to the exact deployment ID and require the expected candidate/rollback split. A discrepancy triggers rollback from the current known state; never infer routing from the candidate's alias list alone.

## Flight evidence

### 1. Class and reason

`critical`: authentication, protected environment injection, privacy-sensitive denial proof, Production bindings, one deployment and five live alias writes are in scope.

### 2. Acceptance invariant at risk

One exact retained trainer with active membership and `horse.records.write` may render only its RLS-accessible horse/workflow. A generated absent/non-owned horse must render the same generic unavailable state without protected horse/stable/state/count data. Sign-out must remove the usable session, and signed-out/anonymous portal access must redirect to sign-in. Corrected bindings/candidate remain live only if the full invariant passes; otherwise complete compensation restores the known-safe state.

### 3. Affected layers and verified paths

- Caller-generated UUID -> `/portal/horses/[horseId]` route parameter.
- `app/(portal)/layout.tsx` -> `requirePortalAppContext` for authenticated portal access.
- horse page -> `getAppAuthContext`/permission projection -> `getAccessibleHorseDetail`.
- `lib/domain/horses.ts` -> RLS-scoped `horses`, `biochemistry_tests` and write-permission queries -> `composeAccessibleHorseDetail`.
- missing/inaccessible row -> generic `Horse not available` SectionCard sink.
- Next.js rendered HTML/route transport -> `createFetchAdapter` -> `proveSessionJourney` denial predicates.
- generated-link token hash -> SSR cookie adapter -> live positive requests -> local sign-out -> signed-out and anonymous portal redirects.
- Vercel binding transaction -> process-only Supabase target -> exact retained graph -> one candidate -> five aliases -> automatic rollback/restoration.

### 4. Source of truth, transformations and sink

Supabase RLS and the retained 035K ownership ledger are the data/access truth. The exact retained Auth ID plus email hash selects one identity; exact-owned application rows produce membership and permission; SSR verification creates an in-memory cookie; RLS filters requested horse queries. The user-supplied route UUID may be serialized by Next.js, but only database-derived horse/stable/state/count material is protected leakage. The visible generic denial, sign-in redirect and sanitized verifier result are the acceptance sinks.

### 5. Discriminating examples

1. Generic denial plus the generated requested UUID only in route transport must pass. The former plausible implementation failed merely because the caller's own UUID appeared.
2. Generic denial plus retained horse name, retained stable name, `Status:` or `Count:` must fail. A weak verifier that only checks the title would wrongly pass.
3. A fake adapter that grants/returns the generated wrong horse must fail before acceptance. An absent-row-only fixture would not distinguish a broken authorization grant.
4. A sign-out adapter that retains a cookie, or an anonymous adapter that returns the dashboard instead of a sign-in redirect, must fail. Checking only the sign-out API return would miss usable-session persistence.
5. A partial binding write or alias mismatch must restore the entire known-safe set. Checking only the last write would miss split Production state.

### 6. Durable verification state

Git authority is exact starting commit `5a99c98fffd095bcf44166d049026c217951f7b1`. Its 036J closeout records 131 focused + 101 retained 035K + 70 retained 036H = 302 passing, exact `8/1/0/0`, one authoritative of three identities, a real positive session journey, candidate `dpl_HQmutgVoA1rdXDWSgQ5qGwW7YeBY` Ready/alias-free, and final five/five rollback `dpl_EUJyBLQp7okgbPwtBU6nmMH88L6A` plus prohibited original bindings. Current source contains the post-inspection correction removing the generated UUID as a leakage predicate and adding transport-echo/count-leak discriminating tests.

### 7. Known uncertainty

The corrected verifier has not run against a live candidate; sign-out and anonymous checks were not reached in 036J. Current mutable Vercel routing, CLI semantics, bindings, retained graph and candidate source must be freshly proven. Product source inspection indicates a generic RLS-backed denial and no known protected leak, but 036L must not claim that live result before execution. Any contradiction is a material stop or compensated non-target outcome, never an invented pass.

## Permitted outcomes

- `negative-path-trainer-access-live-accepted-clean`: all acceptance passes; approved bindings and five/five candidate remain.
- `negative-path-trainer-access-preflight-blocked-clean`: no alias moved; original bindings are restored and five/five rollback remains.
- `negative-path-trainer-access-rolled-back-clean`: cutover began, a target boundary failed, all five aliases and all three original bindings are restored, and final safety passes.
- `negative-path-trainer-access-blocked`: compensation, integrity, privacy, cleanup or exact-target state cannot be proven; stop without claiming a safe landing.

============================================================
FILE: planning/sprints/036L-privacy-safe-negative-path-production-trainer-acceptance/acceptance.md
============================================================

# Sprint 036L Acceptance

## Pack, authority and plan

- [ ] **A001** Canonical current directory and Git top-level are exact at every role/external transition; starting SHA and inherited dirty paths are classified and preserved.
- [ ] **A002** Pack dry-run/apply/readback proves exactly four traversal-free files under the one 036L folder.
- [ ] **A003** Only the exact 036L branch is used; no legacy/temporary repository or alternate history participates.
- [ ] **A004** Builder supplies the required exact no-edit critical plan including all criteria, arithmetic, Pack corrections and closeout.
- [ ] **A005** A genuinely fresh Architect passes the exact plan within the three-decision budget; stable plan findings and corrections are recorded.

## Corrected denial and local gates

- [ ] **A006** Current corrected harness accepts caller-generated route UUID transport echo while still requiring HTTP 200 and visible generic `Horse not available`.
- [ ] **A007** Discriminating red controls fail for protected horse/stable identity, status/count leakage, actual wrong-horse grant, retained cookie after sign-out and anonymous dashboard access.
- [ ] **A008** Product/runtime source is unchanged unless a failing discriminating test proves a real leak or session defect; any Product correction is the narrowest approved-layer change.
- [ ] **A009** Focused 036J, retained 035K and retained 036H counted tests pass with exact arithmetic; applicable mapped auth/role/dashboard/public/static/JSON controls pass.
- [ ] **A010** Syntax, TypeScript, zero-warning lint, Production build, diff/JSON/encoding/scope/privacy checks pass with exact recorded commands/exits.

## Bindings, identity and graph

- [ ] **A011** Fresh prechange binding projection proves exactly three complete prohibited old-project Production bindings without value exposure.
- [ ] **A012** The one proven in-memory authenticated CLI key read and stdin-only transaction replaces exactly the three authorized Production bindings and proves the complete approved projection; partial failure compensates all three.
- [ ] **A013** No protected value, raw identity, email, token hash, cookie or response body reaches arguments, logs, files, evidence, commits or result objects.
- [ ] **A014** Exact retained ledger/target guards pass before data access; current Auth population is three, with exactly one authoritative retained ID-plus-hash match and two excluded identities unchanged.
- [ ] **A015** Exact graph is `application=8`, `auth=1`, `storage=0`, `wrongHorse=0`, active trainer membership and effective `horse.records.write`; any repair is exact-owned/idempotent and conflicts fail before broad mutation.

## Source, candidate and routing

- [ ] **A016** Exact 036L source is remote-backed with direct-remote equality; scoped staging/commit excludes inherited method/template dirt.
- [ ] **A017** Fresh Vercel proof identifies the exact project, exact Ready rollback and exactly five affected aliases independently resolving five/five rollback.
- [ ] **A018** Exactly one Production-target `--skip-domain` candidate is exact-project, exact-source/branch and Ready; immediate reread proves aliases did not move.
- [ ] **A019** Immutable candidate public/protected/API/disabled-commerce safety passes before cutover.
- [ ] **A020** Exactly five candidate assignments in fixed order reach five/five candidate with full independent rereads and expected `1/4` through `5/0` transitions.

## Complete live session journey

- [ ] **A021** Admin link generation targets only the authoritative retained identity, sends no email and keeps token hash/cookies in memory only.
- [ ] **A022** Programmatic verification yields a real server session and `/portal` renders the retained trainer dashboard without redirect/bootstrap collision.
- [ ] **A023** Exact retained horse workspace and existing permitted workflow render.
- [ ] **A024** Generated absent/non-owned horse returns HTTP 200 and generic denial without protected horse/stable identity, profile, status, count or record data; caller route-state echo alone is not treated as leakage.
- [ ] **A025** Local sign-out clears usable session state; the signed-out portal request and a fresh anonymous portal request redirect to sign-in.

## Final success or compensation

- [ ] **A026** Target success retains exactly the approved three Production bindings, exact graph and five/five candidate only after A001-A025 pass.
- [ ] **A027** Every non-target outcome after alias movement performs exactly five rollback assignments in fixed order with full rereads to exact Ready rollback before binding restoration.
- [ ] **A028** Every non-target outcome restores all three original bindings from the unchanged Development copy and proves the complete prohibited projection.
- [ ] **A029** Final canonical public/protected/API/disabled-commerce safety, privacy/residue and exact routing/source checks pass for the retained success or compensated state.
- [ ] **A030** No ambiguous identity, non-owned row, schema/RLS/contract/Storage/provider/DNS/commerce/029N surface or unlisted environment variable/alias is changed; no second deployment occurs.

## Inspection and closeout

- [ ] **A031** A new fresh critical inspector, distinct from the plan reviewer, judges every applicable numbered criterion against the approved plan, full diff and real evidence within the three-decision budget.
- [ ] **A032** All blocking inspection findings are resolved through Builder re-plan and fresh review; advisories/residual risks are recorded under the stable ledger.
- [ ] **A033** Proportional state/decision/risk/question/status/roadmap/lifecycle/schedule/evidence/briefing/auth-operation records state the exact real outcome without upgrading partial proof.
- [ ] **A034** Only the 036L roadmap row changes to `done`; the briefing contains current Executive summary, Readiness signals, exact tests, evidence, Plan corrections and structured recommended next Architect action.
- [ ] **A035** STATUS phase is `sprint-closed`; scoped closeout commit/push and direct-remote equality pass; final readback proves one permitted outcome and no scope drift.

============================================================
FILE: planning/sprints/036L-privacy-safe-negative-path-production-trainer-acceptance/handoff-prompt.md
============================================================

# Sprint 036L Builder Handoff

Build only from these applied Sprint 036L files in the permanent canonical repository.

## Task contract

**objective:** Complete one autonomous Production trainer journey through privacy-safe wrong-horse denial, sign-out and anonymous denial, retaining approved bindings/candidate only on complete success and otherwise restoring the original three bindings and all five aliases.

**owns:** Canonical/source/branch proof; corrected 036J harness/tests; conditional narrow denial/session Product fix only after a red discriminating test; exact process-only three-binding transaction; exact retained identity/graph verify or exact-owned idempotent repair; one source-proven candidate; fixed five-alias cutover/rollback; generated-link in-memory session; complete live journey; final proof; fresh reviews; proportional closeout and scoped direct-remote publication.

**must_not:** Ask for or expose passwords, codes, keys, emails, token hashes, cookies, identities or response bodies; use email/mailbox/OTP delivery; guess identity; modify/delete excluded identities or non-owned data; change schema/RLS/contracts/Storage/provider/DNS/commerce/029N or unlisted bindings/aliases; make a second deployment; stage inherited method dirt; merge/PR/develop/force-push/rewrite.

**acceptance:** Corrected deterministic proof distinguishes caller route transport from protected horse leakage; local/full checks pass; binding/identity/graph foundation is freshly exact; one exact-source Ready candidate reaches five/five; the real session passes portal, owned horse, workflow, generic wrong-horse denial, sign-out and anonymous denial; final success retains approved bindings/candidate, while every non-target outcome proves complete alias and binding compensation plus safety.

**verification:** A001-A035; exact canonical/Git/Pack checks; fresh critical plan review; focused/retained tests and full Product gates; sanitized binding/identity/graph projections; direct-remote source proof; Vercel project/rollback/candidate/alias ledgers; complete live session; final safety/privacy/residue; distinct fresh critical inspection; closeout commit/push and full disk readback.

## Builder planning gate

Before editing implementation, read the applied folder and current source yourself. Return:

- the task contract;
- exact implementation file list;
- scope guards;
- every A001-A035 criterion mapped to work/evidence;
- exact verification commands;
- observed test baselines and target arithmetic;
- standard closeout files/actions after inspection;
- `Pack corrections` (`None` is valid); and
- any blocking ambiguity.

Do not edit implementation while producing this plan. The fresh Architect must pass the exact plan before implementation begins.

## Execution rules

Trust git and executable output over prose. Start with the corrected current harness; do not change the horse route merely because the generated URL UUID appears in Next.js transport. A Product edit requires a red discriminating test demonstrating protected data or a real session boundary failure.

Keep protected values in process memory. Use the proven Vercel/Supabase child envelopes, stdin writes, finite sanitized output and `finally` cleanup. No person enters a code or secret. Freshly prove exact targets before mutation. On any non-target result, execute compensation immediately in the required order and do not retry with a second deployment.

Apply the Evidence-Proportional Execution Standard. Stop only for material target, authority, security/privacy, ownership, scope, Production integrity, compensation or cleanup risk. Use equivalent or stronger safe proof for supporting-tool failures. Keep deterministic harness/validator/reporter/encoding corrections in this sprint. Manual intervention is last, and no follow-up is created merely for an optional tool limitation.

After real checks, give the applied sprint, approved plan, complete diff and exact evidence to a new critical inspector distinct from the plan reviewer. Only inspection `pass` permits closeout. Then refresh the required durable records, change only the 036L roadmap row to done, set STATUS `sprint-closed`, publish only scoped 036L work and reread the landing from disk.
