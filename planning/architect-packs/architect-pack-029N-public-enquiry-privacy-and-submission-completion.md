# Architect Pack - Sprint 029N Public Enquiry Privacy and Submission Completion

============================================================
FILE: planning/sprints/029N-public-enquiry-privacy-and-submission-completion/requirements.md
============================================================

# Sprint 029N - Public Enquiry Privacy and Submission Completion

## Outcome

Replace the visibly unavailable `Request a Stable Trial` form with one governed, accessible public enquiry journey that validates and accepts the existing seven-field enquiry, stores it privately in the approved Supabase project, notifies exactly one protected Aprec8 recipient through the existing encrypted SMTP channel, gives the visitor a truthful receipt reference, enforces privacy/abuse/retention controls, and is proven on an exact Production candidate before the five public aliases move.

Target outcome: `public-enquiry-live-accepted-clean`.

This is one consolidated completion sprint. Deterministic validator, test harness, mail classifier, migration verifier, reporter, command-envelope, fixture cleanup or other non-product correction needed to complete this exact outcome remains inside 029N. Do not create another micro-sprint for a supporting-tool inconvenience when equivalent or stronger safe proof exists.

## Workflow profile and Flight class

Use `strict`. Flight class is `critical` because the flight activates public personal-data collection, adds remote schema and restricted service-role writes, uses encrypted email credentials, sends an external notification, creates a Production candidate and may reassign five live aliases. Critical treatment requires an exact Builder plan, fresh Architect plan review, real end-to-end checks, a distinct fresh final inspector and proven compensation/cleanup.

## Canonical starting authority

Work only in `C:\Users\rrank\OneDrive\PNR Precision Performance Canonical` from exact SHA `bc2cc029ef4251d92bb7f46e59d18f32033230f6` on branch `codex/029N-public-enquiry-privacy-and-submission-completion`. Resolve the current directory and `git rev-parse --show-toplevel`; both must normalize to the canonical path before Pack application, implementation, staging, migration or external action.

The existing uncommitted method/template changes under `.120x/`, `.agents/`, templates and method-maintenance scripts belong to the user. Preserve them and exclude them from 029N edits, staging and commits. Do not use a legacy repository, temporary checkout, alternate history or worktree.

The Pack must dry-run to exactly four traversal-free destinations inside one 029N sprint folder, then apply and reread all four files. Those applied files become the Builder authority.

## Source authority and resolved decisions

Use, in order:

1. `AGENTS.md`, the 120x Fly doctrine and these four applied files.
2. The accepted 029M form/content surface and its evidence, which expressly deferred transmission until the privacy/data gate was resolved.
3. The user's approved external `WEBSITE CHANGES 27072026.docx` and `Electrolytes.pdf` as read-only source inputs. Neither document supplies a private recipient address and neither is modified by this sprint.
4. Current source, migrations, tests, public content, Supabase server boundary and accepted 036L Production routing/rollback evidence.
5. Fresh installed CLI/API help and executable readback for mutable Vercel/Supabase/deployment state. Historical prose never substitutes for current remote proof.

The user has now replaced the private enquiry recipient in Vercel and directs the program to use encrypted environment bindings without asking for repeated codes or secret entry. The address remains hidden by design and is never required in source or evidence. Fresh metadata proves `CONTACT_ENQUIRY_EMAIL` is encrypted and assigned to Production. Builder preserves that binding and proves only its name, protection class and Production availability.

The following product/privacy decisions are final for 029N:

- collect enquiries only to respond about a stable trial, consultation and related Precision Performance services;
- do not use an enquiry for marketing, profiling, commerce, onboarding, horse-health diagnosis, analytics or model training;
- keep the existing seven field concepts; stable address and person referred by are optional, while trainer name, stable name, phone, email and approximate horse volume are required;
- require an unchecked acknowledgement of the collection notice and permission to use the information to answer the enquiry; it is not marketing consent;
- store submissions in the approved Supabase project in Singapore for 90 days, unless the person separately enters a governed customer relationship; do not automatically copy or convert the enquiry;
- notify one encrypted `CONTACT_ENQUIRY_EMAIL` recipient using the existing encrypted SMTP channel; send no confirmation email to the visitor;
- treat SMTP provider acceptance as notification acceptance, not proof that a human opened an inbox;
- let a person request access, correction or deletion through the published business contact method while quoting the opaque receipt reference;
- retain no raw IP address or user-agent string; retain only an HMAC-derived abuse bucket for at most 24 hours;
- use server validation, same-origin and content-type checks, a honeypot, bounded rate limiting and idempotency; do not add a new CAPTCHA/provider account;
- persist first, notify second, return a truthful stored receipt even if notification needs a safe retry, never claim email delivery to the visitor, and do not automatically retry an ambiguous SMTP outcome; and
- disclose approved storage and provider processing truthfully on a public Privacy page before submission is enabled.

## Task contract

**objective:** Activate one privacy-governed public trainer-enquiry path that stores each accepted request once, notifies the single protected recipient through the existing SMTP channel, provides a truthful opaque receipt, enforces collection/retention/abuse/failure controls, and reaches Production only after exact candidate proof and clean synthetic-data removal.

**owns:** Canonical/branch/Pack proof; the current public form and enquiry copy; a public privacy route and links; one additive migration `0022_public_trainer_enquiries.sql`; server-only enquiry contract/persistence/email/maintenance code; focused tests and autonomous live proof; metadata-only proof and preservation of the encrypted Production `CONTACT_ENQUIRY_EMAIL` binding; process-only creation of new random abuse/cron secrets when absent; existing SMTP binding classification and preflight; one scoped checkpoint commit/push; application of only migration 0022 to approved Supabase project `uvskssaecdhxcgytkasc`; one exact-source Production candidate; fixed five-alias cutover/rollback; synthetic submission/duplicate/negative/failure/cleanup evidence; privacy and operations documentation; fresh reviews; proportional closeout and scoped closeout commit/push.

**must_not:** Display, return, persist, log, commit or place in arguments/files any recipient address, SMTP value, Supabase secret, generated secret, token, raw network address, real enquiry payload, protected identity or mailbox content; ask a person for a password/code/OTP/secret; decrypt or replace the newly supplied recipient; use `ADMIN_NOTIFICATION_EMAIL`, `gmail_emails` or another address as a guessed fallback; add analytics, tracking, marketing consent, CAPTCHA, a new mail vendor/account, checkout, onboarding or medical fields; expose enquiry rows to browser Supabase clients, `anon` or `authenticated`; change migrations 0001-0021, existing RLS/contracts/Auth/Storage/Stripe, the accepted trainer graph or Sprint 036K; drop/overwrite existing schema/data; send to any address except the protected configured recipient; move an unlisted alias; make a second deployment; stage inherited method dirt; merge, PR, push `develop`, force-push or rewrite history.

**acceptance:** The public form clearly explains collection, links Privacy, labels optional fields, requires explicit acknowledgement and is keyboard/screen-reader usable. Valid requests are validated again on the server, stored exactly once behind service-role-only controls with 90-day expiry, and assigned an opaque reference. The existing SMTP provider is safely classified and accepts one text-only notification to the protected recipient without secret/address exposure. Duplicates do not create or notify twice; invalid, cross-origin, honeypot and rate-limit controls fail generically; definite notification failures are safely retryable, ambiguous outcomes are not duplicated; exact synthetic data is removed. One exact-source Ready candidate passes immutable and live evidence before exactly five aliases reach it. Any non-target result keeps/restores the accepted 036L deployment on all aliases and leaves an inert additive schema with no public collection, or rolls back source/aliases cleanly.

**verification:** A001-A040; canonical/Git/Pack gates; fresh critical plan review; migration semantic and remote readback; focused unit/contract/integration tests; retained public/auth/role/commerce gates; TypeScript, zero-warning lint and Production build; source/privacy/secret/encoding/diff scans; metadata-only env and provider-class proof; exact project/source/rollback/candidate/five-alias ledgers; live synthetic success, idempotency and negative controls; sanitized row/notification status; exact fixture purge; final public/privacy/API/protected/commerce/routing/source proof; distinct fresh critical inspection; closeout commit/push and final disk/direct-remote readback.

## Collection, display and validation contract

The form collects only:

- trainer name: required, trimmed, 2-120 characters;
- stable name: required, trimmed, 2-160 characters;
- stable address: optional, trimmed, maximum 500 characters;
- phone number: required, trimmed, 6-40 characters, permissive international punctuation but no control characters;
- email address: required, normalized for validation, maximum 254 characters;
- approximate horse volume: required integer 1-9999;
- person referred by: optional, trimmed, maximum 160 characters;
- acknowledgement: required boolean and stored notice version/timestamp; and
- a hidden honeypot that must remain empty and is never stored.

Reject unknown keys, oversized bodies, non-JSON content, invalid control characters and invalid origin/host combinations. Client validation is convenience only. Responses use a small stable contract with generic field errors, generic retry/limit language and an opaque public reference on accepted or idempotently repeated submissions. Never echo the submitted payload in a response or log.

## Privacy notice contract

Create `/privacy` and link it next to the acknowledgement, from the enquiry section and from the disclaimer/navigation surface. State in clear Australian English:

- Aprec8 Pty Ltd trading as Precision Performance is the collector and the published public phone/contact method is available for questions or access/correction/deletion requests;
- the sole purpose and the consequence that required details are needed to answer the enquiry;
- the exact field categories, optional fields, no requested medical/health information, no marketing and no automated decision-making;
- storage in the approved Supabase project in Singapore, service providers used for hosting and email, and likely overseas processing derived from the allowlisted provider class where practicable;
- only authorised Aprec8 handling, one protected notification recipient and no sale of information;
- 90-day application/database and operational mailbox retention unless a separately governed relationship begins;
- raw IP/user-agent non-retention, a pseudonymous abuse bucket held no more than 24 hours, encryption in transit and restricted server access;
- reference-based access/correction/deletion requests and reasonable identity verification; and
- the notice version/effective date.

The Privacy page must fail closed or state submission unavailable if the SMTP host cannot map to an approved provider disclosure class. Do not print the host or address. The existing public disclaimer must stop claiming the form never transmits or stores once the live workflow is accepted.

## Data and access contract

Add only `supabase/migrations/0022_public_trainer_enquiries.sql`. It may create:

1. `public.trainer_enquiries` for the seven fields, notice version/timestamp, opaque reference, keyed idempotency hash, created/expiry timestamps, provider class, finite notification state/attempt metadata and sanitized error class; and
2. `public.trainer_enquiry_abuse_buckets` for HMAC-only network buckets and expiry, or an equivalent equally private structure.

Use UUID primary keys and database constraints for lengths/ranges/statuses/expiry. Never store a raw idempotency key, IP, user agent, SMTP response, exception text, secret or provider hostname. Enable RLS on each table, create no browser policies, explicitly revoke all privileges from `public`, `anon` and `authenticated`, and grant only the minimum service-role privileges. Explicitly remove permissive default public-schema grants. Any SQL function used for atomic insert, queue claim, retention or cleanup must set a safe `search_path`, be service-role-only, be concurrency/idempotency safe and return sanitized fields only.

The API accesses these tables only through `lib/supabase/admin.ts` or an equally server-only service-role boundary. Add `import "server-only"` where appropriate. No client bundle may import server env/provider/storage modules.

Database enquiries expire 90 days after creation. Abuse buckets expire within 24 hours. Maintenance deletes expired rows and never logs contents. Mailbox copies are an operational responsibility of the configured recipient and must be deleted within the same 90-day window unless a separate governed relationship exists; document that responsibility without naming the address.

## Delivery, duplicate and failure contract

Use the current encrypted `SMTP_HOST`, `SMTP_PORT`, `SMTP_USER`, `SMTP_PASS`, `SMTP_FROM` and `CONTACT_ENQUIRY_EMAIL` bindings. Add the narrowest maintained SMTP library and exact lockfile change; do not hand-roll SMTP. Validate binding presence and syntax without logging values. Map the protected host to an explicit allowlisted provider class and public disclosure; an unclassified host fails closed before form activation.

The email is plain text, has no attachment, image, tracking, remote content or HTML, uses a stable subject with the opaque reference, contains only the seven necessary form values plus submission time/reference, and goes only to `CONTACT_ENQUIRY_EMAIL`. The visitor's email may be a reply-to only if header-injection-safe; otherwise include it in the text body. SMTP sender/recipient/envelope details and response text never enter evidence.

Persist first. Atomically reserve the idempotency hash so concurrent duplicates return the same generic accepted response/reference and never send twice. Mark a notification attempt before network delivery. A definite connection/auth/pre-envelope failure may enter bounded retry with backoff and a maximum of three attempts. A provider-accepted result becomes `sent`. An error after a potentially accepted SMTP envelope becomes `delivery_unknown` and is never automatically retried. The visitor still receives truthful `Enquiry received` only after durable storage; the response does not distinguish notification status.

Use an authenticated internal maintenance boundary for bounded notification reconciliation, 90-day purge and exact synthetic-fixture inspection/deletion. Authenticate with a random sensitive secret, return counts/status classes only, use database claiming/locking to avoid concurrency duplicates and never expose PII. Scheduled execution may use Vercel Cron only within current plan limits; absence of a preferred cron frequency is not a blocker if an equivalent safe scheduled/manual operations path and live proof exists.

## Abuse and logging contract

Generate an `ENQUIRY_ABUSE_HMAC_SECRET` and, when required, `CRON_SECRET` in process memory. Write them to Vercel through stdin/API request bodies only, mark Sensitive, never print values, and do not create or overwrite them when a correctly targeted binding already exists. Hash the platform-provided network identifier with HMAC-SHA-256 and a bounded time bucket; immediately discard the raw value. Enforce at most five accepted attempts per one-hour network bucket, with honeypot and validation failures responding generically. Rate limiting supplements rather than replaces database idempotency.

Application logs and evidence may contain only timestamp, public reference, provider class, notification status class, result class and counts. They must never contain form fields, raw request bodies, network identifiers/hashes, headers, exception messages from external libraries, SMTP response/envelope data or secret/address material.

## Encrypted binding verification

Fresh metadata-only readback must prove the exact Vercel project. The new `CONTACT_ENQUIRY_EMAIL` value is authoritative and remains unreadable. Current metadata proves an encrypted Production binding. Capture only key name, protection class, Production availability and timestamps. Do not read, update, replace, delete/recreate or export it. Public alias activation remains prohibited if Production availability later drifts or runtime validation fails.

Do not alter the value/targets of `SMTP_HOST`, `SMTP_PORT`, `SMTP_USER`, `SMTP_PASS`, `SMTP_FROM`, `ADMIN_NOTIFICATION_EMAIL`, `gmail_emails` or any Supabase/Stripe/Auth binding. The existing SMTP bindings must be complete for candidate runtime; a missing/unclassified/broken channel fails closed and does not authorize a new provider.

## Approved implementation and closeout files

Primary Product scope:

- `components/forms/trainer-enquiry-form.tsx`
- `app/page.tsx`
- `app/pricing/page.tsx`
- `app/privacy/page.tsx`
- `app/disclaimer/page.tsx`
- `app/sitemap.ts`
- `app/api/enquiries/route.ts`
- `app/api/internal/enquiries/route.ts`
- `lib/enquiries/contract.ts`
- `lib/enquiries/env.ts`
- `lib/enquiries/provider.ts`
- `lib/enquiries/server.ts`
- `supabase/migrations/0022_public_trainer_enquiries.sql`
- `.env.example`
- `vercel.json`
- `package.json`
- `package-lock.json`
- the narrowest focused 029N test/harness files under `scripts/`
- `scripts/lib/migration-ledger-validation.ps1`
- `scripts/validate-supabase-clean-rebuild-020G.ps1`
- `scripts/test-supabase-clean-rebuild-ledger-023G.ps1`
- `docs/PUBLIC_ENQUIRY_PRIVACY_AND_OPERATIONS_029N.md`

The Builder plan must reduce this candidate list to the exact files actually required. A new helper/module/test is allowed only when named in the approved plan and directly supports this contract. Closeout may update the four 029N files and the standard directly affected planning records: `planning/STATUS.json`, `planning/STATE.md`, `planning/ROADMAP.md`, `planning/ARCHITECT_BRIEFING.md`, `planning/SPRINT_LIFECYCLE_LEDGER.md`, `planning/SPRINT_SCHEDULE.md`, `planning/EVIDENCE_INDEX.md`, `planning/FINAL_PRODUCT_ACCEPTANCE_MATRIX.md`, `planning/DECISIONS.md`, `planning/RISKS.md` and `planning/QUESTIONS.md`. Preserve inherited method/template dirt.

## Migration, release and compensation authority

Apply exactly migration 0022 to approved Supabase project `uvskssaecdhxcgytkasc`. Before mutation prove linked project identity, current remote migration head 0021 and no unexpected 0022. Validate SQL locally or through equivalent semantic proof. After application, read back exact tables, RLS, grants, constraints/functions and remote migration head without row contents. No destructive rollback/drop is authorized. If later release fails, leave the additive locked-down empty/inert schema in place and record it; aliases stay/return to the accepted 036L deployment.

Freshly prove exact Vercel project `prj_6To7czLpCEGL6fInkQwE4egePPpq`, the current accepted 036L Ready deployment and all five aliases before any deployment. The only affected aliases are:

1. `precisionperformance.com.au`
2. `www.precisionperformance.com.au`
3. `pnr-precision-performance.vercel.app`
4. `pnr-precision-performance-rankin007s-projects.vercel.app`
5. `pnr-precision-performance-rankin007-rankin007s-projects.vercel.app`

Candidate order is legacy-team, team-project, project, `www`, apex. Rollback order is apex, `www`, project, team-project, legacy-team. Resolve all five independently after every assignment.

Create no more than one Production-target `--skip-domain` candidate from an exact remote-backed checkpoint. Require exact project, source SHA/branch, Ready state and immediate proof that zero aliases moved. Candidate immutable checks cover public routes, Privacy, protected redirects, API method/content/origin failures, disabled commerce and absence of client-secret leakage.

Run one synthetic valid submission through the rendered candidate with clearly synthetic data and a reserved `.invalid` email address. Prove a single sanitized row/reference and provider-accepted notification status without reading the recipient or mailbox. Replay the exact idempotency key and prove the same reference, one row and no second notification. Run invalid, unknown-key, honeypot, cross-origin and bounded rate-limit controls with separate fixture keys. Exercise deterministic definite-failure and ambiguous-outcome cases locally; do not deliberately break live SMTP credentials. Delete the exact synthetic accepted row and all synthetic abuse buckets through the authenticated operations boundary, then prove zero fixture residue without revealing payloads.

Only after all candidate and cleanup gates pass may exactly five aliases move in fixed order. On any non-target result after movement, restore all five aliases in fixed rollback order and prove five/five accepted 036L routing. Do not make a second deployment. A migration-applied but release-failed result is acceptable only as `public-enquiry-inert-rolled-back-clean`: schema locked down, no public submitting form live, no synthetic residue and aliases on 036L.

## Evidence-proportional execution

Stop only for a material wrong target, authority gap, secret/privacy exposure, unclassified/broken recipient or SMTP channel, unexpected protected data, migration partial state, unauthorized scope expansion, failed integrity/compensation, Production impact or unprovable cleanup. Substitute equivalent or stronger safe evidence when a preferred supporting tool is unavailable. Keep deterministic harness, validator, migration reporter, formatting, encoding and command-envelope corrections inside 029N. Do not create a follow-up solely for browser automation, an optional CLI path, renderer, clipboard control, schema dump or redundant proof. Manual intervention is the last safe option after process-only and equivalent mechanisms are exhausted.

============================================================
FILE: planning/sprints/029N-public-enquiry-privacy-and-submission-completion/blueprint.md
============================================================

# Sprint 029N Blueprint

## Execution sequence

1. Re-prove canonical path, exact starting SHA/current branch and inherited dirty-path classification. Dry-run/apply/reread the exact four-file Pack.
2. Builder reads the applied sprint, current form/privacy/disclaimer/Supabase boundary/migrations/tests, 029M evidence and current 036L release evidence, then returns the exact no-edit critical plan.
3. A genuinely fresh Architect reviews that complete plan under the three-decision budget. Amend applied files only for accepted within-intent Pack corrections. No implementation starts without `pass`.
4. Add failing deterministic tests for server validation, privacy copy, field optionality, acknowledgement, honeypot, origin/content-type, idempotency, rate limit, persistence-first delivery states, log redaction, retention and provider-class fail-closed behaviour.
5. Implement the contract, migration, public Privacy/disclaimer/form changes, server-only persistence/delivery/maintenance boundary and exact autonomous proof harness. Keep browser code free of all server env/service-role/mail imports.
6. Run focused tests first, then retained public/auth/role/commerce/static/JSON gates, TypeScript, zero-warning lint, Production build and source/diff/privacy/secret/encoding scans. Record exact counts and exits.
7. Obtain fresh critical inspection-ready local evidence for every pre-remote criterion and establish a scoped exact-source checkpoint commit/push excluding inherited method/template dirt.
8. Reconcile installed Vercel/Supabase CLI/API help, exact remote targets, accepted 036L deployment and five independent alias targets. Read environment metadata only.
9. Prove `CONTACT_ENQUIRY_EMAIL` remains encrypted and available to Production without reading or changing it. Create only missing random abuse/cron secrets process-only.
10. Prove the existing SMTP binding set is present. At candidate runtime classify the host through the allowlist and verify the safe provider class; never print host, address, sender, user or response.
11. Validate and apply only migration 0022 to project `uvskssaecdhxcgytkasc`; read back migration/RLS/grants/constraints/functions with no row content.
12. Create exactly one Production-target `--skip-domain` candidate from the exact checkpoint. Require exact project/source/branch, Ready and zero alias movement.
13. Run immutable candidate public/Privacy/protected/API/disabled-commerce and client-bundle secret safety.
14. Run one rendered synthetic valid submission. Prove one reference/row/provider-accepted notification class, exact duplicate replay without second row/mail, and generic invalid/unknown/honeypot/cross-origin/rate-limit controls.
15. Purge the exact synthetic accepted row and all synthetic buckets through the authenticated internal boundary; prove zero fixture residue. Do not inspect the protected recipient or mailbox.
16. Only with complete candidate/cleanup proof, assign exactly five aliases in candidate order with full independent rereads and expected `1/4` through `5/0` transitions.
17. At five/five candidate, repeat public form/Privacy/API safety smoke with a validation-only non-storing request and prove protected routes and commerce remain unchanged. Do not send a second live notification.
18. Give the applied sprint, approved plan, complete diff and evidence to a new critical inspector distinct from the plan reviewer. Resolve blocking findings through Builder re-plan and fresh review within the decision budgets.
19. Close proportionally: record exact outcome, provider/storage/retention/operations contract, migration/deployment/routing/source/evidence facts, residual mailbox-retention duty and any substitute proof. Mark only 029N done on full live acceptance.
20. Commit/push scoped closeout and reread disk, direct remote branch, migration, environment metadata, aliases, live routes and zero synthetic residue. If any post-cutover target check fails, compensate aliases first and close only a truthful rolled-back-clean outcome.

## Architecture

```text
Browser form
  -> POST /api/enquiries
     -> body/origin/honeypot/consent validation
     -> HMAC abuse bucket + idempotency reservation
     -> service-role-only Supabase insert (90-day expiry)
     -> existing encrypted SMTP notification
     -> generic stored receipt + opaque reference

Internal authenticated maintenance
  -> claim definite retryable notifications safely
  -> never retry delivery_unknown
  -> purge expired enquiries/buckets
  -> inspect/delete exact synthetic reference using sanitized status/counts
```

Supabase is the durable enquiry source of truth. The browser form state is transient. The API normalizes and validates it, derives only an HMAC abuse/idempotency value, stores the governed record and converts delivery outcomes into a finite sanitized status. SMTP is a notification sink, never the source of truth. The opaque reference connects visitor receipt, operator requests and sanitized proof without exposing database IDs or personal fields.

## Failure and compensation matrix

| Boundary | Required result | Non-target handling |
| --- | --- | --- |
| Recipient metadata | Same encrypted binding remains encrypted and available to Production | No value read/re-entry/fallback; prohibit activation and record precise intervention only after safe alternatives fail |
| SMTP classification | Complete bindings, allowlisted provider class | Fail closed; no new provider/account and no public activation |
| Database insert | Durable unique row/reference | No email attempt; generic retry-later, no payload log |
| Duplicate | Same idempotency hash/reference | Return same generic accepted result; no new row/mail |
| SMTP definite pre-accept failure | Sanitized retryable state | Bounded claim/backoff, maximum three attempts |
| SMTP accepted | `sent` | Receipt remains storage-truthful; no inbox/open claim |
| SMTP ambiguous | `delivery_unknown` | Never automatic retry; surface only to restricted operations evidence |
| Migration partial/failure | Exact remote state known | Stop external progression; no destructive drop; aliases unchanged |
| Candidate failure before aliases | Accepted 036L remains five/five | Purge fixtures; no second deployment |
| Failure after alias movement | Exact five/five 036L rollback | Fixed-order compensation, full rereads, then cleanup/closeout |
| Fixture cleanup failure | Zero synthetic residue unproved | Material blocker; do not cut over/close as clean |

## Flight evidence

### 1. Class and reason

`critical`: public personal data crosses browser, server, Supabase and SMTP boundaries; remote migration, encrypted configuration, external delivery and Production routing are included.

### 2. Invariant

No real or synthetic enquiry is accepted unless it is minimally collected with notice/acknowledgement, stored once behind service-role-only controls with expiry, notified only to the protected recipient through an allowlisted existing provider, never exposed in logs/client/evidence, and exactly removable. Production aliases move only after complete candidate and cleanup proof.

### 3. Origin-to-sink and failure paths

Origin: seven public form values, unchecked acknowledgement, honeypot and client UUID. Transformations: client trim/validation; server strict schema; normalization; HMAC network/idempotency derivation; opaque reference; finite delivery-state mapping. Durable sink: locked-down Supabase enquiry row and short-lived abuse bucket. External sink: one text-only SMTP notification to the encrypted recipient. Failure path: validation/origin/abuse rejection stores/sends nothing; database failure sends nothing; definite SMTP failure becomes bounded retryable state; ambiguous SMTP outcome is not retried; deployment failure keeps/restores 036L; exact fixtures are purged before cutover.

### 4. Source of truth, transformations and sink

Applied sprint files govern scope. Git governs source. Supabase project `uvskssaecdhxcgytkasc` governs persisted enquiry state. Vercel project `prj_6To7czLpCEGL6fInkQwE4egePPpq` governs bindings/deployment/aliases. The API is the only transformation boundary. SMTP is notification-only and is never used to reconstruct enquiry state.

### 5. Discriminating examples

- Two concurrent posts with the same client UUID must produce one row, one reference and at most one notification; two rows or two accepted envelopes fails.
- A stored row plus definite SMTP connection failure may be retried; a provider-accepted or ambiguous envelope may not be automatically resent.
- A caller-controlled `.invalid` email in the submitted form is fixture data; the only envelope recipient remains the hidden configured Aprec8 address.
- An `Origin` for another host, a filled honeypot, an unknown JSON key or horse volume `0` must store/send nothing and reveal no rejection detail useful to abuse.
- `anon`/`authenticated` SQL grants or a browser import of server modules fails even if UI tests pass.
- One remaining synthetic row, abuse bucket, payload log or mailbox/address disclosure fails cleanup/privacy acceptance even if the page renders successfully.

### 6. Durable Git source

Start `bc2cc029ef4251d92bb7f46e59d18f32033230f6`; branch `codex/029N-public-enquiry-privacy-and-submission-completion`; exact checkpoint and closeout SHAs must be pushed to the same remote branch with direct-remote equality. Inherited method/template dirt is excluded.

### 7. Uncertainties and blockers

- The protected recipient now has verified encrypted Production metadata. Any later absence or runtime invalidity fails closed; it does not authorize value inspection or fallback guessing.
- SMTP host/provider is hidden. Candidate runtime must map it to an allowlisted disclosure class and pass provider verification; an unknown/broken class fails closed.
- Provider acceptance proves handoff to SMTP, not inbox placement or reading. This is an explicit evidence limit, not a reason to ask for mailbox codes.
- Mailbox deletion within 90 days is an operational recipient duty; database retention is automated and independently proven.
- The Vercel plan may limit cron frequency. Equivalent safe bounded maintenance is acceptable when the required retention/retry facts are still executable and documented.

## Plan-review ledger

Use stable IDs `PLAN-001` onward. Critical plan decision budget is three total, including the initial decision. A correction that changes privacy, schema, delivery semantics, remote targets, deployment count or file scope requires full re-plan and fresh review. Formatting/reporting/tool substitutions that preserve the contract may be recorded without inventing a new sprint.

- Decision 1/3: `pass`; no finding. Accepted the within-intent Pricing copy file and Production-only recipient wording corrections.
- Decision 2/3: `PLAN-002` `fix`; the migration-ledger helper amendment omitted its coupled reporter and adversarial test.
- Decision 3/3: `pass`; `PLAN-002` resolved by adding exactly the helper, reporter and adversarial test with local ledger `0001-0022`, future rejection `0023`, preserved remote-not-inspected semantics and no Product/remote behaviour change.

## Inspection ledger

Use stable IDs `INSPECT-001` onward. The inspector is new and distinct from the plan reviewer and Builder. Critical inspection decision budget is three total. Any blocking finding returns to Builder re-plan and fresh Architect review before more implementation. No blocking item may be waived by prose.

============================================================
FILE: planning/sprints/029N-public-enquiry-privacy-and-submission-completion/acceptance.md
============================================================

# Sprint 029N Acceptance

## Canonical, Pack and plan

- [ ] **A001** Canonical CWD/top-level, exact start SHA/branch and inherited dirty-path exclusions are proved before Pack application and every mutation phase.
- [ ] **A002** Pack dry-run reports exactly the four traversal-free 029N destinations; application succeeds and all four files are reread as authority.
- [ ] **A003** Builder returns the complete no-edit task contract/file/guard/A001-A040/command/baseline/closeout/Pack-corrections plan.
- [ ] **A004** A genuinely fresh critical Architect returns `pass` within the three-decision budget before implementation.

## Public collection and privacy

- [ ] **A005** The form collects only the seven approved concepts; trainer/stable/phone/email/horse volume are required, stable address/referred-by are clearly optional and acknowledgement starts unchecked.
- [ ] **A006** Client and server enforce the exact normalization, length/range, unknown-key, body-size, content-type and control-character contract; server results never echo payloads.
- [ ] **A007** Form, errors, acknowledgement, Privacy link, progress and success/failure messages are keyboard, focus, label, live-region, contrast, zoom and screen-reader accessible.
- [ ] **A008** `/privacy` contains the complete collector, purpose, consequence, categories, optionality, no-health/no-marketing, recipient/access, Singapore/provider processing, retention, abuse-token and request-process notice.
- [ ] **A009** Public disclaimer/home copy no longer claims the accepted form does not transmit/store and does not overclaim notification/inbox delivery.
- [ ] **A010** Public reference is opaque and safe; success says `Enquiry received` only after durable storage and exposes no delivery state or submitted field.

## Data and server boundary

- [ ] **A011** Migration 0022 is additive, constrained and idempotent enough for one controlled application; prior migrations/source contracts remain unchanged.
- [ ] **A012** All enquiry/abuse tables have RLS enabled, no browser policies, explicit revokes from `public`/`anon`/`authenticated`, least service-role grants and safe service-only SQL functions.
- [ ] **A013** No raw IP, user agent, idempotency key, provider host/response, exception text, secret or SMTP envelope is stored; abuse HMAC rows expire within 24 hours.
- [ ] **A014** Enquiry rows expire at 90 days and maintenance deletes expired rows without content logging; mailbox 90-day responsibility is documented.
- [ ] **A015** Browser/client bundles cannot import or receive the service role, SMTP/binding modules or any server env value; all persistence uses the server-only admin boundary.
- [ ] **A016** Atomic uniqueness/claiming makes concurrent duplicate storage and notification impossible under the tested idempotency contract.

## Delivery and failure behaviour

- [ ] **A017** Exact existing SMTP bindings are presence/syntax checked without value output; protected host maps to one allowlisted public provider class or form activation fails closed.
- [ ] **A018** Notification is plain text, single-recipient, necessary-fields-only, stable-reference subject, no HTML/attachments/tracking, and header-injection safe.
- [ ] **A019** Database failure sends nothing; durable insert precedes mail; accepted SMTP becomes `sent`; definite pre-accept failure is bounded retryable; ambiguous delivery is never automatically retried.
- [ ] **A020** Duplicate replay returns the same reference and creates one row/at-most-one SMTP acceptance; retry claim/attempt ceilings and concurrency are executable.
- [ ] **A021** Honeypot, same-origin/host, JSON content, validation and five-per-hour HMAC bucket controls store/send nothing on rejection and return generic bounded responses.
- [ ] **A022** Application/test/evidence output contains only allowed reference/provider/status/result/count classes and passes scans excluding PII, addresses, network hashes, SMTP response/envelope and secret material.

## Encrypted configuration and remote schema

- [ ] **A023** Metadata-only proof identifies exact Vercel project and `CONTACT_ENQUIRY_EMAIL` without displaying its value/address.
- [ ] **A024** The protected recipient remains encrypted and available to Production with no value read/update/re-entry/replacement/delete; any drift stops activation with no guessed fallback.
- [ ] **A025** Only absent abuse/cron secrets are generated process-only and written Sensitive without value output; no SMTP/Supabase/Stripe/Auth or unrelated env binding changes.
- [ ] **A026** Fresh Supabase proof identifies project `uvskssaecdhxcgytkasc`, remote head 0021 and no unexpected 0022 before mutation.
- [ ] **A027** Exactly migration 0022 applies once; remote readback proves migration head, tables, RLS, grants, constraints/functions and zero pre-existing enquiry rows without row-content output.

## Local and source proof

- [ ] **A028** Focused tests prove valid/invalid schema, privacy copy, acknowledgement, idempotency/concurrency, abuse, persistence-first delivery matrix, provider fail-closed, maintenance/retention, log redaction and exact fixture deletion with recorded assertion arithmetic.
- [ ] **A029** Retained public 029M/Australian-English, auth/role/dashboard, static/JSON and disabled-commerce gates pass with exact commands/exits.
- [ ] **A030** TypeScript, zero-warning lint, Production build and diff/encoding/JSON/secret/privacy/client-boundary scans pass.
- [ ] **A031** Exact checkpoint source is scoped, excludes inherited method/template dirt, is committed/pushed to the 029N branch and matches direct remote SHA.

## Candidate, live evidence and routing

- [ ] **A032** Fresh Vercel proof identifies exact project, accepted Ready 036L rollback and five/five independent alias baseline before deployment.
- [ ] **A033** Exactly one Production-target `--skip-domain` candidate is exact-project/source/branch, Ready and zero/five aliases immediately after creation.
- [ ] **A034** Immutable candidate public/Home/Pricing/Privacy/Disclaimer/protected/API/disabled-commerce/client-secret safety passes before any submitting live proof or alias movement.
- [ ] **A035** One rendered synthetic valid request produces one opaque reference, one sanitized row and one provider-accepted notification status to the hidden recipient; no mailbox read/code is used.
- [ ] **A036** Exact live duplicate replay and invalid/unknown/honeypot/cross-origin/rate-limit controls prove correct row/mail/no-store arithmetic without payload/address/secret exposure.
- [ ] **A037** Exact synthetic enquiry and related abuse buckets are deleted; sanitized remote readback proves zero fixture residue before cutover.
- [ ] **A038** Exactly five assignments in fixed order reach five/five candidate with independent `1/4` through `5/0` rereads; any non-target outcome restores five/five accepted 036L and no second deployment occurs.

## Inspection and closeout

- [ ] **A039** A new fresh critical inspector, distinct from Builder and plan reviewer, judges every applicable A001-A040 item against the approved plan, full diff and real evidence within the three-decision budget; blocking findings are resolved through re-plan/review.
- [ ] **A040** Durable records state the exact result/evidence/limitations, only 029N becomes `done` on full acceptance, STATUS is `sprint-closed`, scoped closeout commit/push has direct-remote equality, final readback proves live/rolled-back routing and zero synthetic residue.

## Permitted outcomes

- `public-enquiry-live-accepted-clean`: A001-A040 pass; one exact candidate is live five/five; migration/bindings/retention/delivery controls are accepted; zero synthetic residue.
- `public-enquiry-inert-rolled-back-clean`: a material post-migration boundary prevented activation; locked-down additive schema may remain, submitting UI is not live, five aliases are on accepted 036L, zero synthetic residue, and the exact blocker is recorded without claiming completion.

No other outcome may be described as clean or complete.

============================================================
FILE: planning/sprints/029N-public-enquiry-privacy-and-submission-completion/handoff-prompt.md
============================================================

# Sprint 029N Builder Handoff

Build only from these applied Sprint 029N files in the permanent canonical repository.

## Task contract

**objective:** Activate one privacy-governed public trainer-enquiry path that stores each accepted request once, notifies the single protected recipient through the existing SMTP channel, provides a truthful opaque receipt, and reaches Production only after exact candidate proof and clean synthetic-data removal.

**owns:** Canonical/Pack/source proof; public form/Privacy/disclaimer; one additive locked-down migration; server-only storage/email/maintenance; focused tests and autonomous proof; metadata-only preservation/proof of the encrypted Production recipient; process-only missing-secret creation; exact SMTP provider classification; one migration, checkpoint, Production candidate and fixed alias transaction; synthetic proof/purge; fresh reviews; proportional closeout and scoped publication.

**must_not:** Ask for or expose addresses, passwords, codes, keys, form payloads, raw network data, hashes, provider host/responses or protected identity; guess recipient/fallback; replace/delete the new encrypted recipient; add providers/marketing/analytics/CAPTCHA/commerce/onboarding/medical collection; expose tables to clients; alter prior migrations/Auth/RLS/Storage/Stripe/trainer graph/036K; send to another recipient; make a second deployment; stage inherited method dirt; merge/PR/develop/force-push/rewrite.

**acceptance:** The accessible seven-field form and complete Privacy notice pass; service-only Supabase storage, 90-day retention, HMAC abuse controls, idempotent persistence-first notification and safe failure states pass; the same encrypted recipient targets Production without disclosure; one exact-source candidate proves one synthetic provider-accepted notification, duplicate/negative controls and zero residue; five aliases move only on complete success or restore to 036L.

**verification:** A001-A040; canonical/Git/Pack checks; exact plan and fresh critical review; migration/tests/full local gates; metadata/provider/source/project/candidate/alias proof; live synthetic/duplicate/negative/cleanup evidence; distinct fresh inspection; closeout commit/push and final disk/direct-remote/remote-state readback.

## Builder planning gate

Before editing implementation, read the applied folder and current source yourself. Return:

- the five-field task contract;
- the exact implementation file list, reducing the approved candidate list to what is necessary;
- scope, secret, privacy, migration, deployment, alias, fixture and compensation guards;
- every A001-A040 criterion mapped to work and evidence;
- exact verification commands, including safe remote projections;
- observed test baselines and target assertion arithmetic;
- standard closeout files/actions after inspection;
- `Pack corrections` (`None` is valid), each naming the applied file and exact contradiction; and
- any blocking ambiguity.

Do not edit implementation while producing this plan. The fresh Architect must pass the exact plan before implementation begins.

## Execution rules

Trust git and executable sanitized output over prose. Keep protected values in process memory and provider-managed encrypted bindings. Use stdin or direct API request bodies for generated secrets, finite allowlisted output, and `finally` cleanup. No person enters a code or secret. Never read/decrypt the recipient merely to prove it; target/type/runtime acceptance are sufficient.

Persist before email. Test ambiguous and definite delivery semantics deterministically without deliberately damaging live SMTP credentials. A provider-accepted synthetic notification is enough; do not ask for mailbox access or an inbox code. The live fixture uses obviously synthetic values and a reserved `.invalid` submitted email, then is deleted exactly before cutover.

Apply the Evidence-Proportional Execution Standard. Stop only for a material target, authority, security/privacy, provider classification, migration, scope, Production integrity, compensation or cleanup risk. Use equivalent or stronger safe proof for supporting-tool failures. Keep deterministic harness/validator/reporter/encoding corrections in this sprint. Manual intervention is last, and no follow-up is created merely for an optional tool limitation.

After real checks, give the applied sprint, approved plan, complete diff and exact evidence to a new critical inspector distinct from the plan reviewer. Only inspection `pass` permits closeout. Then refresh the required durable records, change only the 029N roadmap row to `done` on full live acceptance, set STATUS `sprint-closed`, publish only scoped 029N work and reread the landing from disk and direct remote.
