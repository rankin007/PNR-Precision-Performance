# Project State

## Sprint 035C active — 2026-07-30

Sprint `035C-trainer-participation-and-final-mvp-acceptance` is active on branch `codex/035C-trainer-participation-and-final-mvp-acceptance` from exact clean Sprint 035B local/remote SHA `497c8628c2fe481dcf2ca1d205f379e311b3df75`. The product-owner/provider-operator confirms protected inbox readiness and availability for Trainer Participants A, B and C. Sprint 035B remains closed `trainer-pilot-participation-partial-clean`; historical outcomes are unchanged.

The initial exact Preview `dpl_3YuZ36Jh8h6U5CgCGw9NQzpDTDfy` at candidate `aa0cf5ddb598a73056677e030ec6a39141764cb9` was Ready/Preview and alias-free before the Participant A redirect finding. Its callback has now been retired. The accepted protected matcher uses only administrator-controlled `app_metadata` field names `participant_alias` and `pilot_sprint`; it is not authorization authority.

Participant A is now classified as a Sprint-035C-owned confirmed Auth identity; B and C were not started. Current owned creation is application/Auth/Storage `0/1/0`. Direct Preview origin and callback route checks passed, and the exact callback configuration is correct. The Dashboard invitation path cannot set `redirectTo` and is rejected. The diagnosed defect is the Preview passwordless action's localhost fallback when no site URL variable exists; an in-scope trusted-origin correction and one-participant hidden-input app-metadata helper are under validation before any resend, B/C action or application fixture creation.

Correction commit `07edea49153fb557a2afc2a066f96c30a102e4c7` is pushed on the scoped branch. Exact generated deployment `dpl_G8StMkGqfTmUsNMpAfNUCwontC42` is Ready/Preview, alias-free and built successfully from that exact clean archive at `https://pnr-precision-performance-n7xbelwg8-rankin007s-projects.vercel.app`. Origin returned `200`; artifact-free callback returned `307` to the same Preview `/portal`. The old Preview callback was replaced with this exact callback; the production Site URL and callback remain unchanged. Protected Participant A app-metadata tagging and authenticated session proof are the next one-participant gate.

An operator-side manual secret conversion attempt was safely terminated before the helper ran; no inbox, app-metadata or remote mutation occurred. A Sprint-owned guarded PowerShell wrapper and no-secret static self-test are now under final validation. Participant A remains owned-confirmed-untagged/session-unproven and owned application/Auth/Storage remains `0/1/0`; B/C remain not started.

Participant execution is now stopped. A Preview passwordless request redirected to the production root with a code query instead of the Preview callback, and that code was disclosed outside the protected path. The value is treated as compromised and will not be inspected or exchanged. No portal acceptance is claimed. B/C remain not started while Builder performs sanitized session/application containment, exact-owned A cleanup and redirect correction.

The first A-only containment attempt safely returned `OWNERSHIP_AMBIGUOUS` before mutation because its guard incorrectly required one total project Auth identity. The child secret environment ended and the parent service-role environment is absent. Owned state remains `0/1/0`. Builder corrected the guard to exact private-inbox match plus recorded event window, untagged/no-conflict metadata, zero access, protected ownership ledger, Auth-last deletion and unrelated-identity preservation; focused no-secret validation is in progress.

Participant A containment passed. Sanitized provider evidence records a prior sign-in indicator, no production callback processing, no application records removed, active session state revoked by exact-owned Auth deletion, and owned application/Auth/Storage `0/0/0`. The protected ledger independently records A `contained-owned-deleted` and Sprint-owned, with no B/C entries; the helper's post-delete guards proved exact A Auth/application absence and unchanged unrelated identities. Parent service-role environment is absent. No compromised artifact is retained or reconstructed, and no acceptance is claimed.

Fresh exact-source Preview `dpl_CGtFBxpQakJHi3x41hzcgpvmDL3A` at candidate `5b55aea39abcb45db06a42312d3ff9a7f293f3f9` is Ready/Preview and alias-free. Vercel production build passed; origin returned `200`; artifact-free callback returned `307` to the same Preview `/portal`; five stable aliases remain unchanged. The new exact Preview callback must replace the old temporary Preview callback before a new A-only handoff. No identity or passwordless message has been created.

Callback replacement passed. Supabase project `uvskssaecdhxcgytkasc` is `Precision Performance Clean Rebuild`, `ap-southeast-1`, `ACTIVE_HEALTHY`, with exact local/remote ledger `0001`–`0021`. Auth URL configuration now contains exactly the unchanged production callback and `https://pnr-precision-performance-ge5io3pa3-rankin007s-projects.vercel.app/auth/callback`; the stale callback and wildcards are absent, and production Site URL is unchanged. Post-change Preview/route/alias proof remains green; owned state remains `0/0/0`, A absent, B/C untouched.

A fresh A-only passwordless request was issued from the corrected Preview, creating at most the single expected fresh owned Auth identity; the message remains unopened and no session/application bootstrap occurred. Apply returned `ALIAS_ALREADY_PROCESSED_A` before hidden inbox input and metadata mutation because the helper unconditionally rejected the required prior `contained-owned-deleted` ledger entry. Parent service-role environment is absent; ledger remains contained-owned-deleted; current owned state is `0/1/0`; B/C remain untouched. A deterministic guard correction is under validation.

The corrected Apply subsequently passed with sanitized ownership `sprint-owned`; the protected ledger now contains only A as `existing-tagged`/`sprint-owned`. A passwordless request was correctly initiated on the exact Preview (`/sign-in`, sent, next `/portal`), but authentication was not established. Later use of an old, expired, consumed or superseded message landed at production root with sanitized `otp_expired`; no code query was visible and all browser windows were closed. B/C remain untouched and no participant acceptance is claimed. A read-only protected verification mode now gates any single fresh-message retry on exactly one A/035C tag, the Sprint-owned ledger, no provider sign-in indicator and zero application/access state.

Protected verification passed: tagged Auth `1`, ownership `sprint-owned`, sign-in indicator not observed, application/Storage `0/0`, owned counts `0/1/0`, decision `fresh-message-retry-eligible`. Pre-retry external verification reconfirmed Preview `dpl_CGtFBxpQakJHi3x41hzcgpvmDL3A` Ready at exact source `5b55aea39abcb45db06a42312d3ff9a7f293f3f9`, Preview environment, assigned aliases `0`; Auth URL configuration contains exactly the unchanged production callback and exact `ge5io3pa3` Preview callback, with no stale/wildcard entry and unchanged production Site URL. Production remains Ready at its accepted deployment/source with the same five aliases. Forwarded-host/protocol structural proof passes, and an artifact-free callback remained same-origin and returned to the `/portal` sign-in boundary. One controlled A-only fresh-message retry is authorized; B/C remain untouched and acceptance is not claimed.

The controlled retry failed: both displayed actions landed at sanitized production root with no authenticated session and `otp_expired`. Execution is stopped and A acceptance remains unclaimed. Provider inspection proves the active Magic Link and Confirm sign up templates are separate and each has exactly one `ConfirmationURL` action; email confirmation and new-user signup are enabled. Root cause is the application omitting `shouldCreateUser: false`, allowing the first passwordless request to auto-create A through the confirmation/signup branch, combined with one-time `ConfirmationURL` prefetch/consumption or supersession documented by Supabase. The production-root error is the provider Site URL fallback after an invalid one-time verification, not a successful Preview callback. Source now restricts portal OTP requests to existing identities and fails callback error parameters closed before code exchange. The prior protected pass plus the failed no-session/no-callback outcome, unchanged Sprint-owned ledger and absence of any application/Storage mutation retain sanitized state `0/1/0`; containment was not required. A further retry is not authorized until Architect review, a fresh exact-source Preview, callback rotation and confirmation-state-safe protected preparation.

Plus-address routing into a base mailbox is expected delivery behavior and is not classified as a redirect root cause. Protected matching normalizes only surrounding whitespace and case; it preserves the complete local part including `+tag` and requires exact equality. Deterministic tests prove the full plus identity matches case-insensitively while the base address and other plus aliases remain distinct. No participant inbox, header or message artifact is recorded. Any future protected run must confirm exact-identity matching and privately confirm the newest message targets that identity; participant retry remains prohibited until the complete synthetic Preview journey passes.

Architect accepted candidate `d9c020f2d09df5b59ce1a5e589bd68d8145cb1d0`. Fresh Preview `dpl_kKRfUc4Sy9zL4cw32rFm8C6DD27r` is Ready, Preview environment, exact `ppSourceSha`, assigned aliases `0`, and its production build passed. The old `ge5io3pa3` temporary callback was removed and replaced with the exact new `c16cgcork` callback. Sanitized Auth URL configuration contains exactly unchanged production plus the new Preview callback; old/stale/wildcard entries are absent and production Site URL is unchanged. Artifact-free callback stayed on the new Preview and returned to `/sign-in?next=%2Fportal`; production deployment and five stable aliases remain unchanged.

Participant and synthetic execution remain stopped at a material provider boundary. Builder process has no protected service-role value, so the requested post-deployment guarded A Admin verification cannot be rerun without prohibited operator intervention; the last accepted sanitized A state remains `0/1/0`, tagged/Sprint-owned, with no observed sign-in indicator or application/Storage state, but is not overstated as a new query. No controlled synthetic inbox is available. More importantly, the shared Magic Link template still uses one-time `ConfirmationURL`, which Supabase documents as consumable by ordinary mail-security prefetch. Source assertions or Admin-generated links cannot satisfy the delivered-flow requirement. Prefetch resistance requires an OTP or two-step/interstitial template design and therefore a shared provider-template mutation with possible production impact. Per Architect boundary, no template mutation, synthetic message, participant message or A authorization occurred; provider design review is required.

## Sprint 035B closeout — 2026-07-30

Sprint `035B-preview-configuration-and-trainer-pilot-completion` is closed `trainer-pilot-participation-partial-clean`. Exact-candidate Preview deployment `dpl_HDtvZnnz9osHuyQ7zW7WX1w1mpWE` at SHA `dedc19001acd9229d435718e51ceabbbdd208860` passed the authenticated passwordless synthetic matrix on phone, desktop and tablet. Exact-owned application/Auth/Storage cleanup is `0/0/0`, and the temporary Preview callback is removed. Trainer Participants A, B and C did not start because no protected inbox/provider-operator coordination path was available. Trainer acceptance is not claimed, production remains unchanged and product-wide Done is not met.

## Sprint 035 closeout — 2026-07-30

Sprint `035-trainer-pilot-and-dashboard-mvp` is closed `trainer-dashboard-validation-blocked-clean` on branch `codex/035-trainer-pilot-and-dashboard-mvp`, created from exact baseline `d949069834c375ce4b485a4336eb701944b2d984`. The trainer dashboard and focused horse workspace are locally implemented; sample fallbacks are removed; focused, retained regression, canonical validation and production builds pass.

Authenticated rendered dashboard proof and trainer participation did not run. Required branch-scoped Preview Supabase values were empty and existing Ready Preview metadata did not prove exact Sprint 035 SHA identity. Participant consent was not privately confirmed. No account, fixture, Storage object, callback, provider or production mutation occurred; trainer acceptance is not claimed.

The horse-detail composition now isolates biochemistry-query failure from horse-access success. An accessible horse with failed workflow loading renders sanitized `Unavailable`, no normal/no-result inference and no record action; inaccessible, cross-stable and revoked horses retain identity-free denial. Focused and full canonical validation plus production build pass. Preview and trainer acceptance remain outstanding.

The scoped Sprint 035 branch was backed up remotely after an SSH authentication failure was safely substituted with the existing authenticated GitHub HTTPS credential. Tip `12e3f81b3d1b9fb84a5385372b4e1b5a719b0c74` was intermediate publication evidence; the final closed Sprint 035 local/remote SHA is `57bfca225a0a41f639b9fa7b0875589bde9372f1`. No PR, merge or `develop` push occurred.

## Current baseline — 2026-07-30

Sprint `034-repository-reconciliation-and-project-simplification` is closed `reconciled-product-baseline-and-project-simplification-complete-clean` on branch `codex/034-reconciled-product-baseline`. The baseline starts from exact accepted Sprint 032 release SHA `f7242ee0785ae9b87022394206c89ebdd5c9f6ad`, whose ancestry includes accepted Sprint 021AH authenticated application work and Sprint 022/022B mobile biochemistry workflow work. The release migration ledger through `0021` is preserved unchanged, including the required accepted auth/mobile boundary `0001`–`0017`; current application source, focused tests, Sprint 033 final acceptance and operational handoff authorities are preserved.

The public release remains valid at deployment `dpl_fPWqinnfL4YZJq41MQPaXhhuh7hi`; rollback target is `dpl_6F1TMjNRECmTCyMbyWXA6ohG8Q2R`. Sprint 034 did not deploy or mutate production. Product-wide Done is not met: clinical authority, audio, upload acceptance, trends, timed real-device acceptance and complete operational ownership remain deferred, unproven or not done.

## Repository truth

- Governing product baseline: `codex/034-reconciled-product-baseline`.
- Reconciliation commit: `aeb24d2d038f9875973764b25538caaea6473d02`; planning closeout and first verified remote tip: `aa87dfe010ca1ae900f0ce633ee7b2fad2a076bf`.
- Source release: `codex/032-public-relaunch-production` at `f7242ee`.
- Local `develop` (`b8961b9`) and `origin/develop` (`502b45a`) remain separate historical/divergent lineages and were not merged, rebased or pushed.
- The original dirty root remains untouched except for applying the Sprint 034 Pack; its uncommitted material is classified in the Sprint 034 reconciliation ledger.
- No uncertain historical file was deleted or moved. Discoverability was improved through current indexes and lifecycle classification.

## Active product outcome

Sprint 035B is closed partial-clean. The next product sprint requires Architect selection and should retain trainer-visible participation as outstanding acceptance. Voice, OCR, transactional commerce, sophisticated saved views and broad public enhancements remain deferred.

## Governing authorities

- `planning/FINAL_PRODUCT_ACCEPTANCE_MATRIX.md`
- `planning/DEFERRED_SCOPE_AND_OWNERSHIP.md`
- `docs/OPERATIONS_HANDOFF.md`
- `planning/EVIDENCE_INDEX.md`
- `planning/SPRINT_LIFECYCLE_LEDGER.md`
- `planning/reviews/034-repository-reconciliation-ledger.md`
- `planning/reviews/034-repository-reconciliation-and-project-simplification.md`
