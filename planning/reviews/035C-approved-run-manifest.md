# Sprint 035C Approved Run Manifest

- Branch: `codex/035C-trainer-participation-and-final-mvp-acceptance`.
- Baseline: exact clean local/remote Sprint 035B SHA `497c8628c2fe481dcf2ca1d205f379e311b3df75`.
- Vercel: `pnr-precision-performance`, generated Preview only, deployed with explicit no-domain assignment.
- Supabase: `uvskssaecdhxcgytkasc`, `Precision Performance Clean Rebuild`, Singapore `ap-southeast-1`, migrations `0001`–`0021`.
- Run ID: `035C-20260730-01`.
- Ceiling: one synthetic stable, three horses, three participant-linked application identities, minimum existing trainer memberships/assignments and minimum biochemistry fixtures; Storage zero.
- Participants: Trainer Participant A, Trainer Participant B and Trainer Participant C only in durable evidence.
- Authentication: existing passwordless OTP/magic-link flow only; provider operator retains all protected inbox and authentication artifacts.
- Approved product paths: none expected unless a material participant finding remains inside the established Sprint 035 contract.
- Approved durable paths: applied Sprint file, this manifest, current state/status/schedule/evidence/briefing, narrow participant/cleanup evidence, and deterministic harness/reporter corrections.
- Cleanup: application records first, owned Auth last, callback removed, mappings/sessions cleared, final owned application/Auth/Storage `0/0/0` or pre-existing Auth explicitly excluded.
- Production, old provider, schema, migrations, RPCs, RLS, roles, permissions, clinical scope, Storage, commerce and public pages are prohibited.

Product-owner/provider-operator readiness is confirmed through sanitized status: three protected participant inboxes are configured and Participants A, B and C are available. No protected mapping is stored here.

## Protected matching correction

Process-memory-only matching could not bridge the operator handoff without Builder receiving protected inbox or Auth identity data. The accepted narrow bridge is administrator-controlled Supabase Auth `app_metadata`, which is safer than user-controlled `user_metadata`. Only `participant_alias` (`A`, `B`, `C`) and `pilot_sprint` (`035C`) may be added, always by merging and preserving every existing `app_metadata` key.

The fields are matching labels only. No application authorization, permission or RLS decision may rely on them; existing memberships, assignments, permissions and RLS remain authoritative. Protected matching moves into process memory after discovery. Cleanup deletes Sprint-035C-owned Auth identities Auth-last or removes only these two Sprint fields from pre-existing identities while preserving all unrelated metadata.

Current verified handoff state: Preview `dpl_3YuZ36Jh8h6U5CgCGw9NQzpDTDfy`, exact candidate `aa0cf5ddb598a73056677e030ec6a39141764cb9`, Ready/Preview, zero aliases; exact callback temporarily present; participant-account handoff pending; Sprint-owned application/Auth/Storage creation `0/0/0`.

Redirect decision: the Dashboard invitation dialog cannot specify a redirect, and the current Preview sign-in action cannot guarantee this callback under the approved three-variable configuration. Operator handoff must use the bounded trusted-server helper with the exact approved callback; Dashboard invitation sending is prohibited for this run.
