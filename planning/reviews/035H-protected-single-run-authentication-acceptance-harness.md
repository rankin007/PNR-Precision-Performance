# Sprint 035H Protected Single-Run Authentication Acceptance Harness

## Outcome

Closed `protected-mailbox-automation-authority-pending-clean` on 2026-08-01.

The four-file Architect Pack was applied from exact closed 035G SHA `bfd4fc494d68a0f53d6c0f5f40c8fd4c8bec7059` in the dedicated worktree and generated exactly the required four sprint files. Branch creation and all work occurred only on `codex/035H-protected-single-run-authentication-acceptance-harness`.

## Sanitized architecture and readiness

Added a pure lifecycle controller, strict recovery-safe ledger schema, permanent send/verification reservations, mailbox message classifier, trim-only six-character recovered-code contract, allowlisted reporter, guarded top-level runner and protected-console operator entry. Live mode remains fail-closed because no supported least-privilege read-only OAuth/API mailbox adapter backed by an approved OS/provider secure store is established.

No manual mailbox inspection, code copy, browser handoff or agent-visible protected value was used. No-send readiness recorded request/verification counts `0/0`, mailbox `authority-pending`, authentication `not-attempted`, session `not-established`, permission `not-tested`, exact-owned application/Auth/Storage `0/0/0` by zero-action continuity and ledger absent.

No preparation, email request, OTP acquisition, verification, session creation, application bootstrap or cleanup mutation occurred. The prior three preserved identities, Participants A/B/C, approved project, correction Preview, production, rollback, five aliases, callback set, Resend/sender/template and OTP/rate classifications were not contacted or mutated.

## Deterministic evidence

The focused 035H suite passed 32 checks covering state transitions, permanent reservation semantics, cleanup-only restart, wrong/old/base/other-alias/sender/ambiguous/linked/invitation/malformed mailbox content, leading-zero preservation, exact plus-address handling and protected report refusal.

Inherited OTP/recovery, protected preparation recovery, wrapper/no-secret, bootstrap concurrency and trainer dashboard tests passed. JSON validation, lint, typecheck, encoding and the complete canonical static suite passed. No package dependency was added. No live or remote validation was run because the pre-send mailbox authority gate failed closed.

## Manual intervention boundary

Provider classification supplied at closeout is Gmail. This classification did not authorize mailbox access, OAuth setup, credential creation, token storage, message inspection, identity preparation, email sending or OTP activity.

Blocked fact: the repository and protected operating context do not provide an approved Gmail-specific least-privilege read-only mailbox adapter with long-lived authorization in an approved secure store.

Evidence checked: the mailbox runbook and 035F/035G reviews describe private/manual mailbox confirmation, while Sprint 035H expressly prohibits that mechanism. The readiness runner independently reports `authority-pending` before preparation or send.

Required future private action: an authorized operator must select and establish the dedicated test mailbox's supported read-only OAuth/API integration, store long-lived authorization only in Windows Credential Manager or the provider's equivalent secure store, verify plus-alias delivery privately, and expose only the adapter readiness classification to the runner. No address, token, message, code or credential may enter arguments, environment files, repository files, output or conversation.

Builder verification after that action: review the concrete adapter and scope, extend deterministic provider-fixture tests, confirm secure-store/ACL and protected-output gates, rerun all no-send readiness checks, and only then consider the single authorized live run. A manual mailbox workflow is not an alternative.

No commit or push occurred. Trainer-pilot completion and product-wide Done are not claimed.
