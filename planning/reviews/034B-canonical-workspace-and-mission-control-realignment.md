# Sprint 034B Canonical Workspace And Mission Control Realignment

## Status

Sprint 034B is closed `canonical-realignment-complete-legacy-cleanup-deferred-clean`. Local implementation, validation and the required owner-operated Mission Control Re-detect passed.

Two already-absent legacy metadata directories remain Git-prunable but locked; no manual deletion was attempted.

## Starting authority

- Canonical root and Git top level: `C:\Users\rrank\OneDrive\PNR Precision Performance Canonical`.
- Standalone common directory: canonical `.git`; one registered canonical worktree.
- Branch: `codex/034B-canonical-workspace-and-mission-control-realignment`.
- Starting and current committed HEAD: `47d1df447e819b8ec31aaa085a0ff6aeff6e7e8b`.
- Origin: `https://github.com/rankin007/PNR-Precision-Performance.git`.
- The eight official method-v8 paths plus the Architect Pack, schema-v1 status and mid-flight roadmap were explicitly accepted as the complete control-plane baseline.

## Method v8 adoption

- Manifest version `8`, release `2026-07-25`, mode `existing-project`, source `https://launcher.120x.ai`.
- All seven declared payload SHA-256 values match exactly.
- The Architect Pack remains byte-identical; dry-run and application produced exactly the four generated Sprint 034B files.
- Both updated Node scripts pass syntax checks.
- The Pack README now documents only the supported Pack path plus optional `--dry-run` interface.
- The official custom-instructions prompt remains byte-identical, including its two intentional Markdown hard-line breaks.

## Canonical and Mission Control control plane

- `AGENTS.md` names the exact permanent canonical root and fails closed for the old OneDrive root and temporary worktrees.
- The mid-flight roadmap contains only 034B, 036 and 029N and does not authorize future work.
- STATUS uses schemaVersion 1 and contains no implementation-authorization gate.
- The briefing spec and template now require the complete v8 executive, readiness, Evidence, Plan corrections, Tests and Do/Owner/Decision contract.
- Current state, decisions, domain, risks, questions, schedule, lifecycle, evidence and inventory records identify the canonical workspace and preserve Sprint 035K production-not-promoted truth.

## Legacy disposition

- Three already-absent registrations identified by Git as prunable were pruned.
- The exact clean duplicate `C:\tmp\pnr-035k-live-trainer-access` passed branch, SHA, cleanliness, non-ignored-untracked, canonical-standalone and live remote-equality guards. Git's non-force worktree removal removed the directory and registration.
- Its stale metadata directory and the already-absent `pp-012d-production-baseline-clean` metadata directory remain Git-prunable but could not be deleted because of a permission/lock boundary. No manual deletion was attempted.
- The dirty legacy root, all branches and every other registered worktree/directory remain preserved.
- Detailed sanitized Git metadata is in `planning/reviews/034B-legacy-worktree-disposition.md`.

## Validation evidence

- `node --check scripts/apply-architect-pack.js` - passed.
- `node --check scripts/update-method.js` - passed.
- Pack dry-run - passed; exactly four 034B targets.
- Seven declared payload hashes - 7/7 exact.
- `node --experimental-strip-types scripts/test-live-trainer-access-035K.mjs` - 89 assertions passed, 0 failed.
- JSON validation - self-test 8 cases and 7 maintained JSON files passed.
- Static validation - encoding 967 maintained text files and seven static validator groups passed.
- Maintained CI components before the optional transport dependency - 11 passed.
- The unlisted `playwright-core` dependency was absent, so the 031B transport component was not rerun. This is a supporting dependency limitation; no package declaration was changed. The subsequent 031C, role and nine Supabase harness self-test groups passed directly.
- TypeScript - passed.
- Next lint - passed with zero warnings/errors.
- Production build - passed after granting the sandbox permission to create ignored `.next`; 29 static pages generated.

## Product and external invariants

No `app/`, `components/`, `lib/`, Supabase, migration, dependency declaration, product-test, authentication or public-enquiry file was edited. No provider, deployment, callback, alias, DNS, production, retained-identity or synthetic-fixture action occurred. Sprint 035K remains closed and production remains unpromoted.

## Mission Control acceptance

The owner clicked **Re-detect** once in the already signed-in Mission Control/Launcher session. Sanitized screenshot observations confirmed:

- `C:\Users\rrank\OneDrive\PNR Precision Performance Canonical` is connected and up to date;
- Sprint 034B is current and displayed as Sprint closed;
- validation shows 89 tests, all passing; and
- Business outcome, Current focus, What is proven and What is not live are populated.

Sprint 036 is recorded in the roadmap and briefing as the explicit next destination. The delivered count is one sprint since roadmap adoption: 034B. Sprint 036 and 029N remain unresolved and planned.

## Supporting Mission Control rendering limitation

The final owner Re-detect visibly loaded the latest local state: the canonical folder was up to date, Sprint 034B rendered closed, 89 tests rendered passing, the four executive fields were current, and Sprint 036 appeared in Current focus and Recommended next action.

The exact manifest-governed v8 roadmap template requires the standard heading/metadata, optional mid-flight history, the five-column table, `planned`/`done` statuses and optional consecutive phase bands. The current roadmap conforms: 034B is the sole `done` row, 036 and 029N are `planned`, the delivered-since-adoption statement is one sprint, and Sprint 036 is the explicit next destination. The official v8 template does not document the Delivered or Forward Plan card algorithms.

Mission Control nevertheless rendered Delivered as `0 sprints` and Forward Plan as `1 sprint - Through Release`. Because the same Re-detect demonstrably read the latest files while these two cards did not reflect the valid source rows, classify both as a hosted Mission Control rendering limitation under the Evidence-Proportional Execution Standard. Retain the truthful roadmap, STATUS, history and briefing unchanged; do not create another sprint or edit product/Mission Control application code solely for this display limitation.
