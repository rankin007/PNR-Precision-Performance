# Sprint 026 — Uploads And Evidence Management Closeout

Outcome: `uploads-and-evidence-management-complete-fail-closed-clean`.

## Delivered

- One cohesive authenticated evidence manager on the biochemistry result route.
- Safe server-side list projection with lifecycle wording, lineage, active-hold classification and server-authoritative capability flags.
- Upload, cancel/retry, exact-predecessor replacement, soft delete, restore request/execution, hold/release and separately permissioned governed purge controls.
- Consequence confirmations, duplicate-submission locks, refreshed state, live announcements, focus recovery, non-colour labels, 44 px targets and narrow-layout wrapping.
- Fail-closed blocked evidence with no preview, download or signed-read affordance.
- Accurate JPEG/PNG/PDF limits, disabled CSV, Singapore overseas-processing and role-controlled retention/removal wording.

All mutation controls are presentation hints only. The established lifecycle RPC freshly rechecks authenticated identity, active membership, test/horse/stable scope, role, separate purge permission, eligibility timing and active holds. Unknown state and failed list/action paths expose no consequential action or private internals.

## Validation and substitution

Passed: Sprint 026 focused synthetic test; Sprint 022 workflow; maintained 023E/023J domain, migration, upload and correction suites; JSON; roles; Supabase harness self-tests; encoding/static validators; ESLint; TypeScript; production build; and `git diff --check`.

The first typecheck could not start because the isolated worktree had no dependency directory. No install or update was permitted, so a worktree-local untracked junction reused the repository's existing dependency installation; TypeScript, ESLint and the production build then passed without dependency or lockfile change. No browser/renderer proof was run. Executable source/markup assertions plus the successful production build provide substitute evidence for keyboard semantics, live regions, touch targets, responsive wrapping and fail-closed UI absence; no rendered visual claim is made.

The delivered Pack contained mojibake in its four generated sprint files. Those generated files were mechanically normalised to valid UTF-8 in scope; the maintained encoding validator then passed.

## Changed-file manifest

- Route/actions: `app/(ops)/data-entry/biochemistry/[testId]/page.tsx`, `app/(ops)/data-entry/biochemistry/evidence-actions.ts`.
- UI: `components/ops/test-evidence-manager.tsx`, `components/ops/test-evidence-upload.tsx`, `components/ops/test-evidence-state.ts`.
- Safe contracts/projection: `lib/evidence/contracts.ts`, `lib/evidence/server/repository.ts`.
- Tests/registration: `scripts/test-test-evidence-026.mjs`, `scripts/fixtures/026-test-evidence/manifest.json`, `scripts/run-validation-suite.mjs`, `package.json`.
- Documentation/planning: `docs/TEST_EVIDENCE_MANAGEMENT_026.md`, the four Sprint 026 files, this closeout, and required current planning records.

Migrations `0018`–`0021` are unchanged. No dependency, provider, remote, Production, deployment, alias/domain, real-data, staging, commit, push, PR or merge action occurred. The original dirty `develop` and Sprint 025 worktrees were not modified.
