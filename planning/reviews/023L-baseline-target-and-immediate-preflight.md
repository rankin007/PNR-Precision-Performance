# Sprint 023L - Baseline, Target And Immediate Preflight

Local baseline passed at exact commit `a15d89b2f95382d77a3f3ed450e1f4f16f254b51`, parent `fcf818fe3a8001b12941adc9dd121c6dbe8c002f`. The committed ledger is exactly `0001` through `0019`; migration `0018` is byte-identical to its approved baseline; migration `0019` canonical SHA-256 is `67C0877038738EC5D3C4965DE10F3048D37D4E920407C4E675CB948C3450B80A`; production `.schema("storage")` references are zero.

Safe provider checks confirmed the approved Supabase organisation/project/name/region/status tuple and the approved Vercel team/project tuple. The Vercel account contains one known production domain at team scope; no Preview deployment or alias was created. The new 023L worktree is not linked to Supabase, so no CLI database query ran.

The authenticated dashboard confirmed the exact Supabase organisation, project, reference and Singapore region. During this read-only settings inspection, the dashboard view surfaced identifiable staff account data in browser inspection output. No password, token, key, URI, database row, Storage object or payload was queried or exposed, but the Sprint 023L real-staff-data prohibition requires an immediate stop. The aggregate SQL preflight did not run.

## Manual intervention

1. **Blocked/not working:** immediate preflight cannot continue in this run because the dashboard inspection surface exposed identifiable staff account data, contrary to the sprint evidence boundary.
2. **Evidence checked:** local baseline, ancestry, ledger, migration invariants, Supabase target tuple and Vercel team/project tuple passed; no database, Storage, configuration or deployment mutation occurred.
3. **Exact action required:** the authority owner must provide a fresh Builder instruction authorising a resumed 023L preflight through a constrained surface that bypasses member/account panels and returns only approved target identifiers, counts and classifications.
4. **Steps:** confirm no identifying dashboard data may be captured; direct Builder to use only focused project-field reads, the SQL Editor aggregate batch and environment-variable presence/scope checks; do not paste credentials or personal data; retain the existing exact targets and production exclusions.
5. **Builder verification afterward:** restart at immediate preflight, confirm exact targets through focused fields, run only `BEGIN TRANSACTION READ ONLY` aggregate SQL ending in `ROLLBACK`, prove Preview mapping without values, and stop on any nonzero/discrepant or identifying result before mutation.

Outcome after first attempt: `remote-immediate-preflight-privacy-exposure-blocked-clean`.

## Privacy-safe resumed attempt

The authority owner clarified that provider account metadata is not application-database content, must be excluded from evidence and does not by itself invalidate the synthetic classification. Builder resumed through direct SQL Editor navigation only and did not inspect broad administrative panels.

The focused project identity boundary remained exact. Builder submitted one aggregate-only batch intended to begin read-only and end with rollback. The browser-to-editor input transport corrupted the statement text; PostgreSQL returned a syntax-error classification before the aggregate statement executed. The editor returned zero result rows. No database row, Auth record, Storage record or payload was queried, and no transaction or mutation ran.

### Resumed manual intervention

1. **Blocked/not working:** the constrained aggregate preflight did not execute because the browser SQL-editor input transport altered the statement text.
2. **Evidence checked:** direct project scope was exact; editor source was the primary database and postgres role; the response was a syntax-error-before-execution classification with zero rows; no identifying information appeared during the resumed attempt.
3. **Exact action required:** the approved operator must clear the current private SQL Editor query manually and provide a fresh instruction to resume with an alternate constrained input method, or run the approved aggregate batch manually and report only pass/fail counts and classifications.
4. **Steps:** open the exact project SQL Editor; select all and clear the private query; do not run any statement; confirm only that the editor is blank and still scoped to the primary database/postgres role; never paste results, credentials, names or payloads into conversation.
5. **Builder verification afterward:** re-establish exact project scope, submit the bounded read-only aggregate batch through the newly approved input method, require one count-only result row and explicit rollback, and stop on any nonzero, unexpected or unclassifiable result.

Outcome after first constrained SQL attempt: `remote-immediate-preflight-query-transport-blocked-clean`.

## Approved alternate SQL input attempt

The operator confirmed the exact project SQL Editor was blank on the primary database with postgres role and that no query had run. Builder reopened the direct exact-project SQL Editor route and prepared a clipboard-based insertion with byte-for-byte editor verification required before execution.

The browser could not focus the unique SQL editor input within the constrained interaction window. The batch was not pasted or submitted. The temporary clipboard content was cleared immediately and the controlled tab was closed. No SQL, result row, transaction, database read or mutation occurred, and no identifying information appeared.

### Alternate-input manual intervention

1. **Blocked/not working:** the approved alternate browser input path failed before insertion because the SQL editor control could not be focused reliably.
2. **Evidence checked:** exact project URL scope passed; one intended editor control was identified; paste, structural verification and Run were never reached; temporary clipboard content was cleared; no query executed.
3. **Exact action required:** the approved operator must execute the verified bounded aggregate batch manually in the exact SQL Editor, or provide a protected non-browser SQL execution mechanism that preserves exact statement bytes.
4. **Steps:** keep the exact project, primary database and postgres role; paste the approved batch; visually verify it begins `BEGIN TRANSACTION READ ONLY;` and ends `ROLLBACK;`; run once; do not provide row contents or screenshots; report only whether exactly one aggregate row returned and whether every required zero/exact-ledger classification passed.
5. **Builder verification afterward:** accept only the safe aggregate classification, stop on any nonzero/mismatch/error, and continue to Storage-recovery readiness only if every immediate-preflight gate passed.

Outcome after alternate-input attempt: `remote-immediate-preflight-editor-focus-transport-blocked-clean`.

## Operator-executed constrained aggregate preflight

The approved operator manually executed the canonical bounded batch in the exact project SQL Editor and reported only the permitted classifications:

- read-only mode confirmed;
- exactly one aggregate row returned;
- remote migration ledger exactly `0001` through `0017`;
- every required legacy, collision, Auth, application and Storage zero classification passed;
- no error or ambiguous result; and
- explicit rollback completed.

No screenshot, SQL output, row content, account information, filename, object key, credential or payload was provided or recorded. Immediate preflight outcome: `remote-immediate-preflight-passed-clean`.
