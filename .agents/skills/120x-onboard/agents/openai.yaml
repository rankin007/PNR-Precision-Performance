interface:
  display_name: "120x Onboard"
  short_description: "Map an existing codebase onto the 120x method"
  default_prompt: "Use $120x-onboard to survey this existing project and bring it onto the 120x method."
policy:
  allow_implicit_invocation: false
