interface:
  display_name: "120x Fly (lean)"
  short_description: "Discover, approve, build, and verify one sprint"
  default_prompt: "Use $120x-fly-lean to run one reviewed 120x flight from this project folder."
policy:
  allow_implicit_invocation: false
