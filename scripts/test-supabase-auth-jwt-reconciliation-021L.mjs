import assert from "node:assert/strict";import {spawnSync} from "node:child_process";const r=spawnSync(process.execPath,["scripts/supabase-auth-jwt-reconciliation-021L.mjs"],{encoding:"utf8",windowsHide:true});assert.equal(r.status,0);const o=JSON.parse(r.stdout);assert.equal(o.checks.length,8);assert.ok(o.checks.every(x=>x.endsWith(":pass")));process.stdout.write("021L reconciliation self-tests passed (8/8).\n");
