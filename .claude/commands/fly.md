# /fly — Run one reviewed 120x flight

Run the portable 120x Fly workflow for this project.

Do this now:

1. Read `templates/method/120x-fly.md`.
2. Follow its layout rules before selecting the canonical Architect body.
3. Follow it exactly from the Architect's plain-English opening through the
   human `go`, Pack application, the class-scaled authority transition, Builder
   work, real checks, required independent review, standard close-out, and final
   read-back.
4. Do not substitute the standalone `/architect` or `/build` stopping behavior
   for the authority transition defined by Fly.

The shared doctrine is the workflow. This command is only its Claude Code entry
point.
