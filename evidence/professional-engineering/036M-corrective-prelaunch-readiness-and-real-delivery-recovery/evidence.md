# Sprint 036M Evidence

Current checkpoint: local gates passed; private provider/identity/trainer window not started.

- Focused recovery: 110/110.
- Focused provider: 90/90.
- Focused identity: 70/70.
- Focused trainer: 60/60.
- Unconditional Supabase API-key compatibility, including PKCE: 30/30.
- Focused total: 360/360.
- Retained 036K/036J/035K/managed-role/Owner/role/033B/commerce/enquiry proof: 943/943.
- Counted total: 1303/1303.
- Additional uncounted 033B migration-ledger gate: 42/42.
- TypeScript: pass.

Red-first proof found and corrected two contract defects: replacement graph cleanup could not ledger the same exact-owned IDs created for that graph, and partial provider mutations were latched after rather than before the mutating call. The compatibility gate then proved all six reviewed Product surfaces required the opaque-key fetch correction. The helper sends opaque application keys as `apikey`, removes only exact bearer duplication and preserves a different user-session JWT; browser, server, admin, middleware and PKCE callback construction now install it.
The protected dispatcher now reaches its native child mode; the irreversible legacy attempt is latched before the provider call. Pre-latch compensation is attempt-aware, discovers an ambiguous partial key create only by the exact sprint-owned names, deletes only sprint-created keys, preserves selected keys, restores all observed binding targets and five fixed aliases, and requires independent key/target/alias/candidate readback.

The exact-source gate compares every exported file to the accepted Git tree, compares .env.example to its committed blob bytes, rejects nested or root credential-shaped .env siblings, retains the randomized export path only in opaque memory and proves it absent. Trainer delivery uses one wrapper-owned ordinal and requires the exact Auth identity's last-sign-in time to advance after the trainer's phone-native verification; the controller still receives no address, code, cookie or session.

TypeScript, ESLint, production build (29 static pages), JSON parsing, JavaScript syntax, PowerShell AST and the uncounted 033B migration-ledger 42/42 gate pass. The authoritative disabled public-enquiry gate 029U passed 36/36. Same-Architect repair-diff PASS accepted only `prelaunch-recovery-blocked-clean`; this is not authority to start a private window.


Final authority readback also found and corrected three executable-flow gaps before the checkpoint: sanitized blocking phase results now stop the wrapper; all nine consumer classes require bounded private exact name/target inventories instead of projected completeness; and seven credential classes require finite provider-native disposition/readback rows instead of hardcoded outcomes. The irreversible phase and trainer phase each use a tested non-advancing sanitized handshake: legacy mutation then exact old-deployment rejection reconciliation, and graph creation/readback then phone-native delivery/session readback. The fake JSON-lines stream executes all eleven requests across the nine ordered phases.

After these corrections, focused 360/360, retained 943/943, TypeScript, ESLint, production build, migration-ledger, privacy, no-OTP-bridge, JavaScript syntax and wrapper AST were rerun and passed. Readiness remains conditional on the same Architect's full diff pass.

No provider, private identity, message, verification, deployment, alias, credential, application graph, Auth, Storage, commerce or public-enquiry action has occurred. External mutations and residue remain 0/0. No protected value was read or recorded.


## Repair round 1

The same-Architect full-diff repair moved pair and binding attempt latches before their calls, added end-to-end mutate-then-throw compensation cases, and made the exact-owned trainer fixture independently cleanable after the legacy latch. All eight graph failure positions prove reverse-order zero-residue cleanup. The trainer identity is a separately masked owner-approved exact Auth ID with independent Auth read, zero public.users preflight and last_sign_in baseline.

Operator-entered inventories, old-deployment counts and seven-class success rows are no longer accepted as authority. Native uncertainty lands unchanged-blocking or blocked-retained before the irreversible latch. The current native adapter cannot yet establish exhaustive consumer/old-deployment closure, so this remains a corrected local implementation checkpoint rather than ProtectedWindow readiness.

The single child now redirects stdin/stdout/stderr. A fixed 4097-byte asynchronous stderr drain treats any byte as a sanitized protocol failure; the executable canary passed without reproducing its content. Focused 360/360 and retained 943/943 pass after repair, for 1303/1303. External/private/provider/trainer/temp-export actions and mutations/residue remain exactly 0 and 0/0.
